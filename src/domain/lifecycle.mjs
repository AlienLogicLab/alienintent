// Project lifecycle vocabulary. Membership does not grant runtime dispatch authority.
export const lifecycleStatuses = Object.freeze([
  "CAPTURE", "SPECIFY", "PLAN", "TASKS", "READY",
  "IMPLEMENT", "VERIFY", "REVIEW", "ACCEPT", "DONE",
]);
