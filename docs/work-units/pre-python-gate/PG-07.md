PG-07: event-ingress — bounded design-document BIU
Outcome: Create one reviewable CANDIDATE design artifact docs/architecture/pre-python-gate/event-ingress.md. It is not an implementation unit, API stability claim or Founder-approved model. Only this document plus its local packet/readiness evidence may change.
Reuse source material cited below; do not replace existing decisions with fresh invention. Acceptance:
Specify direct webhook and outbound relay to one internal event contract, authentication/profile selection, identity/correlation/deduplication/reordering/replay and durable effects; no network exactly-once claim or polling; enumerate crashes around persist/ack/effect boundaries.
G13 event-ingress port and direct-webhook/outbound-relay adapters: Direct and outbound relay adapters share a profile-scoped ingress contract, authenticity and acknowledgment semantics; relay reconnect uses durable replay without polling.
Existing evidence: Authority §3; bin/alienintent.mjs; Issue1 webhook proof
G14 no-polling / async integration rule: Architectural integration uses events/asynchronous operations; polling has no normal path; exceptional timed retries are bounded and are not disguised polling.
Existing evidence: Authority §21; B-DISP D10; dispatcher event paths
G15 internal event model and once-only processing semantics: Domain events include identity/schema/profile/causation/invocation/version; duplicates/delay/reorder/crash scenarios yield effectively once-only effects with durable evidence.
Existing evidence: dispatcher.mjs; github-authority tests; Issue1 replay evidence
Completion: source/invariant mapping, explicit contract semantics with successful and failing examples, scope limitations, exact unresolved material Founder decisions (if any), independent content review. Verification evaluates this document against these rows and its source authority. Neighbor documents cannot be adopted implicitly; unresolved cross-document choices are recorded, not invented. All needed source documents exist in named local baselines. Agent-Ready is evaluating readiness to write the candidate design, NOT approval to implement.

Append assessment-input-common.md verbatim to reconstruct submitted input.
