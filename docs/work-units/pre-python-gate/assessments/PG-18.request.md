# Agent-Ready request — PG-18

Assess readiness for the documentation-only BIU at
`docs/work-units/pre-python-gate/PG-18.md` against the current Pre-Python Gate,
Architecture Authority, FD-01 record and affected candidate contracts. This is
an assessment of readiness to revise documentation, not Python readiness or
implementation authorization.

Founder authority to apply:

- **FD-02:** approved. Keep Execution Coordination, Invocation Runtime, Context
  Assembly, Evidence & Learning, and Installation as modules initially. Promote
  a module only when domain evidence demonstrates a distinct language,
  invariant boundary or ownership model.
- **FD-03:** approved with correction. Per-BIU capabilities use sensible
  profiles/conventions plus explicit BIU-specific additions, including shell,
  network, cloud, migration, service-control, deployment and other production
  authority when legitimately required. Required budgets fail closed: unknown
  or unmeasurable consumption is not zero, and an unenforceable required hard
  limit makes a provider path ineligible. Automatic release defaults ON and is
  configurable OFF. READY plus satisfied release policy proceeds to IMPLEMENT
  when ON; when OFF, a human explicitly authorizes release. Neither path may
  bypass readiness checks; the setting is deployment/profile policy.
- **FD-04:** approved. Require end-to-end authenticated events. Treat relay as
  transport, not domain/security authority. Use a deployer-controlled gateway
  where the upstream cannot provide the required authenticated envelope.
- **FD-05:** approved. Use inbox/effect-intent/outbox, expected versions,
  reservation fencing, idempotent effects and read-back/reconciliation. Do not
  adopt full event sourcing for this purpose. Unknown external outcomes block
  conflicting work rather than trigger blind retries.
- **FD-06:** do not apply the prior mixed-maturity pin. EOS is one coherent
  system/version. AlienIntent must not claim EOS conformance until EOS is
  audited, maturity inconsistencies are reconciled in EOS, and one internally
  consistent approved EOS version exists.

No Python, Node, FactoryChecks, EOS, runtime, service, credential, deployment,
GitHub or provider-spend mutation is authorized. Return a disposition with
scope/authority risks and the exact next action.
