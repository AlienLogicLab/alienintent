PG-08: scheduling-and-recovery — bounded design-document BIU
Outcome: Create one reviewable CANDIDATE design artifact docs/architecture/pre-python-gate/scheduling-and-recovery.md. It is not an implementation unit, API stability claim or Founder-approved model. Only this document plus its local packet/readiness evidence may change.
Reuse source material cited below; do not replace existing decisions with fresh invention. Acceptance:
Define bounded global/profile/repository admission, reserve-before-await ownership, dependencies, cancellation ordering, finite retry/backoff/jitter, crash recovery and ambiguous-worker handling; preserve evidence and never clean live or unowned resources.
G16 concurrency/cancellation/retry policy: Global/profile/repository bounds, atomic reservations, cancellation and finite retry budgets prevent double mutation and preserve live/ambiguous resources on recovery.
Existing evidence: dispatcher/worktree-manager; worktree-lifecycle tests; B-DISP D17,D18,D31
Completion: source/invariant mapping, explicit contract semantics with successful and failing examples, scope limitations, exact unresolved material Founder decisions (if any), independent content review. Verification evaluates this document against these rows and its source authority. Neighbor documents cannot be adopted implicitly; unresolved cross-document choices are recorded, not invented. All needed source documents exist in named local baselines. Agent-Ready is evaluating readiness to write the candidate design, NOT approval to implement.

Append assessment-input-common.md verbatim to reconstruct submitted input.
