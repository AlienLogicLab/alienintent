# PG-18 — apply FD-02 through FD-05 and record FD-06 EOS-normalization blocker

**Type:** documentation-only Pre-Python Gate revision BIU.

**Baseline:** `727de90685ebdd6cca48f61fd9b1948efc8bbc33`, clean `main` worktree.

**Authority:** direct Founder decisions dated 2026-09-19: FD-02, FD-03 (as
corrected), FD-04 and FD-05 are binding; FD-06 remains blocked pending EOS
normalization. The Founder decision text is preserved in the associated
assessment request and must be recorded in a binding decision record.

**Outcome:** update only Pre-Python architecture/design and decision evidence to
apply the approved internal Execution module decomposition, per-BIU capability
profiles with explicit additions, fail-closed budgets, automatic release ON by
default and configurable OFF, end-to-end event authentication with relay as
transport, and inbox/effect-intent/outbox durability with versions/fencing.
Record that AlienIntent has no EOS conformance baseline until EOS itself is
normalized to one internally consistent approved version.

**Excluded:** Python code or scaffolds; Node source, tests, configuration,
runtime, services or bootstrap behavior; FactoryChecks; EOS edits; GitHub or
other external mutation; deployment, credentials, provider spending and new
implementation BIUs.

**Acceptance:**

1. Binding records distinguish approved FD-02–FD-05 from blocked FD-06.
2. Automatic release is explicitly default ON and configurable OFF; both paths
   preserve readiness checks and generate attributable release authority.
3. Capability profiles avoid needless per-operation bureaucracy while retaining
   explicit BIU-specific authority, including powerful capabilities when needed.
4. Budget controls fail closed when a required hard limit cannot be enforced or
   consumption is unknown.
5. Relay is transport only; authenticated envelopes and a deployer-controlled
   gateway are required where an upstream source cannot provide the envelope.
6. Durable candidate contracts specify inbox/effect-intent/outbox, expected
   versions, reservation fencing, idempotent effects, read-back/reconciliation,
   and blocking of conflicting work after unknown external outcomes.
7. FD-06 states that EOS must be audited, reconciled and normalized in EOS
   before AlienIntent records any EOS inheritance/conformance baseline.
8. The 34-row gate matrix remains complete; no Python authorization or EOS
   conformance claim is made. Assessment, independent review and documentation
   checks are recorded before a local commit; no push occurs.

**Affected candidate contracts:** domain model, work/release, capabilities and
assurance, providers/budgets, hexagonal contracts, event ingress, persistence
and evidence, scheduling/recovery, configuration/installation, EOS inheritance,
authority reconciliation, gate matrix/report, execution record and validation.
