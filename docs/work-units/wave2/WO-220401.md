# WO-220401 — Conditional real-worker role outcome producer

Wave 2 BIU compiled from the independently reviewed planning revision on baseline `308188c`.
Candidate contract sha256 `e9c556e1492a9ee2a7f08d33cd9c4c5377d778dd8cd752f65fe8f9bb24272408`.

## Intent

Deliver the bounded planned extent: Conditional real-worker role outcome producer

## Requirements satisfied (extent)

- SF-REQ-039

## Fixed decisions

- SF-REQ-039 already authorizes deterministic Test Worker coverage of the canonical producer/verifier lifecycle. Execution Coordination owns lifecycle orchestration; Invocation Runtime owns real/test worker invocation behind WorkerProvider. No new owner is required.
- Closure records only actions actually performed/read back by local adapters; never populate required_closure_actions merely because a producer succeeded.
- Offline tests exercise the production orchestration path through composition, not a second mocked lifecycle.
- K2 / canonical_role_orchestration owns the broader success-collapse replacement inside Execution Coordination: producer success advances only to VERIFY, a distinct independent verifier evaluates the exact candidate, verifier rejection records findings and returns to IMPLEMENT under existing attempt/budget policy, and acceptance/review/closure remain canonical lifecycle operations. K1 does not implement that orchestration; K1 supplies only the durable real WorkerProvider outcome/readback seam that K2 consumes.
- WorkerProvider.start/read_back/cancel remains the common worker boundary for Real Worker and Deterministic Test Worker. Durable role, invocation, attempt, contract/candidate and finding/receipt correlation is retained in existing invocation/effect/execution evidence records. Extend the typed result contract only if revised Design Verification proves current WorkerOutcome cannot carry a required observable; do not introduce a separate RoleOutcomeRecord state owner by default.
- Before activating multi-role behavior in shared/live profiles, composition must prove the configured worker adapter and durable correlation/readback path. Missing/miscorrelated outcome holds the transition even if the worker process exits successfully. Existing unmodified profiles are not claimed to satisfy future multi-role proof until their revised design is implemented.
- Worker Port is the same worker-facing port/protocol used by production: WorkerProvider -> Deterministic Test Worker for scripted scenarios, beside WorkerProvider -> Real Worker -> provider. No test-specific lifecycle shortcuts or script-driven mark_done.
- Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: Under existing Execution Coordination and Invocation Runtime ownership, the non-scripted WorkerProvider path durably retains and reads back attributable role/invocation/attempt/contract/candidate outcome evidence across restart. Missing or miscorrelated durable result holds despite process success. The result remains compatible with the common WorkerProvider/WorkerOutcome contract; no parallel sidecar state owner.
- Owns capabilities: real_role_outcome_readback
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.
- DAG scope: SF-REQ-039 consumes this prerequisite. Binding FD-01/FD-02 assign released-execution lifecycle/result correlation to Execution Coordination and worker/resource invocation behind WorkerProvider to Invocation Runtime. K1 is scoped to proving the real WorkerProvider outcome/readback seam under those owners; it creates no new bounded context or state owner.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220101 (Issue #69, DONE): Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.
- WO-220102 (Issue #71, DONE): Definition/Observation/Verdict admission; immutable digest objects with fsync/install before CAS pointer; crash before/after pointer; missing/hash-mismatched records refuse verdict; unknown measurement remains null with reason; competing role/revision/authority records cannot mint PASS. Bridge to existing evaluate_verdict without new policy.

## Acceptance criteria / completion predicate

- Under existing Execution Coordination and Invocation Runtime ownership, the non-scripted WorkerProvider path durably retains and reads back attributable role/invocation/attempt/contract/candidate outcome evidence across restart. Missing or miscorrelated durable result holds despite process success. The result remains compatible with the common WorkerProvider/WorkerOutcome contract; no parallel sidecar state owner.

## Proportional proof plan

Proof fixture: **FX-K1** — local/composed or mechanical unless explicitly stated otherwise.

Material failure classes:
- The real WorkerProvider path durably correlates role, invocation, attempt, contract and exact candidate outcome evidence and reads it back across restart.
- Process success without a correlated durable result holds; it cannot advance lifecycle.
- Wrong role/invocation/candidate correlation holds rather than being accepted.
- Restart/readback preserves the original attributable identity and does not duplicate the result/effect.

Existing proof surfaces:
- `tests/execution_coordination/test_factory_coordinator.py`
- `tests/invocation_runtime/test_scripted_worker.py`
- `src/alienintent/execution_coordination/ports/worker_provider.py`
- `src/alienintent/invocation_runtime/application/real_worker.py`

Initial bounded commands:
- `python3 -m pytest -q tests/execution_coordination/test_factory_coordinator.py tests/invocation_runtime/test_scripted_worker.py`

Proof-policy constraint:
- Use one discriminating negative control per materially distinct failure class by default. Additional controls require a distinct invariant/risk; do not mutation-test implementation branches exhaustively.
- Focused tests and architecture checks run before any wider regression. Full repository regression is a terminal confidence check, not repeated per negative control.
- Retain intact/fault/restored evidence for the material controls actually selected.

Explicit non-claims:
- No new RoleOutcomeRecord sidecar/state owner is created by default.
- K1 does not implement canonical multi-role orchestration or remove the success-collapse fallback; that broader capability is K2.
- Split recommendation remains a compatibility constraint through the existing authority-block / HumanDecisionRequired carrier; K1 does not add a dedicated acceptance control for it.
- No full SF-REQ-039 capstone or live sovereignty claim is made.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.
- Binding FD-01/FD-02 assign released-execution lifecycle/result correlation to Execution Coordination and worker/resource invocation behind WorkerProvider to Invocation Runtime. K1 implements only the real outcome/readback seam under those owners; K2 owns canonical multi-role orchestration and removal of the success-collapse fallback.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Execution allocation

- PRODUCER: Morty via configured Claude provider.
- VERIFIER: JC via configured Codex provider, fresh independently owned worktree and exact candidate retrieval.
- WIP/concurrency: governed by active factory profile (currently one mutating execution slot).
- Maximum execution cycles: 3.
- Maximum replacement workers per phase: 1.
- Wall-clock: existing systemd supervision (120 min runtime + 2 min startup + 5 min stop grace).
- Tokens/cost: measured when exposed; otherwise UNKNOWN, never inferred zero.
- Current Codex capacity/usage availability is an execution-time capacity concern, not readiness authority; release must still fail/hold safely if the configured verifier is unavailable.

## Landing

SWF-19: merge the exact accepted candidate branch directly into the baseline branch, preserving the accepted SHA. **No pull request.**

## Stop and escalation

- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Readiness

A fresh native Agent Ready assessment of this exact work-unit revision is required before READY. READY is eligibility, not release.
Release/IMPLEMENT is explicitly not authorized by this document.
