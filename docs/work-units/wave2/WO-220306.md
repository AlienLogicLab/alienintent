# WO-220306 — Known-active liveness reconciliation

Wave 2 BIU compiled from the independently reviewed planning revision on baseline `6ea9284`.
Candidate contract sha256 `26d556a0adf1bf39c90e51c0a93ff68e8c7984b94616f9611d080918865f2628`.

## Intent

Deliver the bounded planned extent: Known-active liveness reconciliation

## Requirements satisfied (extent)

- SF-REQ-056

## Fixed decisions

- Policy defaults: G=300 seconds, I=60 seconds, confirmation bound C=90 seconds; all configurable positive finite durations, persisted with policy digest and tested just below/at boundaries. These are design defaults, not a claim that historical bootstrap 5 minutes is a canonical invariant. Missing/invalid policy blocks startup.
- Periodic scan is limited to SF-REQ-056 explicitly specified reconciliation of known active state; it is not a general polling integration or work discovery loop.
- Effectively-once action requires idempotent/fenced consumer and durable readback; physically exactly-once network delivery is not promised. If an existing provider cannot prove these, park rather than relax the boundary.
- SF-REQ-056 is already binding P0/Wave 2 authority under SWF-29. L1 local/composed liveness reconciliation may proceed without a second ratification decision. Operational replacement of the bootstrap liveness service, monitor-host supervision, live transport proof and retirement remain separately gated; no new nonterminal retry semantics or budget reset are introduced.
- Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: With binding SF-REQ-056 authority: fake-clock G=300/I=60/C=90 boundaries, active/pending/completed/unknown/terminal/no-closure cases, and unavailable-evidence holds. Delayed original plus two contenders and crash/restart retain one authorized effect/readback. Completed judgment suppresses indefinitely; valid resolution permits reinspection only. Guard- and suppression-removal controls fail. No status toggles/new retries. Local proof does not claim operational monitor-host replacement.
- Owns capabilities: liveness_reconciliation
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220103 (Issue #74, DONE): Two SQLite connections/processes compete; remove fence guard to kill assertion. BEGIN IMMEDIATE checks vector, reservation, epoch/authority and unique intent atomically at intent and claim; validate receipt at confirmation. Crash around intent/send/readback, delayed sender, sorted multi-reservation locking, unknown outcome retain ownership. Consumer dedupes and fences or refuses reassignment until confirmed death/completion.
- WO-220301 (Issue #78, DONE): DONE/judgment events dedupe across restart; notification failure preserves pending. SEEN is not RESOLVED; wrong actor/revision/lane cannot resolve. DecisionInbox remains distinct. Unbound activation causes zero model launches. Preserve origin IDs, history and handler attribution.
- WO-220304 (Issue #96, DONE): Persist instance/generation, tick and scan outcomes independently of model life/workload. At 2*I remains in bound, above becomes STALE; failed/incomplete scan DEGRADED, absent/unreadable/regressing clock UNVERIFIED. End episode without losing local monitor state; this is a service/inspection fixture, not deployment supervision.

## Acceptance criteria / completion predicate

- With binding SF-REQ-056 authority: fake-clock G=300/I=60/C=90 boundaries, active/pending/completed/unknown/terminal/no-closure cases, and unavailable-evidence holds. Delayed original plus two contenders and crash/restart retain one authorized effect/readback. Completed judgment suppresses indefinitely; valid resolution permits reinspection only. Guard- and suppression-removal controls fail. No status toggles/new retries. Local proof does not claim operational monitor-host replacement.
- SF-REQ-056-AC-01: Drop the original trigger for an authorized known BIU whose state age exceeds configured grace G: at the next reconciliation scan (interval I), identify the expected missing effect and submit one authorized recovery intent. Before G, do not recover. Under a progressing test clock the detection bound is G + I after entry.
- SF-REQ-056-AC-02: Fixtures with an active matching invocation, pending claim/effect or correlated completion awaiting projection yield no recovery invocation. ACCEPT without a required closure effect and terminal BIUs yield none.
- SF-REQ-056-AC-03: Race delayed original delivery, duplicate recovery scans and two reconciler attempts for the same effect identity; restart between durable intent and effect completion. The fixture must show one effective authorized action and correlated readback, with stale claims refused. A watcher-only absence check is insufficient.
- SF-REQ-056-AC-04: A completed FOUNDER_EXCEPTION, HumanDecisionRequired or equivalent judgment-blocking result suppresses relaunch after any number of grace periods and creates or retains one attributable durable attention item.
- SF-REQ-056-AC-05: A seen-only notification leaves suppression in place. Explicit resolution under applicable authority (including an attributable resolving acknowledgment) or a newer correlated non-judgment outcome permits reevaluation; stale/out-of-lane resolutions do not.
- SF-REQ-056-AC-06: Recovery follows canonical effect paths and retains lifecycle meaning without status toggling. If required authority/custody/fencing evidence is unavailable or inconsistent, record a hold/attention rather than launching speculatively.
- SF-REQ-056-AC-07: An isolated negative control removing durable identity fencing fails the concurrent/duplicate fixture, and removing judgment suppression fails the completed-outcome fixture. Retain proven-red output plus restored passing evidence.

## Proportional proof plan

Proof fixture: **FX-L1** — local/composed or mechanical unless explicitly stated otherwise.

Material failure classes:
- After configured grace, a known active lifecycle state with complete evidence of a missing expected actor/effect yields one reconcilable liveness gap.
- Active, pending, completed or unknown correlated effect evidence suppresses duplicate recovery; unknown evidence holds rather than fabricating absence.
- A completed judgment-required outcome suppresses relaunch until matching resolution/supersession.
- Delayed original delivery plus competing recovery and restart still yields one authorized effect/readback identity.
- Stale/unavailable monitor-health evidence prevents claiming the bounded G+I operational guarantee.

Existing proof surfaces:
- `tests/control_plane/test_monitor_health.py`
- `tests/control_plane/test_attention.py`
- `src/alienintent/control_plane/application/monitor_health.py`

Initial bounded commands:
- `python3 -m pytest -q tests/control_plane/test_monitor_health.py tests/control_plane/test_attention.py`

Proof-policy constraint:
- Use one discriminating negative control per materially distinct failure class by default. Additional controls require a distinct invariant/risk; do not mutation-test implementation branches exhaustively.
- Focused tests and architecture checks run before any wider regression. Full repository regression is a terminal confidence check, not repeated per negative control.
- Retain intact/fault/restored evidence for the material controls actually selected.

Explicit non-claims:
- No work-discovery polling is introduced.
- No bootstrap liveness service retirement or live monitor-host replacement is claimed.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Execution allocation

- PRODUCER: Morty via configured Claude provider.
- VERIFIER: JC via configured Codex provider, fresh independently owned worktree and exact candidate retrieval.
- WIP/concurrency: governed by active factory profile (currently one mutating execution slot).
- Maximum execution cycles: 3.
- Maximum replacement workers per phase: 1.
- Wall-clock: existing systemd supervision (120 min runtime + 2 min startup + 5 min stop grace).
- Tokens/cost: measured when exposed; otherwise UNKNOWN, never inferred zero.
- Current Codex capacity/usage availability is an execution-time capacity concern, not readiness authority; release must still fail/hold safely if the configured verifier is unavailable.

## Landing

SWF-19: merge the exact accepted candidate branch directly into the baseline branch, preserving the accepted SHA. **No pull request.**

## Stop and escalation

- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Readiness

A fresh native Agent Ready assessment of this exact work-unit revision is required before READY. READY is eligibility, not release.
Release/IMPLEMENT is explicitly not authorized by this document.
