# WO-220102 — Typed immutable evidence and neutral references

Wave 2 BIU candidate compiled from the approved Wave 2 design (DAG node S1). Baseline `2528e3acd773f53de116f6f28537244d1f1f8b2f`.
Candidate contract `docs/evidence/wave2-candidate-bius.json#/bius/1` (sha256 `d74262cfb09af5d127832762983b8b2c368b075cdd9b6d9fde7ad7cda8ee1710`).

## Intent

Deliver the bounded planned extent: Typed immutable evidence and neutral references

## Requirements satisfied (extent)

- SF-REQ-016

## Fixed decisions (from the verified Design Contract; not reopened here)

- (SF-REQ-016) JSON scalar types and explicit null/uncertainty distinguish unavailable measurements from zero.
- (SF-REQ-016) No learning promotion or RAI wiring; definitions cannot be authored by evidence ingestion.
- The pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within this node extent. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- DAG scope: evidence_learning port/local adapter; shared neutral Ref values; composition/evidence_profile.py; no inventory application dependency.
- Extent: Definition/Observation/Verdict admission; immutable digest objects with fsync/install before CAS pointer; crash before/after pointer; missing/hash-mismatched records refuse verdict; unknown measurement remains null with reason; competing role/revision/authority records cannot mint PASS. Bridge to existing evaluate_verdict without new policy.
- Migration M-EVIDENCE (additive): create version-1 immutable external objects; validate exact digests and typed links; CAS versioned operational pointers only after durable write. Rollback retains old references and objects; incompatible schemas hold. No GC or source-authority migration.
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.
- Owns capability: typed_evidence_repository

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220101 (S0): consume its exact independently verified predecessor candidate and retained proof receipts. Its planned capability alone is not available execution substrate; predecessor authority holds must be discharged by their named actor.
- Required predecessor completion predicate: Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.
- Retained predecessor receipts: accepted candidate `761a3cb4d6e24dc24ec370de45e25fe8c505eeda`; independent verifier receipt [Issue #69 comment](https://github.com/AlienLogicLab/alienintent/issues/69#issuecomment-5777072691); landing/closure receipt [Issue #69 comment](https://github.com/AlienLogicLab/alienintent/issues/69#issuecomment-5780241887); `docs/evidence/wave2-proof-fixtures/FX-S0/{execution-record.json,run-report.json,proven-red.json}`; and `docs/work-units/wave2/WO-220101.release.md`. These receipts preserve the reported success-collapse and platform limitations; S1 does not reinterpret or repair them.

## Acceptance criteria

- Definition/Observation/Verdict admission; immutable digest objects with fsync/install before CAS pointer; crash before/after pointer; missing/hash-mismatched records refuse verdict; unknown measurement remains null with reason; competing role/revision/authority records cannot mint PASS. Bridge to existing evaluate_verdict without new policy.
- SF-REQ-016-AC-01: Given a requirement definition, a worker statement that tests passed and a test result, retain each with its role; none alone creates an authorized PASS verdict.
- SF-REQ-016-AC-02: A verdict identifies the exact definition revision, evidence inputs and authorized evaluator/policy. Missing evidence or mismatched revisions leaves the claim UNVERIFIED or blocks acceptance.
- SF-REQ-016-AC-03: A conflicting observation is preserved alongside earlier evidence and causes reevaluation/hold under the applicable policy rather than rewriting the requirement.
- SF-REQ-016-AC-04: A fixture presenting an observation or learned hypothesis as an authoritative definition is rejected at the admission boundary; a properly authorized versioned definition remains admissible.

## Verification obligations

Proof fixture FX-S1 (PLANNED_NOT_EXECUTED):
Definition/Observation/Verdict admission; immutable digest objects with fsync/install before CAS pointer; crash before/after pointer; missing/hash-mismatched records refuse verdict; unknown measurement remains null with reason; competing role/revision/authority records cannot mint PASS. Bridge to existing evaluate_verdict without new policy.

Probe SF-REQ-016-AC-01: Store three separate roles; worker/test observations alone cannot authorize verdict.
Probe SF-REQ-016-AC-02: Refuse missing or mismatched definition/evidence/evaluator refs.
Probe SF-REQ-016-AC-03: Append competing observation and invalidate applicability without modifying definition.
Probe SF-REQ-016-AC-04: Reject observation/hypothesis promotion; admit correctly authorized definition.
Blocking enforcement 016-role-boundary: remove kind/authority/revision validation individually; injected role promotion or stale evidence must fail acceptance assertion.

Evidence contract: Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.

Order: Before implementation, pin executable fixture commands, inputs, expected outcomes and evidence schema against this node predicate; demonstrate intact/fault/restored discrimination for mechanical controls. Retain independent judgment where specified. S0 establishes the shared harness at current interfaces; all consumers wait for predecessor proof. No future fixture is claimed executed here.

Operational fixtures require their separately authorized target and actual readback. Local substitutions must be labelled and cannot satisfy operational acceptance.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.
- evidence_learning port/local adapter; shared neutral Ref values; composition/evidence_profile.py; no inventory application dependency.

## Capabilities

Needs: existing_kernel_fixture, local_proof_transport. Provides: typed_evidence_repository.

## Execution Packet and Allocation (bound 2026-09-23)

Allocation decision: `docs/evidence/wave2-execution-packets/WO-220102.allocation.json` — deterministic Allocator branch; PRODUCER = configured `codex` worker, VERIFIER = configured independent `codex` worker (live self-hosting profile).

Limits (SWF-09 dimensions): concurrency 1 (enforced by lane claim); attempts 3 execution cycles (SWF-32 semantics) and retries 1 replacement worker per phase (hard-enforced by the retained per-BIU dispatcher guard once its exact normal Issue identity is bound in the active `execution.biuLimits` profile); cancellation via owned systemd unit and operator stop; wall-clock hard limit 120,000 ms startup + 7,200,000 ms runtime + 300,000 ms stop grace = 7,620,000 ms (127 min) per invocation, enforced by configured systemd supervision. Tokens/cost are measured if reported, UNKNOWN otherwise; no numeric token/cost ceiling is allocated. Remaining allowance: 3 of 3 cycles. Profile binding is a release-admission prerequisite; no unbound attempt/replacement tracking is relied upon.

Release authority: the binding standing SWF-35 grant applies; it authorizes the Factory Director to release this BIU only after a fresh native Agent Ready READY assessment, Work Management READY, retained predecessor evidence, the exact active-profile binding, and passing release admission. This packet alone does not transition IMPLEMENT or bypass any prerequisite.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

## Stop and escalation

Stop: Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate: Route unmet source/product authority through the Factory Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Readiness

This packet requires a fresh native Agent Ready assessment before Work Management READY. The native assessment is [WO-220102.2026-09-23T052603.078312Z.assessment.json](../../evidence/wave2-readiness-assessments/WO-220102.2026-09-23T052603.078312Z.assessment.json): `READY`, produced by Agent Ready CLI `0.1.0rc1` at `6b056813f6e3643219b4fac50be2d2f2785536a7`, input sha256 `9312d1c586a949bcafe3fcedba7edc97e063909cdaa89228dff449ee19b3b161`. This is not a release verdict; Work Management READY and release admission remain required.
