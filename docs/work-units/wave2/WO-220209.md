# WO-220209 — Readiness lint and retained assessment consumer

Wave 2 BIU compiled from the approved Wave 2 design (DAG node U9).
Baseline `ffbc993`. Candidate contract sha256 `0adf1ddfc35b430c91275b54faf26c40445c45b8d80c314d294ef96261add0f0`.

## Intent

Deliver the bounded planned extent: Readiness lint and retained assessment consumer

## Requirements satisfied (extent)

- SF-REQ-015

## Fixed decisions (from the verified Design Contract; not reopened here)

- (SF-REQ-015) Known MCP parsing: isError=true is failure; collect structuredContent disposition and parseable JSON text-content dispositions. Require at least one recognized terminal value and agreement among every supplied value. Unknown/malformed/conflicting purported result is failure. Preserve wrappers. Ownership per #/contracts/4/ownership_statement (DV-5). Producer/version provenance per #/contracts/4/provenance_contract (DV-5). Reuse Before Build: the public Agent Ready product already exists; use its CLI/MCP, never raw model prompts shaped as its schema. check_assessment_producer.py supplies negative shape evidence, not positive producer authentication. tools/orchestration/readiness_assessment.py is an interim programme adapter/provenance example, not the product implementation or proof of trustworthy provenance by itself. Require configured invocation-to-result custody and independent producer checks at admission.
- (shared contracts, `#/fixed_design_baseline/shared_contracts`) Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result.
- Owns capabilities: readiness_consumer
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220205 (U5, Issue #97): Existing architecture checks first, then distinct reviewer invocation bound to exact design/proof/premise revisions. Missing decisions, impossible premise, self-review and stale revision hold. Open directional-authority question returns ARCHITECTURE_AUTHORITY_HOLD for affected admission; observed_edges never becomes policy.
- WO-220207 (U7, Issue #101): Reject incomplete contract, uncovered requirement/acceptance/verification/evidence obligation, cyclic/missing/predicate-free edge, stale design or unauthorized/nonconserving replan. Validate deterministic identity rewrites, preserved predicates, lineage and immutable stale-assessment history. A hand-authored mapping fixture proves validation only; U8 must derive initial decomposition.

Dependency state at this baseline (canonical read-back): WO-220205 (#97) is DONE; WO-220207 (#101) is READY and not released. This BIU is preparation only: it must not move to READY until its executable proof packet is concretely pinned, and it must not be released before WO-220207 is DONE. Admission: Consume exact independently verified predecessor candidate and retained proof receipts; predecessor authority holds must be discharged by their named actor. A planned capability is not available execution substrate.

## Acceptance criteria

- Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result.
- SF-REQ-015-AC-01: A complete pinned BIU may proceed to assessment; fixtures missing a decision, boundary, verification obligation, architecture constraint or dependency produce an attributable lint hold before implementation.
- SF-REQ-015-AC-02: READY with applicable inputs -> eligibility for separate SF-REQ-002 release gates only; CLARIFY -> resolve only material owner questions through SF-REQ-035 Decision Inbox, then reassess; SPLIT -> authorized SF-REQ-013 Split Transaction, invalidate stale applicability, lint and reassess each materialized result including a retained Integration Parent; HOLD -> satisfy prerequisite, then reassess. No non-READY disposition authorizes implementation and no resolution rewrites an old assessment.
- SF-REQ-015-AC-03: After a material contract, baseline, governing decision or prerequisite change, a fresh assessment links the prior immutable assessment. Resolving a blocker cannot rewrite the old verdict to READY.
- SF-REQ-015-AC-04: A direct readiness object and a known MCP envelope carrying the same disposition yield the same semantic handling while preserving raw evidence. Conflicting text/structured values, unknown disposition, missing terminal result, timeout or provider failure produce an attempt failure and hold, never synthesized readiness.
- SF-REQ-015-AC-05: Replaying the retained PY-10 bootstrap-assessor history preserves verbatim BLOCKED → SPLIT_RECOMMENDED → READY, with READY a distinct fresh assessment after prerequisite completion and all prior revision references retrievable. These are historical Surrogate Readiness Assessments, not an Agent Ready sequence; retention does not coerce or relabel historical verdicts.

## Verification obligations

Proof fixture FX-U9 (PLANNED_NOT_EXECUTED, proof level LOCAL_COMPOSED_OR_MECHANICAL):

- Environment: Disposable local profile unless explicitly marked operational; no live action authorized by this plan.
- Scenario: Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result.
- Acceptance IDs: SF-REQ-015-AC-01, SF-REQ-015-AC-02, SF-REQ-015-AC-03, SF-REQ-015-AC-04, SF-REQ-015-AC-05
- Probe SF-REQ-015-AC-01: Missing lint duties yield pre-assessment hold and zero calls. 015-composed-authority covers unbound/surrogate composition with CAPABILITY_PROVENANCE_HOLD and no release. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)
- Probe SF-REQ-015-AC-02: READY with applicable inputs -> eligibility for separate SF-REQ-002 release gates only; CLARIFY -> resolve only material owner questions through SF-REQ-035 Decision Inbox, then reassess; SPLIT -> authorized SF-REQ-013 Split Transaction, invalidate stale applicability, lint and reassess each materialized result including a retained Integration Parent; HOLD -> satisfy prerequisite, then reassess. No non-READY disposition authorizes implementation and no resolution rewrites an old assessment. Probe all four routes, stale applicability and reassessment of every split result; no generic planning hold substitutes for the handoff. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)
- Probe SF-REQ-015-AC-03: Input fingerprint change appends new assessment with immutable predecessor, old READY inapplicable. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)
- Probe SF-REQ-015-AC-04: Known direct/MCP parity; conflict, timeout, error, unknown, missing terminal all failure/hold. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)
- Probe SF-REQ-015-AC-05: Replaying the retained PY-10 bootstrap-assessor history preserves verbatim BLOCKED → SPLIT_RECOMMENDED → READY, with READY a distinct fresh assessment after prerequisite completion and all prior revision references retrievable. These are historical Surrogate Readiness Assessments, not an Agent Ready sequence; retention does not coerce or relabel historical verdicts. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)

Enforcement obligations:

- `015-envelope-applicability` (python_unit_integration, BLOCKING): Equivalent retained direct and known MCP shapes have identical consumer handling; all non-READY/failure/stale cases hold. Proven-red obligation: Use top-level-only parsing, trust exit zero or overwrite prior outcome; corresponding fixture must fail.
- `015-composed-authority` (python_unit_integration, BLOCKING): Reuse LRN-002 at actual readiness construction/admission with local transport and real temporary SQLite: unbound or unestablished product/package provenance returns CAPABILITY_PROVENANCE_HOLD; a supported product binding reaches the consumer, preserving raw bytes and complete provenance. Test doubles are explicitly fixture producers and cannot discharge native-producer proof. Proven-red obligation: Supply a schema-perfect surrogate READY, copied provider_evidence, wrong package identity or a response disconnected from its invocation; each must fail native admission. Bypass provenance checking independently: corresponding refusal assertion must fail. Intact identified public-interface binding reaches consumer without release bypass.

Order: Before implementation, pin executable fixture commands, inputs, expected outcomes and evidence schema against this node predicate; demonstrate intact/fault/restored discrimination for mechanical controls. Retain independent judgment where specified. S0 establishes the shared harness at current interfaces; all consumers wait for predecessor proof. No future fixture is claimed executed here.

Conditional proof: Operational fixtures require their separately authorized target and actual readback. Local substitutions must be labelled and cannot satisfy operational acceptance.

Evidence contract: Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.

## Execution packet and allocation

Allocation: `docs/evidence/wave2-execution-packets/WO-220209.allocation.json` — deterministic
Allocator branch; configured PRODUCER (Morty, Claude) and independent VERIFIER (JC, Codex);
concurrency 1; 3 execution cycles and 1 replacement worker per phase (`execution.biuLimits`,
bound before release); wall-clock bounded by systemd supervision (120 min runtime + 2 min
startup + 5 min stop grace); tokens and cost measured, never assumed zero. Remaining
allowance: 3 of 3 cycles.

Landing: SWF-19 — merge the accepted candidate branch directly into the baseline branch,
preserving the accepted SHA. **Do not open a pull request.**

Release authority is not granted by this document.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Stop and escalation

Stop:
- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Pinned proof packet (FX-U9)

Pinned by Factory Director episode `factory-director-e88e1e81df3a416a9dcfed5b0f0cf6e3`, acting on the standing Founder
direction `founder-priority-no-starvation-quality-20260925T043140Z` and continuing the handoff
`director-handoff-wo220209-prep-20260925T0025Z`, from the draft
`docs/evidence/wave2-execution-packets/WO-220209.proof-packet.draft.md` (sha256
`9c0f20c5f55175b8df4828838a9c1ddd0ac951dc898c44b402bec85d31547ac7` as reviewed, before its status line was updated to
`DRAFT_SUPERSEDED_BY_PIN`, prepared read-only at `8d7b2cb` by Factory Director episode
`factory-director-b72fe86b08d04397aedd046d9ef6562e`). The Director reviewed it, updated the worktree
to `origin/main` (this Director worktree had drifted to `8d7b2cb`; `git fetch origin && git checkout origin/main`
moved it to `0588eae`, the tip that also carries the WO-220207/U7 implementation and the WO-220302/WO-220304
landings), re-verified every input digest below at that baseline, and re-read the load-bearing clauses:
`D#/nodes/11/completion_status`, `/proceeds_regardless`, `/completion_predicate`; `C#/contracts/4/bounded_context`,
`/ports`, `/composition`, `/provenance_contract`. The admission blocker recorded in the draft — "WO-220209 must not
be released before WO-220207 (#101) is DONE" — is now satisfied: #101 reached DONE (merge `0588eae`) after this
draft was written. Nothing here authorizes release, implementation, or live operation.

### Director dispositions (these control over the draft text below)

| Q | Disposition | Pinned answer and authority |
|---|---|---|
| U-1, U-2 | DISCHARGED | By the reviewed candidate repair (`ffbc993`, review noted in that commit) and re-compile (`8d7b2cb`). Unchanged at `0588eae` (`git diff --stat ffbc993 0588eae -- docs/evidence/wave2-dependency-dag.json docs/evidence/wave2-candidate-bius.json` is empty for these pointers). |
| U-3 | **DECIDED, Director-confirmed** | `D#/nodes/11/completion_status` = `PROCEEDS_REGARDLESS`; `/proceeds_regardless` names exactly "Consumer semantics, retained-envelope parity and provenance/admission refusal tests at current interfaces; Agent Ready product ownership is settled by A1." FX-U9 does not wait on independent Phase 10 Design Verification of contract 4. Label `CONTRACT4_NOT_INDEPENDENTLY_DESIGN_VERIFIED` applies throughout, mirroring the FX-U7 N-1 precedent (`WO-220207.md` line 116). |
| U-4 | **DELEGATED, Director-confirmed**, with a binding condition | At baseline `0588eae`, the Director re-scanned every non-test `.py` file under `src/alienintent/context_assembly` for `from alienintent.<pkg>` imports (measured, not repeated from the draft): the only cross-group targets are `execution_coordination.domain`, `execution_coordination.ports`, `evidence_learning.domain`, `evidence_learning.ports` and `control_plane.application` (the existing `decision_resolution.py:4` pair the draft already named) — no `execution_coordination.application` or `execution_coordination.adapters` import, including from the newly landed `context_assembly/application/compilation_validation_service.py`. A matching reverse scan of `src/alienintent/execution_coordination` for any `alienintent.context_assembly` import returned none. Landing WO-220207 introduced no new cross-group pair in either direction. The Director confirms the draft's placement: lint and CLARIFY/SPLIT routing in `context_assembly`; consumer, port and `adapters/assessment_consumer.py` in `execution_coordination` with no `context_assembly` import; wiring only in `composition/upstream_profile.py`. **Binding condition unchanged (FX-U7 D-1 precedent):** no new cross-group module pair. Any new pair is a stop and returns the directional question to the Founder under `R2-GAP-051-EDGE-AUTHORITY`. |
| U-5 | DELEGATED (`LINT_DUTY_SOURCES_v1`, as drafted) for the decision/boundary/verification/architecture/dependency mapping; the U7-dependent part is **now DISCHARGED, not FOUNDER-reserved** | WO-220207 (#101) is DONE. The landed `CompilationValidation.validate`/`.validate_split` (`context_assembly/application/compilation_validation_service.py:42`/`:46`) persist a `ValidationReport`/`CompilationHold` (`context_assembly/domain/compilation.py:124`/`:107`, `.document()` at `:131`/`:115`) as an immutable `compilation.validated`/`compilation.held` Observation, CASed under an `upstream:compilation:<candidate_digest>` pointer with its own `history` chain (`compilation_validation_service.py:38-40`, `:116-134`). Whether U9's lint additionally consumes a `compilation_validation_hold` for the candidate (`D#/nodes/11/depends_on` names it) is still a PRODUCER engineering choice — DELEGATED, not blocked by a missing type. |
| U-6 | DELEGATED (`FX_U9_FIXTURE_SOURCES_v1`, as drafted) | Unchanged; the 22 retained records at `ffbc993` are unchanged in count and content at `0588eae` (`docs/evidence/wave2-readiness-assessments/` gained no new file between the two revisions). |
| U-7 | **DELEGATED, Director-confirmed** (`INTACT_BINDING_WITHOUT_LIVE_INVOCATION_v1`) | As drafted: FX-U9's intact path is the composed binding validated against provenance values (executable path, distribution metadata, correlated attempt UUID/input fingerprint), never a live Agent Ready invocation. The rejected alternative (a live invocation inside FX-U9) remains FOUNDER_RESERVED/separately authorized and is not proposed. This is unchanged by U7 landing, which supplies no new fact about the Agent Ready provenance boundary. |
| U-8, U-9, U-11 (store/evidence), U-12, U-14, U-15 | DECIDED/DELEGATED as drafted | Unchanged; none depends on WO-220207. |
| U-10 | DELEGATED (`SPLIT_HANDOFF_CONSUMER_PORT_v1`, as drafted: U9 owns only a consumer-side port and hands off the raw assessment); the U7-dependent part is **now DISCHARGED** | No `FrozenSplitProposal` class exists in the landed implementation — the draft's speculative name does not match the shipped code. `validate_split(proposal, original, authority_limits, identity_policy, stages, history) -> ValidationReport | CompilationHold` (`context_assembly/domain/compilation.py:442`) operates directly on a plain `dict` SPLIT_REPLAN proposal; there is no separate frozen-proposal value type to reuse. U9's own SPLIT handoff still does not call `validate_split` — U9 hands the raw assessment to U8's `SplitTransaction` port (`C#/contracts/2/ports/3`), which is not itself landed (`D#/nodes/10`, GAP_BLOCKED) — so U-10 remains scoped exactly as drafted. The only change is that the draft's "waits for their landed signatures" caveat is discharged: the signatures are now known and cited here, and there is nothing further to wait for before pinning. |
| U-11 (naming), U-13 | DECIDED as drafted, plus independent Director re-verification | U-13: the Director independently re-extracted all three PY-10 blobs at this baseline (`git cat-file blob <sha> \| sha256sum`) and confirms each raw-blob digest byte-for-byte against the table in fixture input 8 below: `3c53cd3d…` → `093a9d2c…` (BLOCKED), `fe1e3efe…` → `1fc95f25…` (SPLIT_RECOMMENDED), `30ea7c45…` → `9561bf71…` (READY). All three match. The replay's expected refusal observable (P19) is confirmed as drafted. |
| Admission | **DISCHARGED** | WO-220207 (#101): DONE. Accepted candidate `9d97365f58d41147923d3070ac2f838937ed0f4f` (branch `b-disp/d48a98bd-ac48-4f98-9745-2622c4e57064`), independent JC VERIFIER ACCEPT https://github.com/AlienLogicLab/alienintent/issues/101#issuecomment-5824501971 (invocation `b87bfc8e-901c-4e3e-bb60-82c64a952609`), post-ACCEPT closure and SWF-19 direct merge `0588eae74bbbdddfd8650e349fff472f0db20eff` https://github.com/AlienLogicLab/alienintent/issues/101#issuecomment-5826622726. FX-U7 evidence: `docs/evidence/wave2-proof-fixtures/FX-U7.md` and `docs/evidence/wave2-proof-fixtures/FX-U7/` (43/43 discriminating controls, `independent_verdict: PENDING_FRESH_BIU_VERIFIER` resolved by the JC ACCEPT above, `live_proof: NOT_ESTABLISHED`). Project #1 status DONE, Director board read-back at this baseline (`python3 tools/live/project_materialization.py verify 101` reports `Status 'DONE'`). |

**Pinned inputs at baseline `0588eae` (sha256, whole file; Director-verified, re-measured, all byte-identical to
the draft's `ffbc993` measurement — no B/D/S/C/REPAIR/RESP/M/PM/T pointer changed between the two revisions).**

| Pointer | File | sha256 |
|---|---|---|
| packet | `docs/evidence/wave2-execution-packets/WO-220209.packet.json` | `91b972424f1ae8338ed0c235abddf026f792bb79b706ebc9ef48c302527288bb` |
| allocation | `docs/evidence/wave2-execution-packets/WO-220209.allocation.json` | `47b3a0ff9b7857a53ac50690c8baa4591b156eb7e76fa17774424777e961d13d` |
| B | `docs/evidence/wave2-candidate-bius.json` | `5515b6ce24286dd83a40cd295f61dd74a6c55a5b1c407597c0a65db36504d1f1` |
| D | `docs/evidence/wave2-dependency-dag.json` | `be5f39f8d6d8c319dfd990ae9f52c32e84e7db6af13ef87f9d45b6cbeff20476` |
| S | `docs/evidence/wave2-specified-requirements.json` | `332757c43b8754196e45f75a8ce2ee422e19382415e17ed22cd431f348d9b800` |
| C | `docs/evidence/wave2-design-contracts.json` | `56676dd97cd09031882ee01e61af2a15071f1b92f6c914fde2169b682f056242` |
| REPAIR | `docs/evidence/wave2-revisions/2026-09-25-wo-220209-candidate-consistency-repair.md` | `59ea62bd68bd0371cb03f2c9ebb17ad23736ac774511fb99c214ebb0d30e42f6` |
| RESP | `docs/evidence/wave2-revisions/2026-09-22-respecify-015-039.md` | `ad7308bbdcb45dd303bee44a44b4c29cb0a17e783b0f67501d1e88a913495a60` |
| M | `docs/evidence/wave1-agent-ready-outcome-matrix.json` (PY-10 `coercion_finding`, gap `G3`) | `f9730f9eccf4022e314f74bdf3d3ac257c0e07d00c76d6cf87419e472fe4e145` |
| PM | `docs/evidence/wave1-readiness-assessment-provenance.json` (producer manifest) | `a8f977e0bb26bd7f0b599042ccce853ff5d76dd9e00adff033990550408a8f21` |
| T | `docs/evidence/execution-trajectories/PY-10.jsonl` | `101ffc18714a16f9096bd559fcfa7b15814b80b31dc47c84e1de9fd392cff282` |
| U7 contract | `docs/work-units/wave2/WO-220207.md` (pinned FX-U7 packet) | `0e05a12001ee303b5d7eebb4d57afe536def5f2f323c7946a69d1b30390e9525` |
| U7 evidence | `docs/evidence/wave2-proof-fixtures/FX-U7.md` | `7d72d6bfbc4b54255017200eafad958246689ed01e34daf9d6d383079536acaf` |

The contract file's own digest (this file) is not listed here, following the FX-U7 pin's convention: a file cannot
byte-stably hash itself before its own edit is complete. The PRODUCER re-pins the contract/packet/allocation digests
from the commit that actually lands this pin, as the draft's digest table already noted.

Status `PINNED_PLANNED_NOT_EXECUTED`. Proof level `LOCAL_COMPOSED_OR_MECHANICAL` (`D#/proof_fixtures/11/proof_level`).
Disposable local profile only. The fixture uses no live store, provider, model launch, network, host or RAI. It uses
the harness shape of FX-C1 and FX-U5 (`docs/evidence/wave2-proof-fixtures/FX-C1.md`, `tools/evidence/fx_c1_evidence.py`,
`tools/evidence/fx_u5_evidence.py`, `docs/evidence/wo-220205-fx-u5.md`). Anything marked **PLANNED** does not exist at
baseline `0588eae`; it is this fixture's own port/adapter/attempt-record surface, still to be built by the PRODUCER.

**Governing extent.** Unchanged from the draft: `D#/nodes/11/completion_predicate` = `D#/proof_fixtures/11/scenario`
= `B#/bius/11/scope/extent` = `B#/bius/11/acceptance_criteria/0`:

> "Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result."

- **Acceptance IDs:** SF-REQ-015-AC-01..05 (`D#/proof_fixtures/11/acceptance_ids`; `D#/acceptance_trace/17..21`, owner U9, fixture FX-U9).
- **Enforcement:** `015-envelope-applicability` (`D#/enforcement_obligations/8`) and `015-composed-authority` (`D#/enforcement_obligations/9`). Both BLOCKING, both `python_unit_integration`.
- **`D#/nodes/11/proceeds_regardless`:** "Consumer semantics, retained-envelope parity and provenance/admission refusal tests at current interfaces; Agent Ready product ownership is settled by A1."
- **Node scope:** `D#/nodes/11/scope` is empty.

**Pinned predecessors.**
- **U5 (WO-220205, #97): DONE.** Unchanged from the draft.
  - Accepted candidate `6518d1ef09fc99e8abccf1c6234106f860fce7e6`, merged by `036c5fc`.
  - Receipts: `docs/evidence/wo-220205-fx-u5.md`, `docs/evidence/wo-220205-fx-u5-predicate-mapping.json`, `docs/evidence/wo-220205-fx-u5/`, `docs/evidence/wo-220205-fx-u5-r2/`.
  - JC ACCEPT https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816278191; closure https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816356325.
- **U7 (WO-220207, #101): DONE.** Updated from the draft's "READY, not released, not implemented".
  - The FX-U7 packet is pinned in `docs/work-units/wave2/WO-220207.md` § "Pinned proof packet (FX-U7)" at `5a79622`.
  - Accepted candidate `9d97365f58d41147923d3070ac2f838937ed0f4f` (branch `b-disp/d48a98bd-ac48-4f98-9745-2622c4e57064`), independently verified by JC (invocation `b87bfc8e-901c-4e3e-bb60-82c64a952609`, ACCEPT): 83 focused tests, 771 full regression, architecture fitness PASS, canonical Node checks PASS, a fresh FX-U7 runner exit 0 with 43/43 controls discriminated and the digest-mismatch control exiting 2.
  - Landed by SWF-19 direct merge `0588eae74bbbdddfd8650e349fff472f0db20eff` (parents `7b6e19c`, `9d97365f`); merged tree equals the accepted candidate tree (no conflict, no drift). Post-merge regression on the merge tree: 771 passed, architecture PASS, Node checks PASS.
  - Implementation: `src/alienintent/context_assembly/domain/compilation.py` (`validate`, `validate_split`, `ValidationReport`, `CompilationHold`), `ports/compilation.py`, `application/compilation_validation_service.py`, `adapters/compilation_repository.py`, `composition/compilation.py`, plus a `composition/upstream_profile.py` addition. No new cross-group import pair (D-1 held).
  - Explicit non-claims carried by the landed U7: derivation, split prepare/apply, reservation, invalidation records, apply/readback/recovery (U8); SF-REQ-015; `013-graph-coverage`; SF-REQ-013 AC-06..08 (`UNASSIGNED_IN_DAG`); the active-invocation fence (N-10); Phase 5 approval; operational acceptance. FX-U9 relies on none of these.

U5 and U7 source and proof stay unmodified by this pin.

### What FX-U9 proves, and what it must not claim

Unchanged from the draft. FX-U9 proves the **readiness consumer**: lint before assessment; consumer handling of
direct and known-MCP results; the four disposition routes up to their handoff boundaries; immutable attempts with
applicability kept separate; stale invalidation; refusal of provenance and admission at the actual composition
boundary; the PY-10 historical replay.

FX-U9 must **not** claim:
- **U8 split/replan.** It does not claim `SplitTransaction.prepare`/`apply` or result materialization (`C#/contracts/2/ports/3`; `D#/nodes/10`, GAP_BLOCKED). The predicate states "the transaction itself is U8 (DV-7)".
- **U10 native-producer binding.** It does not claim CLI/MCP adapter conformance to the published contract, and it does not claim positive native-producer proof (`D#/nodes/12/completion_predicate`). "Test doubles are explicitly fixture producers and cannot discharge native-producer proof" (`D#/enforcement_obligations/9/rule`).
- **SF-REQ-015-AC-06 or AC-07.** Still unassigned in the DAG (REPAIR "Recorded, not repaired"); no DAG revision has assigned them between `ffbc993` and `0588eae`. The run report records `ac_06_07_trace_ownership: UNASSIGNED_IN_DAG`. A FX-U9 PASS is never counted toward them.
- **SF-REQ-029 retained-assessment serialization.** SF-REQ-029 owns it (`C#/contracts/4/ownership_statement`); no D node lists SF-REQ-029.
- **Release.** READY yields eligibility only. `admit_release` stays the separate SF-REQ-002 gate. This pin grants no release authority.
- **Anything about Agent Ready itself.** FX-U9 does not implement, import or copy Agent Ready (`C#/contracts/4/non_goals/specified/0`).
- **Positive native-producer proof of the landed WO-220207 checker.** FX-U9 may cite `CompilationValidation`'s landed signature (U-5, U-10 above) as an existing interface; it does not thereby claim to test or re-verify U7's own behaviour, which is JC's and the FX-U7 evidence's claim, not this fixture's.

### Fixture inputs (durable state only; `tmp_path` or `--output` root, never a live profile)

Unchanged from the draft except item 11, which is updated below.

1. **Store and evidence.** A real temporary `SQLiteOperationalStore` (`src/alienintent/execution_coordination/adapters/sqlite_store.py:49`) and a `LocalEvidenceRepository` (`src/alienintent/evidence_learning/adapters/local_evidence_repository.py:15`, `put` `:51`, `get` `:80`). Both are composed through `UpstreamProfile` (`src/alienintent/composition/upstream_profile.py:22`), the "actual profile boundary" (`C#/contracts/4/composition`).
2. **Design gate (U5, existing).** `UpstreamProfile.design_readiness` (`upstream_profile.py:59`) driven into CURRENT_VERIFIED, STALE, held-with-no-review, and absent. `DesignReadiness.admit` (`design_admission_service.py:254`) is the only read.
3. **Candidate BIU documents (AC-01).** One complete candidate through `contract_from_document` (`sandbox_run_profile.py:97`) into `BiuContract` (`contract.py:49`), plus one-fault variants for decision, boundary, verification-obligation, architecture-constraint (design gate not CURRENT_VERIFIED, per U-5) and dependency faults, per the draft.
4. **Retained native direct assessments (existing; read by path and digest, never modified).** Same table as the draft: `WO-220207.2026-09-25T001654.663618Z.assessment.json` (ASSESSED READY, `provenance.input_sha256` `0e05a120…` equals the sha256 of `docs/work-units/wave2/WO-220207.md` at `5a79622`, re-measured unchanged above), `WO-220102.2026-09-23T041739.192319Z.assessment.json` (ASSESSED HOLD), `WO-220102.2026-09-23T053013.882036Z.assessment.json` (ASSESSED READY), `FDH-01.2026-09-24T021848.446095Z.assessment.json` (EXECUTION_FAILURE). Record count at `docs/evidence/wave2-readiness-assessments/` is unchanged (22) between `ffbc993` and `0588eae`.
5. **Known MCP envelopes** (synthetic, `SYNTHETIC_MCP_ENVELOPE_FROM_RETAINED_DIRECT`). Unchanged from the draft.
6. **Fixture producers** (`FIXTURE_PRODUCER_NOT_NATIVE`). Unchanged from the draft.
7. **Provenance bindings** (`015-composed-authority`). Unchanged from the draft.
8. **PY-10 historical bootstrap-assessor history** (AC-05; read-only; `SURROGATE_READINESS_HISTORY_NOT_AGENT_READY`). Unchanged from the draft; the three blob digests are independently re-verified above (U-13).
9. **Dependency snapshot.** Unchanged from the draft.
10. **Decision Inbox (CLARIFY route, SF-REQ-035; existing).** Unchanged from the draft.
11. **Split handoff (SPLIT route).**
    - A recording fake implements the **consumer side** of the SplitTransaction port (`C#/contracts/2/ports/3`).
    - Case (a): it returns no materialized result set.
    - Case (b): it returns a fixture-supplied result set labelled `FIXTURE_MATERIALIZED_RESULT_SET_NOT_U8_PROOF`. The set holds children C1 and C2 and a retained Integration Parent O, each a complete `BiuContract`, plus an invalidation set.
    - **Updated from the draft.** WO-220207 is landed, and its actual value shapes are `ValidationReport`/`CompilationHold` (`context_assembly/domain/compilation.py:124`/`:107`); there is no separate `FrozenSplitProposal` type — `validate_split` (`:442`) takes a plain dict SPLIT_REPLAN proposal directly. The draft's "Types that FX-U7 defines ... are PLANNED-U7 (U-10)" caveat is discharged: nothing about this fixture family is still blocked on U7 landing. FX-U9's own SPLIT case (a)/(b) fixtures remain fixture-supplied and are not built from `validate_split`'s inputs — see U-10 above for why U9 does not call U7's validator.
12. **Release gate (SF-REQ-002; existing).** Unchanged from the draft.

### Interfaces under test

**EXISTING (verified at `0588eae`; consumed, not modified):**
- **U5 design gate.** `DesignReadiness.admit(design_key, current_vector) -> ReadinessDecision` (`design_admission_service.py:248`/`:254`). Domain values `CurrentVerified`/`Held`/`Stale` at `context_assembly/domain/design_admission.py:348`/`:356`/`:362`.
- **Composition.** `UpstreamProfile` (`upstream_profile.py:22`) with `inbox` `:37`, `questions` `:38`, `ambiguity` `:39` and `design_readiness` `:59`.
- **Contract.** `BiuContract` (`contract.py:49`) with required-field validation at `:75-90` and `content_digest` at `:73`/`:92`. `contract_from_document` at `sandbox_run_profile.py:97`.
- **Store.** `OperationalStore` (`execution_coordination/ports/operational_store.py:58`) with `commit` `:64`, `read_state` `:67`, `list_states` `:68`. `VersionConflict` at `:13`, `StoreUnavailable` at `:9`.
- **Evidence.** `EvidenceRepository`/`LocalEvidenceRepository`, `Observation` (`evidence_learning/domain/records.py:60`), `Ref` (`evidence_learning/domain/refs.py:7`).
- **Decision Inbox and escalation values.** `DecisionInbox`, `DecisionInboxQuestions`, `HumanDecisionRequired`, `DecisionSubmission` (`escalation.py:40`), `DecisionRecord` (`:63`).
- **Release and coordination.** `admit_release`/`ReleaseRequest` and `FactoryCoordinator`.
- **Architecture fitness.** `tools/fitness/check_architecture.py` and `tests/test_architecture_fitness.py` (extension required, `C#/contracts/4/architecture_fitness`).
- **U7 compiler validation (landed, WO-220207/#101, promoted from PLANNED-U7).** `CompilationValidation.validate(candidate) -> ValidationReport | CompilationHold` and `.validate_split(proposal, original, authority_limits, identity_policy) -> ValidationReport | CompilationHold` (`context_assembly/application/compilation_validation_service.py:42`/`:46`); persistence via an `upstream:compilation:<candidate_digest>` pointer with its own immutable `history` chain (`:38-40`, `:116-134`), never a `factory:*`/`release:*` write. Value shapes: `ValidationReport(mode, candidate_digest, body)` at `context_assembly/domain/compilation.py:124`, `.document()` at `:131`; `CompilationHold(reason_code, affected_refs, mode, candidate_digest, detail, findings)` at `:107`, `.document()` at `:115`. `validate`/`validate_split` themselves are pure functions at `:371`/`:442`. There is no `FrozenSplitProposal` type; a SPLIT_REPLAN proposal is a plain `dict` (`domain/compilation.py:428-439`, `split_units`). FX-U9 does not call these — U-5/U-10 above record only that the types now exist and are citable, discharging the draft's "PLANNED-U7" blocking label.

**PRIOR ART (programme tooling outside `src/alienintent`; not product interfaces; not imported; recorded in the run report):**
- `tools/orchestration/readiness_assessment.py` (sha256 `8753f48b…09b3`). `AgentReadyCliAdapter` at `:55`, `_package_identity` at `:62`, `assess` at `:88`. Maps non-zero exit to `PROVIDER_OR_PRODUCT_FAILURE` (`:128-129`), unparsable output to `MALFORMED_OUTPUT` (`:133-135`), non-native shape to `NOT_AGENT_READY_SHAPE` (`:137-141`).
- `tools/evidence/check_assessment_producer.py` (`7447d1ae…b586`). `looks_native_agent_ready` at `:92`, MCP branch at `:96-98`, `is_native_provider_evidence` at `:75`.
- `tools/orchestration/factory_director_inputs.py` (`8adb3aa6…8e81`). `ASSESSMENT_MARKER` at `:49`; `has_retained_assessment` at `:313`.
- `tools/live/release_admission.py` (`6770284b…fc32`). `disposition_from_record` at `:249`; `NATIVE_COMMENT` at `:269`; `disposition_from_native_comment` at `:272`.

  These two modules show the classes of failure FX-U9's negative controls target: a self-declared producer string, and string-only disposition acceptance. FX-U9 does not change them.

**PLANNED (ports per `C#/contracts/4/ports`; location per U-4; this fixture's own surface, still to be built):**
- `ReadinessAdmission.lint(candidate, design_applicability, proof_plan, dependency_snapshot) -> LintReport`. A failure is a `LintHold` naming the duty and field. "Not a fabricated Agent Ready HOLD disposition" (`C#/contracts/4/transitions/0`).
- `AssessmentConsumer.observe(raw_artifact, attempt_metadata, recognized_shape) -> SemanticAssessment | AttemptFailure` and `consume(assessment, current_input_fingerprint) -> ReadinessEligibility | Hold` (`C#/contracts/4/ports/1`).
- `ReadinessAssessment.assess(candidate_work_unit) -> ReadinessAssessment | AttemptFailure` (`C#/contracts/4/ports/2`). FX-U9 binds only fixture producers or the provenance-refusal variants.
- `execution_coordination/adapters/assessment_consumer.py`. Reads retained direct objects and known MCP envelopes and stores the raw bytes privately before normalization (`C#/contracts/4/adapters/0`).
- An immutable `AssessmentAttempt` with the fields in `C#/contracts/4/state`. Its attempt UUID is persisted before invocation (`C#/contracts/4/identities`). A separately versioned applicability pointer is kept per candidate.
- A compatible composition addition to `composition/upstream_profile.py`. Validates the configured product identity, package and schema binding before admission; if unbound or unestablished, the result is `CAPABILITY_PROVENANCE_HOLD`, never READY (`C#/contracts/4/composition`).

### Commands (all under `rtk proxy`, `PYTHONPATH=src`; PLANNED except regression)
1. PLANNED: `python3 -B -m pytest -q tests/<location per U-4>/test_readiness_consumer.py`.
2. PLANNED: `python3 -B tools/evidence/fx_u9_evidence.py --output <new-dir> --invocation <exact-invocation>`. The output directory must be new. The runner extracts and digest-checks the PY-10 blobs (input 8) and the retained records (input 4). It applies each control mutation exactly once to a disposable source copy, as `fx_u5_evidence.py` and the now-landed `fx_u7_evidence.py` do.
3. Regression (existing): `python3 -B -m pytest -q`; `python3 -B tools/fitness/check_architecture.py --root src/alienintent --check all`; `python3 -B -m pytest -q tests/test_architecture_fitness.py`; `node scripts/check.mjs all`. Each exits 0 at `0588eae` (measured: 771 passed per the U7 closure receipts).

### Probes and expected observables (test ids PLANNED)

Unchanged from the draft (P01–P24, hold and failure codes U-12). See the draft
`docs/evidence/wave2-execution-packets/WO-220209.proof-packet.draft.md` §§ "Probes and expected observables (test ids
PLANNED)" for SF-REQ-015-AC-01 through AC-05 and both enforcement obligations, and "Clause map" for the completion
predicate clause mapping. None of those probes, fixtures or negative controls changes with U7 landing: FX-U9 does not
call `CompilationValidation`, `validate` or `validate_split`, so nothing in the probe table is PLANNED-U7. Reproduced
here for completeness is only the clause map, since it is short and load-bearing:

**Clause map (`D#/nodes/11/completion_predicate`):**
- four dispositions: P05–P10;
- MCP failure regardless of exit: P16, P18;
- raw bytes and predecessor attempts: P12, P15, P17;
- changed inputs invalidate READY: P11;
- separate release gate: P05, P22;
- PY-10 replay, never Agent Ready: P19;
- CAPABILITY_PROVENANCE_HOLD before launch: P20;
- provenance, not schema shape: P21;
- CLARIFY via SF-REQ-035: P06;
- SPLIT port and refusal without a result set: P07;
- lint and reassessment of each result: P08.

### Discriminating controls

Unchanged from the draft: K01–K30, each run intact (exit 0), then with the fault (exit 1 at the named assertion),
then restored (exit 0), with `application_count` 1. The digest-mismatch HOLD for the input 8 blobs and the input 4
records is asserted once, by corrupting an extracted blob, and must exit 2.

### Evidence to retain (`docs/evidence/wave2-proof-fixtures/FX-U9/`, FX-C1 layout)

Unchanged from the draft: `execution-record.json` (`record_kind: ProofFixtureExecution`, `schema_version: 1`,
`fixture_id: FX-U9`, input digests including the table above plus the input 4 records, the input 8 blob digests, and
the test/runner/source files; labels `SYNTHETIC_MCP_ENVELOPE_FROM_RETAINED_DIRECT`, `FIXTURE_PRODUCER_NOT_NATIVE`,
`FIXTURE_MATERIALIZED_RESULT_SET_NOT_U8_PROOF`, `SURROGATE_READINESS_HISTORY_NOT_AGENT_READY`,
`CONTRACT4_NOT_INDEPENDENTLY_DESIGN_VERIFIED`; residuals `NATIVE_PRODUCER_POSITIVE_PROOF_U10_EXTENT`,
`SPLIT_TRANSACTION_U8_EXTENT`, `AC_06_07_TRACE_OWNERSHIP_UNASSIGNED_IN_DAG`, `SF_REQ_029_SERIALIZATION_NOT_CLAIMED`;
`independent_verdict: PENDING_FRESH_BIU_VERIFIER`, `live_proof: NOT_ESTABLISHED`; model-launch/provider-call counts
must be 0; tokens and cost `null` with an `UNKNOWN` reason, never 0), `run-report.json`, `proven-red.json`,
`fixture-inputs/`, `observations/<sha256>` and `digest-manifest.json`.

### Boundaries and non-goals

Unchanged from the draft:
- U8 split/replan transaction and materialization;
- U10 native CLI/MCP binding and published-contract conformance;
- AC-06/07 ownership;
- SF-REQ-029 serialization;
- Agent Ready implementation, rubric or private imports;
- any live Agent Ready, provider, model, network or GitHub call;
- Issue/Project writes and lifecycle transitions;
- release;
- modification of historical PY-10 records, retained Wave 2 assessments or the producer manifest;
- changes to U5 behaviour, `BiuContract`, `DecisionInbox`, `admit_release`, the programme tools cited as prior art, or the newly landed U7 checker.

A local PASS is not operational acceptance. The historical assessment `POSTW1-READY-013:WO-220209:1` (BLOCKED,
baseline `01af974`; `docs/evidence/wave2-agent-ready-assessments.json` line 936) remains history for its own baseline
and does not apply to this candidate (REPAIR "Pinned consumers").

### Owner questions (as drafted; dispositions above control)

The full owner-questions table, with its original candidate answers and source citations, is retained unchanged at
`docs/evidence/wave2-execution-packets/WO-220209.proof-packet.draft.md` § "Owner questions". The Director dispositions
table at the top of this section controls over that draft text wherever the two differ: U-3, U-4, U-7 and U-13 are now
Director-confirmed rather than merely candidate answers; the U7-dependent parts of U-5 and U-10 are discharged rather
than `PINNED-AFTER-#101-DONE`; and Admission is discharged rather than open.

Before implementation, the PRODUCER pins the exact source revision (this commit), reconciles it against any later
`origin/main` movement, and confirms the input digests above match its own working tree before proceeding, per the
WO-220207 precedent for baseline reconciliation.

**Release authority is not granted by this document.**
