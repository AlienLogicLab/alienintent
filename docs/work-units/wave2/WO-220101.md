# WO-220101 — Reusable isolated proof substrate

Wave 2 BIU candidate compiled from the approved Wave 2 design (DAG node S0). Baseline `dfeca645fc015cb7cf7f4facdc7991f9ae55adea`.
Candidate contract `docs/evidence/wave2-candidate-bius.json#/bius/0` (sha256 `b96f8feadacf4cd2ef2ff977cf6dbccb094ffcb70a03767e75780c77991b6360`).

## Intent

Deliver the bounded planned extent: Reusable isolated proof substrate

## Requirements satisfied (extent)

- SF-REQ-039
- SF-REQ-014

## Fixed decisions (from the verified Design Contract; not reopened here)

- (SF-REQ-039) The following multi-role target decisions are conditional design constraints, not implementation authorization. SPECIFY must first identify existing requirement authority and owners for orchestration and real-path outcome emission. Do not replace FactoryCoordinator._completed_for_outcome under SF-REQ-039 alone; current worker/lifecycle contracts remain untouched. Closure records only actions actually performed/read back by local adapters; never populate all required_closure_actions merely because producer succeeded. Offline tests exercise the production orchestration path through composition, not a second mocked lifecycle. A composed role router resolves PRODUCER/VERIFIER/CLOSURE from the durable invocation record keyed by correlation_id and injects the role-specific worker implementation. Existing WorkerProvider signatures stay intact; verifier output must reference retrieved candidate and independent invocation. Missing RoleOutcomeRecord holds even when WorkerOutcome.kind is success. On verifier rejection, persist finding refs and invoke existing transition(action=rework) at the current lifecycle version; enforce remaining authorized attempts/budget before producer allocation. RE
- (SF-REQ-014) For platform isolation pin four observables: positive target access; configured profile/resource scoping; rejected out-of-scope application request; unchanged outside-state readback. Do not require impossible platform credential denial. Proof plans are derived before implementation; semantic predicate mappings are authority-reviewed input, not inferred from passing source code.
- (None) 

## Scope

- DAG scope: Extend existing offline profile and adapters only at current interfaces. This owns transport and fixtures before capstones, not a new role router.
- Extent: Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.
- Owns capabilities: local_proof_transport, existing_kernel_fixture

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- none (root node)

## Acceptance criteria

- Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.

## Verification obligations

Proof fixture FX-S0 (PLANNED_NOT_EXECUTED):
Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.

Evidence contract: Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.

Order: Before implementation, pin executable fixture commands, inputs, expected outcomes and evidence schema against this node predicate; demonstrate intact/fault/restored discrimination for mechanical controls. Retain independent judgment where specified. S0 establishes the shared harness at current interfaces; all consumers wait for predecessor proof. No future fixture is claimed executed here.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.
- Extend existing offline profile and adapters only at current interfaces. This owns transport and fixtures before capstones, not a new role router.

## Capabilities

Needs: none. Provides: local_proof_transport, existing_kernel_fixture.

## Execution Packet and Allocation (bound 2026-09-22)

Allocation decision: `docs/evidence/wave2-execution-packets/WO-220101.allocation.json` — deterministic
Allocator branch; PRODUCER = configured `claude` worker, VERIFIER = configured independent `claude`
worker (live self-hosting profile), subscription-funded.

Limits (SWF-09 dimensions): concurrency 1 (enforced by lane claim); attempts 3 execution cycles
(SWF-32 semantics; enforced by Program Director procedure — the bootstrap enforces no cycle cap);
retries 1 replacement worker per phase; cancellation available via operator stop; **wall-clock:
no per-invocation hard bound is enforced by the bootstrap (dispatcher performs liveness inspection
only)**; tokens/cost measured, never assumed zero, no numeric ceiling exists in any authority.
Remaining allowance: 3 of 3 cycles.

Release authority: NOT granted by this document. Release requires the Founder's Wave 2
authorization (FD-01 §3; W2-P01) and the separate release-admission checks.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

## Stop and escalation

Stop: - Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.
Escalate: - Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Readiness

Historical bootstrap-assessor verdict `POSTW1-READY-013:WO-220101:1` = NEEDS_CLARIFICATION
(AR13-CQ-001: allocation unbound) — retained unchanged. This document is submitted for a fresh
native Agent Ready assessment at baseline `dfeca645fc015cb7cf7f4facdc7991f9ae55adea`.
