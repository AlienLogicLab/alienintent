# WO-220302 — Durable context reconstruction

Wave 2 BIU compiled from the approved Wave 2 design (DAG node C2).
Baseline `669e5f1cf8e759e1b88beacd194650ab0f6a88d8`. Candidate contract sha256 `3ba5c3a0d87f32ac578f702c4c5f53ba4e9dd154871773717b073aa848aa4720`.

## Intent

Deliver the bounded planned extent: Durable context reconstruction

## Requirements satisfied (extent)

- SF-REQ-053

## Fixed decisions (from the verified Design Contract; not reopened here)

- (SF-REQ-053) Default tenure policy: one BIU, maximum age 3600 seconds, maximum 32 admitted transitions, maximum blocked duration 300 seconds, accepted contradiction count 1, any relevant state-vector mismatch ends tenure. Default context-usage source is UNBOUND and its numeric threshold is disabled; absence of a binding alone does not terminate every episode. All other tenure limits remain active. If explicitly BOUND, threshold is 0.8 and reaching it ends tenure; unavailable, stale, wrong-invocation or invalid measurements terminate conservatively. Monotonic clock within process; persisted UTC deadline for restart; clock regression holds. Values are versioned composition policy, not authority or increased budget. The 3600 seconds is a proposed configurable design default, not a claim that the bootstrap waiter timeout or historical values are canonical; no empirical derivation or inheritance is asserted. Terminal/objective/authority/provider/model changes and explicit request end immediately. Reaching any limit ends, never silently renews; authorized begin of new epoch retains budget accounting. SF-REQ-008 durability elaboration and SF-REQ-029 provider normalization amendments are excluded. Existing store versions/events suffice; no promise of normalized provider telemetry. POSTW1-DECIDE-006A gates actual bootstrap handoff. Tenure checks run on each result/event and an injected deadline timer independent of the model. Epoch expiry prevents new authority-bearing commands but does not fabricate cancellation of already-admitted workers. Relevant authority/source changes advance a durable applicability revision observed by guarded admission; an external source whose freshness cannot be checked causes a hold. For a BOUND context-usage source only, unknown usage is conservative termination; UNBOUND is explicitly recorded and never represented as zero usage or measured-safe context. Context observation validity is decidable: matching current invocation, finite used >= 0, finite limit > 0, and sample from the current tenure check (request correlation must match); unavailable or prior-check samples end tenure when BOUND. Compare used/limit >= 0.8; no normalization or estimated values. Default UNBOUND policy does not claim telemetry-based context safety. Explicit context-exhausted operator/provider events still end tenure; neither they nor the optional fixture close LRN-023. Contradiction is a judgment obligation owned by the authorized episode operator under the episode authority, not an inferred semantic predicate. The operator names conflicting authoritative facts/actions and records rationale and evidence refs. Mechanically count only AcceptedContradiction for the current episode/epoch/vector with authorized actor and nonempty resolvable conflicting_refs/rationale; one ends tenure. Invalid, stale or unauthorized reports produce InvalidJudgment and cannot act as authoritative termination input. Truth of the contradiction requires operator judgment; fixture automation proves admission and termination, not semantic truth.
- (shared contracts, `#/fixed_design_baseline/shared_contracts`) Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: Two fresh invocations reading the identical manifest reconstruct equal authorized_next_action_set and blocked_set, current work, unresolved decisions and evidence refs. Conversation memory is neither an input nor authority; unavailable state produces hold.
- Owns capabilities: durable_context_reconstruction
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220301 (C1, Issue #78): DONE/judgment events dedupe across restart; notification failure preserves pending. SEEN is not RESOLVED; wrong actor/revision/lane cannot resolve. DecisionInbox remains distinct. Unbound activation causes zero model launches. Preserve origin IDs, history and handler attribution.
- WO-220201 (U1, Issue #76): Pinned 17-document manifest at d83e87e6f2bb4ff90ff4f4f0e3940582f8d5c998 reproduces 56 referenced/53 defined; test all three prose forms, suffix/extended namespace, malformed tokens, AC-before-requirement classification, compounds/possessives/range endpoints and Markdown links. Excluded files do not alter inventory. Unique 050, retired 054; 056 later revision is separate. Conflicts hold affected spans only.

All listed dependencies are DONE on Project #1 at this baseline (canonical read-back). Admission: Consume exact independently verified predecessor candidate and retained proof receipts; predecessor authority holds must be discharged by their named actor. A planned capability is not available execution substrate.

## Acceptance criteria

- Two fresh invocations reading the identical manifest reconstruct equal authorized_next_action_set and blocked_set, current work, unresolved decisions and evidence refs. Conversation memory is neither an input nor authority; unavailable state produces hold.
- SF-REQ-053-AC-01: Stop an episode after a durable event, then start a fresh context with no transcript. Given the identical authority/state/event snapshot, predecessor and successor identify the same authorized next-action set, blocked work and lifecycle state; record mismatches as failures, not merely a successfully started process.

## Verification obligations

Proof fixture FX-C2 (PLANNED_NOT_EXECUTED, proof level LOCAL_COMPOSED_OR_MECHANICAL):

- Environment: Disposable local profile unless explicitly marked operational; no live action authorized by this plan.
- Scenario: Two fresh invocations reading the identical manifest reconstruct equal authorized_next_action_set and blocked_set, current work, unresolved decisions and evidence refs. Conversation memory is neither an input nor authority; unavailable state produces hold.
- Acceptance IDs: SF-REQ-053-AC-01
- Probe SF-REQ-053-AC-01: Fresh context from identical manifest reconstructs equal deterministic allowed/blocked sets and lifecycle. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)

Order: Before implementation, pin executable fixture commands, inputs, expected outcomes and evidence schema against this node predicate; demonstrate intact/fault/restored discrimination for mechanical controls. Retain independent judgment where specified. S0 establishes the shared harness at current interfaces; all consumers wait for predecessor proof. No future fixture is claimed executed here.

Conditional proof: Operational fixtures require their separately authorized target and actual readback. Local substitutions must be labelled and cannot satisfy operational acceptance.

Evidence contract: Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.

## Execution packet and allocation

Allocation: `docs/evidence/wave2-execution-packets/WO-220302.allocation.json` — deterministic
Allocator branch; configured PRODUCER (Morty, Claude) and independent VERIFIER (JC, Codex);
concurrency 1; 3 execution cycles and 1 replacement worker per phase (`execution.biuLimits`,
bound before release); wall-clock bounded by systemd supervision (120 min runtime + 2 min
startup + 5 min stop grace); tokens and cost measured, never assumed zero. Remaining
allowance: 3 of 3 cycles.

Landing: SWF-19 — merge the accepted candidate branch directly into the baseline branch,
preserving the accepted SHA. **Do not open a pull request.**

Release authority is not granted by this document.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Stop and escalation

Stop:
- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Pinned proof packet (FX-C2)

Status `PINNED_PLANNED_NOT_EXECUTED`. Pinned by Factory Director episode
`factory-director-f2923f765dd14dd5b5e0882aa7050340` from the draft
`docs/evidence/wave2-execution-packets/WO-220302.proof-packet.draft.md`, after every owner
question was dispositioned below. Proof level `LOCAL_COMPOSED_OR_MECHANICAL`; disposable
local profile only. Anything named here that does not exist at the admission baseline is
**PLANNED** and is created by the PRODUCER.

**Pinned inputs (sha256).**
- `docs/evidence/wave2-design-contracts.json` `56676dd97cd09031882ee01e61af2a15071f1b92f6c914fde2169b682f056242` (binding: `#/contracts/8` ports, persistence, failure_modes, recovery, evidence, composition, `053-tenure-fence`; `#/shared_contracts`).
- `docs/evidence/wave2-dependency-dag.json` `f99c0a7a392f8dd1f7889bb99d926f0a5a9de0948cb09845686f9d7e6f2b1e8d` (node C2, fixture FX-C2, migration M-CONTEXT).
- `docs/evidence/wave2-candidate-bius.json` `61f9324e7d1b7f0937bbc1aff7cbaba77c50192204e109cad5d78edb04f677f7`.
- `docs/evidence/wave2-execution-packets/WO-220302.packet.json` `9ae65adb52efc2ddb38dced645cd826e294035fd4c5a685a9384594eae202944`; `WO-220302.allocation.json` `bbaac7bc38cea2c819eb7c7e2a2a22cc691e2ee35ae18fb8672d6680c05ca37e`.
- Predecessor C1 (WO-220301, #78): candidate `8c810d2df815907cb3c1a383d6a645b3cddc7c6e`, landed by `d168b6a`; receipts `docs/evidence/wave2-proof-fixtures/FX-C1/` (execution-record `da3c79511fbbe571b1a9ae4bd4e6b245bfa99a04787b221bf59360f5ea34be55`); independent JC verdict ACCEPT https://github.com/AlienLogicLab/alienintent/issues/78#issuecomment-5794214610; closure LANDED https://github.com/AlienLogicLab/alienintent/issues/78#issuecomment-5794268196.
- Predecessor U1 (WO-220201, #76): candidate `43ea5e2b745be02df6aebaf5ba1e328f4c75fa5e`, merged by `fcb65ff`; receipts `docs/evidence/wo-220201-fx-u1/final/report.json`; independent JC verdict ACCEPT https://github.com/AlienLogicLab/alienintent/issues/76#issuecomment-5792843472; closure DONE https://github.com/AlienLogicLab/alienintent/issues/76#issuecomment-5792906393.
- C1/U1 source and proof stay unmodified.

**Owner-question dispositions.** None is a Founder or architecture decision. Each is either
already decided by the pinned design/DAG (cited) or delegated engineering resolved by a
labelled choice inside the contract's `conditional_proof` and `implementation_local_freedom`.

| Q | Disposition | Pinned answer and authority |
|---|---|---|
| U-1 action/blocked derivation | DELEGATED, labelled choice | The design fixes determinism and forbids "new lifecycle semantics" (`#/contracts/8/non_goals`) and gives lifecycle to `execution_coordination` alone (`domain_owner`). Rule: for each `factory:`/`release:` item, `authorized_next_action_set` = the actions the existing `execution_coordination.domain.lifecycle.transition` accepts from the item's current `ExecutionState` (probed on the pure value, never committed), for items with no blocking condition. An item with an open `DecisionInbox` entry, a PENDING/SEEN attention item, or an authority block goes to `blocked_set` with a typed reason instead. Output is canonically sorted. No new predicate, stage or action is introduced; the rule is recorded in the run report as `LABELLED_DERIVATION_RULE_v1`. |
| U-2 manifest identity/persistence | DECIDED | `#/contracts/8/persistence`: "Context manifest pins all refs and versions"; "active pointers/state in distinct SQLite aggregates"; histories in evidence files; "No new database engine". So: immutable S1 `Observation` (`evidence_id="context.manifest"`) plus a distinct `context:manifest:<id>` pointer aggregate in the existing `OperationalStore`, installed then read back before CAS. "Existing store versions/events suffice" excludes SF-REQ-008 durability elaboration, not a new aggregate. Enumerated record classes: `factory:*`, `release:*`, `decision-inbox`, `attention:*`, `upstream:requirements:current`, and the evidence bodies they reference (the inputs of the six fields plus `lifecycle`). |
| U-3 "stop an episode" | DELEGATED, labelled local substitution | EpisodeControl/epochs are C3 (DAG C3 depends on C2). Under `conditional_proof` ("local substitutions must be labelled"), predecessor and successors are separate OS processes with no shared object and no transcript; the durable event is committed before the predecessor exits. Labelled `LOCAL_PROCESS_BOUNDARY_SUBSTITUTE_FOR_EPISODE`. C3 re-proves replacement equivalence under `053-tenure-fence`. |
| U-4 version drift | DECIDED | `#/contracts/8/failure_modes` "Missing context record -> hold reconstruction"; `concurrency` "VersionConflict returns a typed stale-input hold, with no blind retry"; `#/contracts/5/recovery` "never reconstruction of a measurement by inference". Store version ≠ manifest version → typed `ContextHold(VERSION_DRIFT)`; no reconstruction from history. |
| U-5 M-CONTEXT shadow vs bootstrap checkpoint | DECIDED, out of FX-C2 | DAG C2 `completion_predicate` contains no checkpoint comparison; `proceeds_regardless` = "Local context/manifest comparisons; no resident handoff"; M-CONTEXT rollback keeps the checkpoint active until an explicit retirement gate; POSTW1-DECIDE-006A gates handoff; SF-REQ-053-AC-07 migration is owned by R5 (FX-R5). The live checkpoint is operational state, which `conditional_proof` requires a separately authorized target for. Recorded as residual `M_CONTEXT_SHADOW_DEFERRED`. |
| U-6 malformed DecisionInbox | DELEGATED, labelled choice | "Preserve compatible interfaces" and "changes to C1 behaviour" are out of scope. The context adapter reads and validates the raw `decision-inbox` aggregate itself; a malformed shape → `ContextHold(MALFORMED_DECISION_INBOX)`. `DecisionInbox.list_open` is not changed; its silent `()` on malformed state is recorded as a deferred note for its owner, not repaired here. |
| Admission prerequisite (C1/U1 verdicts) | DISCHARGED | Verdict and closure receipts cited under Pinned inputs above. |

**Interfaces under test (PLANNED; module splits are implementation-local).**
`context_assembly/ports/context_assembler.py` `ContextAssembler.reconstruct(manifest_ref) -> ReconstructedContext | ContextHold`;
`context_assembly/domain/reconstruction.py` (canonical `authorized_next_action_set`, `blocked_set`, `current_work`, `unresolved_decisions`, `pending_attention` — kept separate from decisions — `evidence_refs`, `lifecycle`);
`context_assembly/application/reconstruction_service.py`; a compatible addition to `composition/control_plane_profile.py`.

**Commands** (all under `rtk proxy`, `PYTHONPATH=src`).
1. PLANNED `python3 -B -m pytest -q tests/context_assembly/test_context_reconstruction.py`
2. PLANNED `python3 -B tools/evidence/fx_c2_evidence.py --output <new-dir> --invocation <exact-invocation>` (output directory must be new; one mutation application per control on a disposable copy, as `tools/evidence/fx_c1_evidence.py`).
3. Regression: `python3 -B -m pytest -q`; `python3 -B tools/fitness/check_architecture.py --root src/alienintent --check all`; `node scripts/check.mjs all`. Each exits 0.

Fresh invocations are two separate `sys.executable` subprocesses given only the profile root and
manifest ref; no stdin, argument or environment value carries transcript content.

**Probes (SF-REQ-053-AC-01).**
| Test id (PLANNED) | Expected |
|---|---|
| `test_fresh_invocations_equal` | Predecessor commits the durable event and exits; successors A and B run; all three canonical documents (six fields + `lifecycle`) are byte-equal with equal digests. |
| `test_mismatch_is_failure` | A planted store change after pinning → `MISMATCH` naming differing fields, nonzero exit; a started process is never PASS. |
| `test_conversation_not_input` | Present vs absent `transcript.jsonl` / `ALIENINTENT_CONVERSATION` (contradictory prose) → equal digests; no `input_refs` names the transcript. |
| `test_list_order_independent` | Reversed `list_states` order → equal output. |
| `test_derivation_rule_uses_lifecycle` | Each authorized action is accepted by `lifecycle.transition` on the item's state; blocked items carry a typed reason (U-1). |
| `test_seen_is_not_resolved` | SEEN attention stays in `blocked_set`/`pending_attention`; DecisionInbox entries only in `unresolved_decisions`. |
| `test_unavailable_state_holds[missing\|drift\|digest\|evidence\|store\|malformed_inbox\|inventory]` | Typed `ContextHold` with reason and affected refs; never an empty or guessed action set; runner exits 2 (HOLD). |

**Discriminating controls** (intact exit 0 → fault exit 1 at the named assertion → restored exit 0; `application_count` 1).
`manifest_digest_pin` → `[digest]`; `missing_record_hold` → `[missing]`; `store_unavailable_hold` → `[store]`; `version_fence` → `[drift]`; `malformed_inbox_hold` (fall back to `list_open`) → `[malformed_inbox]`; `canonical_order` → `test_list_order_independent`; `conversation_isolation` → `test_conversation_not_input`; `context_item_omitted` (drop `evidence_refs`) → `test_fresh_invocations_equal`; `mismatch_as_failure` (comparator always equal) → `test_mismatch_is_failure`; `lifecycle_rule_bypassed` (emit every action regardless of state) → `test_derivation_rule_uses_lifecycle`; `seen_not_resolved` / `queue_distinct` → `test_seen_is_not_resolved`; `adapter_import_added` (domain imports adapter) → architecture check nonzero.
A non-discriminating mutation, `application_count` ≠ 1 or a missing observation is a HOLD, not PASS. Hang-only controls are residuals.

**Evidence** under `docs/evidence/wave2-proof-fixtures/FX-C2/` in the FX-C1 layout:
`execution-record.json` (`record_kind: ProofFixtureExecution`, `schema_version: 1`, `fixture_id: FX-C2`, invocation, `source_revision`, empty `source_status`, `admission_baseline`, `input_digests`, `commands[{id, command, exit_status, observation_ref}]`, `labels` [`LABELLED_DERIVATION_RULE_v1`, `LOCAL_PROCESS_BOUNDARY_SUBSTITUTE_FOR_EPISODE`], `residuals` [`M_CONTEXT_SHADOW_DEFERRED`, `DECISION_INBOX_LIST_OPEN_SILENT_EMPTY`], `independent_verdict: PENDING_FRESH_BIU_VERIFIER`, `live_proof: NOT_ESTABLISHED`, tokens/cost `null` with `UNKNOWN` reason); `run-report.json` (AC-01 → probes, predecessor/successor digests, field equality verdict, hold reason codes); `proven-red.json` (per control: `control, application_count, source_digest, mutation, command, assertion, fault_exit, restored_exit, fault_ref, restored_ref, discriminates`); `observations/<sha256>` + `digest-manifest.json` (immutable, collisions refused, prior observations preserved on repair). Reconstruction outputs retained as S1 `Observation` (`evidence_id="context.reconstruction"`).

**Boundaries.** No resident/bootstrap handoff, checkpoint retirement or M-CONTEXT cutover; no live stores, providers, network, model launch or RAI; no C3 or C4 scope; no C1/U1 behaviour change. A local PASS is not operational acceptance.
