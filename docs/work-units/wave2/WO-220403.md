# WO-220403 — Shared-profile role binding and compatibility proof

Wave 2 BIU compiled from normalized supply baseline ffadf3d.
Candidate contract sha256 c08ce43de5353f305202fffa6a54f8e0ca07d4c2581f8b7f63de72281365cddb.

## Intent

Deliver the bounded planned extent: Shared-profile role binding and compatibility proof

## Requirements satisfied (extent)

- SF-REQ-039

## Fixed decisions

- SF-REQ-039 already authorizes deterministic Test Worker coverage of the canonical producer/verifier lifecycle. Execution Coordination owns lifecycle orchestration; Invocation Runtime owns real/test worker invocation behind WorkerProvider. No new owner is required.
- Closure records only actions actually performed/read back by local adapters; never populate required_closure_actions merely because a producer succeeded.
- Offline tests exercise the production orchestration path through composition, not a second mocked lifecycle.
- K2 / canonical_role_orchestration owns the broader success-collapse replacement inside Execution Coordination: producer success advances only to VERIFY, a distinct independent verifier evaluates the exact candidate, verifier rejection records findings and returns to IMPLEMENT under existing attempt/budget policy, and acceptance/review/closure remain canonical lifecycle operations. K1 does not implement that orchestration; K1 supplies only the durable real WorkerProvider outcome/readback seam that K2 consumes.
- WorkerProvider.start/read_back/cancel remains the common worker boundary for Real Worker and Deterministic Test Worker. Durable role, invocation, attempt, contract/candidate and finding/receipt correlation is retained in existing invocation/effect/execution evidence records. Extend the typed result contract only if revised Design Verification proves current WorkerOutcome cannot carry a required observable; do not introduce a separate RoleOutcomeRecord state owner by default.
- Before activating multi-role behavior in shared/live profiles, composition must prove the configured worker adapter and durable correlation/readback path. Missing/miscorrelated outcome holds the transition even if the worker process exits successfully. Existing unmodified profiles are not claimed to satisfy future multi-role proof until their revised design is implemented.
- Worker Port is the same worker-facing port/protocol used by production: WorkerProvider -> Deterministic Test Worker for scripted scenarios, beside WorkerProvider -> Real Worker -> provider. No test-specific lifecycle shortcuts or script-driven mark_done.
- Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: With K1/K2 predecessor proof, actual sandbox_run_profile and github_profile constructors using local transports refuse missing/miscorrelated role authority, candidate custody or durable outcome binding before worker launch. A correctly bound real adapter reaches correlated success/rejection/restart readback through existing WorkerProvider/evidence ownership. Disconnect correlation or bypass the binding guard and the proof must fail. No live/shared-profile activation is claimed.
- Owns capabilities: composed_role_bindings
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.
- DAG scope: K3 proves shared-profile composition compatibility after K1 durable outcome/readback and K2 canonical orchestration. sandbox_run_profile/github_profile must bind the configured WorkerProvider and durable role/candidate/result correlation before multi-role worker launch; no parallel sidecar state owner.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220402 (Issue #115, READY): Under existing Execution Coordination / Invocation Runtime ownership and after K1 proof, exercise durable role routing with distinct producer/verifier/closure invocations, exact candidate custody, canonical verifier rejection -> IMPLEMENT repair, remaining attempt/budget enforcement, trusted REVIEW verdict, and actual closure receipts. No success-collapse fallback, scenario-written lifecycle state, or parallel RoleOutcomeRecord state owner.
- WO-220209 (Issue #105, DONE): Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result.

## Acceptance criteria / completion predicate

- With K1/K2 predecessor proof, actual sandbox_run_profile and github_profile constructors using local transports refuse missing/miscorrelated role authority, candidate custody or durable outcome binding before worker launch. A correctly bound real adapter reaches correlated success/rejection/restart readback through existing WorkerProvider/evidence ownership. Disconnect correlation or bypass the binding guard and the proof must fail. No live/shared-profile activation is claimed.

## Proportional proof plan

Proof fixture: FX-K3.

Material failure classes:
- Sandbox/GitHub composition refuses multi-role launch when role authority, exact candidate custody or durable result correlation is absent or miscorrelated.
- A correctly bound real WorkerProvider path reaches attributable success/rejection/restart readback through existing evidence ownership.
- Bypassing the composition binding guard or disconnecting outcome correlation fails before lifecycle advancement.
- No parallel sidecar state owner or live/shared-profile activation is introduced.

Existing proof surfaces:
- tests/composition/test_sandbox_run_profile.py
- tests/execution_coordination/test_github_work_management.py
- tests/invocation_runtime/test_real_worker_outcome.py
- src/alienintent/composition/github_profile.py

Initial bounded commands:
- python3 -m pytest -q tests/composition/test_sandbox_run_profile.py tests/execution_coordination/test_github_work_management.py tests/invocation_runtime/test_real_worker_outcome.py

Proof-policy constraint:
- One discriminating negative control per materially distinct failure class.
- Additional controls require a distinct invariant; no branch-by-branch mutation matrix.
- Broad regression runs once after focused proof is green.

Explicit non-claims:
- No live/shared-profile multi-role activation is authorized.
- No outcome sidecar state subsystem is introduced.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.
- Use existing WorkerProvider and invocation/effect/execution evidence ownership; do not create OutcomeEvidencePort/RoleOutcomeRecord sidecar state unless separately reviewed as necessary.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Execution allocation

- PRODUCER: Morty / claude-opus-5-5.
- VERIFIER: JC / gpt-reserve, fresh independently owned worktree.
- WIP: active profile, currently one mutating execution slot.
- Maximum execution cycles: 3.
- Maximum replacement workers per phase: 1.
- Tokens/cost: measured where exposed; UNKNOWN otherwise.

## Landing

SWF-19: merge exact accepted candidate SHA; no pull request.

## Stop and escalation

- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Return a genuine interface incompatibility to K1/K2's existing owner; do not create a new role/outcome state owner.
- Any live/shared-profile activation remains separately authorized and is not satisfied by K3 compatibility proof.

## Readiness

Remain TASKS until every predecessor BIU is DONE and a fresh native Agent Ready assessment of this exact revision returns READY.
READY is eligibility, not release. Release/IMPLEMENT is not authorized.
