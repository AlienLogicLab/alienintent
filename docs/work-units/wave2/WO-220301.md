# WO-220301 — Durable attention identity and resolution

**BIU:** `WO-220301` — Wave 2 DAG node `C1`.

**Source of truth:** `docs/evidence/wave2-candidate-bius.json#/bius/13` at `origin/main` commit `fcb65fffda65fc2a01516aed38d8d5a662cbaba8`, source blob `64c113e6788728be3930e1f5a300f83a96f5ec70`; the approved DAG is `docs/evidence/wave2-dependency-dag.json`, node `C1`. This work unit is a materialization of that pinned contract.

**Parent requirement:** `SF-REQ-053`. **Priority:** P0. **Wave:** 2.

## Intent

Deliver the bounded planned extent: durable attention identity and resolution.

## Dependencies

- `WO-220102` / S1 — completed and independently verified.
- `WO-220103` / S2 — completed and independently verified.

Consume retained predecessor proof receipts. A planned capability is not execution substrate.

## Scope

- DONE/judgment events dedupe across restart.
- Notification delivery failure preserves the pending item and records the failed attempt.
- SEEN is not RESOLVED; wrong actor, revision, or lane cannot resolve.
- DecisionInbox remains distinct.
- Preserve origin IDs, history, and handler attribution.
- Migration `M-ATTENTION` is staged: snapshot existing queue/history without retiring writer; stage aliases and pending/seen/resolved history; compare producers/consumers; one-writer switchover only after replacement gates. Rollback retains source queue/aliases and reconciles pending effects.

## Acceptance and proof

- `SF-REQ-053-AC-05`: notification failure leaves an item pending with a recorded failed attempt; a later authorized consumer can inspect and handle it without the former conversation.
- `SF-REQ-053-AC-06`: without unattended-activation authority, a pending item may notify but must not launch an authority-bearing episode; an explicit authorized activation policy is an input, never presumed.
- Pin `FX-C1` commands, inputs, expected outcomes, and evidence schema before implementation. Demonstrate intact/fault/restored discrimination. The planned proof is local/composed/mechanical; no local substitution claims operational acceptance.

## Boundaries

One named DAG node and FX-C1 only. No sibling node, RAI wiring, lifecycle redesign, live operation, source-authority migration, bootstrap retirement, or unapproved host activation. The temporary Factory Director Host remains `KEEP_UNTIL_REPLACED`; C1's unbound activation path launches zero models.

## Admission and custody

This work unit is not READY or release. A native Agent Ready assessment, explicit execution allocation/budget, exact admission baseline, profile binding, predecessor evidence, candidate publication, independent retrieval, and normal BIU verification/closure remain mandatory. No Wave 1 release authority carries over.

The Founder-authorized temporary deterministic Factory Director Host is non-cognizant transitional infrastructure only. It must not replace canonical C1/C2/C3 work, alter lifecycle authority, or be activated/retire existing protection without the required live cross-episode proof.
