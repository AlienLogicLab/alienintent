# WO-220501 — External bootstrap custody and consumer inventory

Wave 2 BIU compiled from supply-frontier planning baseline `e337182`.
Candidate contract sha256 `bbf4ea270a367ca98facf3b4e4f99472b647440039a0124268e9c5295a263164`.

## Intent

Deliver the bounded planned extent: External bootstrap custody and consumer inventory

## Requirements satisfied (extent)

- No Product Requirement identity is claimed by this prerequisite; it supplies the named DAG capability.

## Fixed decisions

- Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.
- No bulk import or deletion; preserve release, monitoring, attention, notification and evidence dependencies.
- Preserve external protection until proof and authority; missing implementation returns to its existing owner. Selected contracts do not authorize external scope.
- B0 authority gate was too broad; read-only current inventory/custody classification may proceed.
- Separate inventory, provenance-preserving migration, live replacement proof, and explicit retirement/risk decisions. Unknowns block only the affected component path.

## Scope

- Extent: Produce a current bootstrap custody manifest covering all discovered external assets, exact revisions/hashes, consumers/service references, test/evidence provenance, and a truthful per-component custody class. Include newly discovered shell helpers/Factory Director Host. Unknowns are explicit and local to the affected component. Perform no migration, deletion, service change, live replacement, cutover or retirement.
- Owns capabilities: bootstrap_custody_manifest
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.
- DAG scope: Read-only current external-bootstrap inventory and custody classification. Inventory the current directory (not a fixed historical 18-file assumption), exact revisions/hashes, consumers, service references and provenance; classify each component's custody/replacement status truthfully. Unknowns hold only the affected component path. No migration, deletion, service change, cutover or retirement.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220101 (Issue #69, DONE): Real temporary SQLite; disposable local bare Git remote, publication/fetch into a fresh verifier worktree, local work-management and provider transport, injected clock/IDs, durable scripted worker journal, credentials absent and enforced denied outbound network with attempted-network refusal. Count zero provider calls. Run existing kernel unchanged and report success-collapse limitation; no scripted lifecycle transitions.

## Acceptance criteria / completion predicate

- Produce a current bootstrap custody manifest covering all discovered external assets, exact revisions/hashes, consumers/service references, test/evidence provenance, and a truthful per-component custody class. Include newly discovered shell helpers/Factory Director Host. Unknowns are explicit and local to the affected component. Perform no migration, deletion, service change, live replacement, cutover or retirement.

## Proportional proof plan

Proof fixture: **FX-B0** — local/composed or mechanical unless explicitly stated otherwise.

Material failure classes:
- Inventory discovers the current external bootstrap surface rather than assuming the historical eighteen-module list is complete.
- Each discovered asset retains exact path plus content/revision identity and attributable consumers/service references where observable.
- Custody classification distinguishes live KEEP_UNTIL_REPLACED protections, historical/provenance utilities, replacement-proof candidates and explicit UNKNOWN without inventing retirement authority.
- Unknowns block only the affected component classification/replacement path; unrelated inventory remains reportable.
- The inventory pass performs zero migration, deletion, service restart/stop, cutover or retirement effects.

Existing proof surfaces:
- `docs/evidence/2026-09-25-external-bootstrap-custody-refresh.md`
- `docs/evidence/wave1-bootstrap-retirement-matrix.json`
- `tools/orchestration/systemd/alienintent-factory-director-host.service`

Initial bounded commands:
- `python3 -m pytest -q tools/evidence/test_bootstrap_custody_manifest.py`

Proof-policy constraint:
- Use one discriminating negative control per materially distinct failure class by default. Additional controls require a distinct invariant/risk; do not mutation-test implementation branches exhaustively.
- Focused tests and architecture checks run before any wider regression. Full repository regression is a terminal confidence check, not repeated per negative control.
- Retain intact/fault/restored evidence for the material controls actually selected.

Explicit non-claims:
- No external bootstrap file is deleted, moved or rewritten.
- No service is started/stopped/restarted and no replacement/retirement is authorized.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.
- Do not assume the historical eighteen-module list is complete. Discover current assets and consumers read-only; preserve KEEP_UNTIL_REPLACED protections.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Execution allocation

- PRODUCER: Morty via configured Claude provider (`claude-opus-5-5`).
- VERIFIER: JC via configured Codex provider (`gpt-reserve`), fresh independently owned worktree and exact candidate retrieval.
- WIP/concurrency: governed by active factory profile (currently one mutating execution slot).
- Maximum execution cycles: 3.
- Maximum replacement workers per phase: 1.
- Wall-clock: existing systemd supervision (120 min runtime + 2 min startup + 5 min stop grace).
- Tokens/cost: measured when exposed; otherwise UNKNOWN, never inferred zero.

## Landing

SWF-19: merge the exact accepted candidate branch directly into the baseline branch, preserving the accepted SHA. **No pull request.**

## Stop and escalation

- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Inventory/classification uncertainty is recorded as UNKNOWN for the affected component and does not block unrelated inventory.
- Any migration, deletion, service change, live replacement, cutover or retirement requires its separately authorized proof/decision and is outside B0.

## Readiness

A fresh native Agent Ready assessment of this exact work-unit revision is required before READY. READY is eligibility, not release.
Release/IMPLEMENT is explicitly not authorized by this document.
