# WO-220303 — Bounded episodes and stale-command refusal

Wave 2 BIU compiled from the approved Wave 2 design (DAG node C3).
Baseline `7be12188edf632710fe842d95fc70b444e2ed279`. Candidate contract sha256 `a1d3d62d871e253238ae44f7a8863d498013b731f82590dfc54bdb1a02b9adc6`.

## Intent

Deliver the bounded planned extent: Bounded episodes and stale-command refusal

## Requirements satisfied (extent)

- SF-REQ-053

## Fixed decisions (from the verified Design Contract; not reopened here)

- (SF-REQ-053) Default tenure policy: one BIU, maximum age 3600 seconds, maximum 32 admitted transitions, maximum blocked duration 300 seconds, accepted contradiction count 1, any relevant state-vector mismatch ends tenure. Default context-usage source is UNBOUND and its numeric threshold is disabled; absence of a binding alone does not terminate every episode. All other tenure limits remain active. If explicitly BOUND, threshold is 0.8 and reaching it ends tenure; unavailable, stale, wrong-invocation or invalid measurements terminate conservatively. Monotonic clock within process; persisted UTC deadline for restart; clock regression holds. Values are versioned composition policy, not authority or increased budget. The 3600 seconds is a proposed configurable design default, not a claim that the bootstrap waiter timeout or historical values are canonical; no empirical derivation or inheritance is asserted. Terminal/objective/authority/provider/model changes and explicit request end immediately. Reaching any limit ends, never silently renews; authorized begin of new epoch retains budget accounting. SF-REQ-008 durability elaboration and SF-REQ-029 provider normalization amendments are excluded. Existing store versions/events suffice; no promise of normalized provider telemetry. POSTW1-DECIDE-006A gates actual bootstrap handoff. Tenure checks run on each result/event and an injected deadline timer independent of the model. Epoch expiry prevents new authority-bearing commands but does not fabricate cancellation of already-admitted workers. Relevant authority/source changes advance a durable applicability revision observed by guarded admission; an external source whose freshness cannot be checked causes a hold. For a BOUND context-usage source only, unknown usage is conservative termination; UNBOUND is explicitly recorded and never represented as zero usage or measured-safe context. Context observation validity is decidable: matching current invocation, finite used >= 0, finite limit > 0, and sample from the current tenure check (request correlation must match); unavailable or prior-check samples end tenure when BOUND. Compare used/limit >= 0.8; no normalization or estimated values. Default UNBOUND policy does not claim telemetry-based context safety. Explicit context-exhausted operator/provider events still end tenure; neither they nor the optional fixture close LRN-023. Contradiction is a judgment obligation owned by the authorized episode operator under the episode authority, not an inferred semantic predicate. The operator names conflicting authoritative facts/actions and records rationale and evidence refs. Mechanically count only AcceptedContradiction for the current episode/epoch/vector with authorized actor and nonempty resolvable conflicting_refs/rationale; one ends tenure. Invalid, stale or unauthorized reports produce InvalidJudgment and cannot act as authoritative termination input. Truth of the contradiction requires operator judgment; fixture automation proves admission and termination, not semantic truth.
- (shared contracts, `#/fixed_design_baseline/shared_contracts`) Pinned Phase 9 shared value, evidence, store, failure and composition contracts apply within node scope. Descriptive imports and withdrawn R1 policy remain non-normative; retain all authority boundaries.

## Scope

- Extent: Boundary tests: one BIU, age 3600s, 32 transitions, blocked 300s, one accepted contradiction, state-vector/authority/provider/model change. UNBOUND usage is explicit with numeric check disabled; BOUND uses same-invocation/current-check valid observation and >=0.8 ends, missing/stale/invalid ends conservatively. Invalid contradiction judgment cannot end as authoritative input. Two competing epochs cannot overwrite; no automatic renewal/budget reset.
- Owns capabilities: bounded_episode_control
- Boundary: Only this node extent. Requirement/design references constrain execution and do not absorb sibling scopes.

## Non-goals

- Replanning the DAG or implementing another node; changing requirements, closing authority gaps or selecting withdrawn architecture policy.
- Automatic release, live operation or retirement from a candidate or local proof result.

## Dependencies

- WO-220302 (C2, Issue #95): Two fresh invocations reading the identical manifest reconstruct equal authorized_next_action_set and blocked_set, current work, unresolved decisions and evidence refs. Conversation memory is neither an input nor authority; unavailable state produces hold.
- WO-220103 (S2, Issue #74): Two SQLite connections/processes compete; remove fence guard to kill assertion. BEGIN IMMEDIATE checks vector, reservation, epoch/authority and unique intent atomically at intent and claim; validate receipt at confirmation. Crash around intent/send/readback, delayed sender, sorted multi-reservation locking, unknown outcome retain ownership. Consumer dedupes and fences or refuses reassignment until confirmed death/completion.
- WO-220205 (U5, Issue #97): Existing architecture checks first, then distinct reviewer invocation bound to exact design/proof/premise revisions. Missing decisions, impossible premise, self-review and stale revision hold. Open directional-authority question returns ARCHITECTURE_AUTHORITY_HOLD for affected admission; observed_edges never becomes policy.

Dependency state at this baseline (canonical read-back): WO-220103 (#74) is DONE; WO-220302 (#95) is DONE (accepted
candidate `46920041299d4fac28c3bbd65eb25c5ac1823787`, landed by direct merge `7b6e19c4a17f1d797f61c99edafadd4857940dd6`);
WO-220205 (#97) is DONE (accepted candidate `6518d1ef09fc99e8abccf1c6234106f860fce7e6`, landed by direct merge
`036c5fc2d2c7f1699b23ac171e9e019aa8d4fc98`). This BIU's proof packet is pinned below (§ "Pinned proof packet (FX-C3)"). Admission: Consume exact independently verified predecessor candidate and retained proof receipts; predecessor authority holds must be discharged by their named actor. A planned capability is not available execution substrate.

## Acceptance criteria

- Boundary tests: one BIU, age 3600s, 32 transitions, blocked 300s, one accepted contradiction, state-vector/authority/provider/model change. UNBOUND usage is explicit with numeric check disabled; BOUND uses same-invocation/current-check valid observation and >=0.8 ends, missing/stale/invalid ends conservatively. Invalid contradiction judgment cannot end as authoritative input. Two competing epochs cannot overwrite; no automatic renewal/budget reset.
- SF-REQ-053-AC-02: Each tenure termination cause in SWF-27 (terminal outcome, objective/authority change, context/contradiction/staleness/age/transition limits, prolonged block, provider replacement or explicit request) has a policy fixture that ends or renews tenure explicitly; the same expired episode cannot continue making authoritative changes.
- SF-REQ-053-AC-03: Return a coordinator result against an obsolete state revision: it is refused without overwriting newer authoritative state. A result at the applicable current revision still undergoes ordinary authority validation.

## Verification obligations

Proof fixture FX-C3 (PLANNED_NOT_EXECUTED, proof level LOCAL_COMPOSED_OR_MECHANICAL):

- Environment: Disposable local profile unless explicitly marked operational; no live action authorized by this plan.
- Scenario: Boundary tests: one BIU, age 3600s, 32 transitions, blocked 300s, one accepted contradiction, state-vector/authority/provider/model change. UNBOUND usage is explicit with numeric check disabled; BOUND uses same-invocation/current-check valid observation and >=0.8 ends, missing/stale/invalid ends conservatively. Invalid contradiction judgment cannot end as authoritative input. Two competing epochs cannot overwrite; no automatic renewal/budget reset.
- Acceptance IDs: SF-REQ-053-AC-02, SF-REQ-053-AC-03
- Probe SF-REQ-053-AC-02: Boundary-test every tenure cause including all numeric limits and explicit renewal to new epoch. Exercise UNBOUND composition, BOUND valid/unavailable boundaries and admitted versus invalid contradiction judgments as specified; semantic contradiction truth remains operator judgment. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)
- Probe SF-REQ-053-AC-03: Two competing results: obsolete vector/epoch cannot overwrite; current still requires authority. (Controlled fixture or independently inspected versioned artifact; retain inputs, observed outputs and verdict separately.)

Enforcement obligations:

- `053-tenure-fence` (python_unit_integration, BLOCKING): Exercise every named termination cause and stale-vector/epoch rejection; replacement reconstructs equal allowed/blocked sets from same snapshot. Include composition with no usage source (other causes still enforce), bound just below/at 0.8, bound unavailable/stale/wrong invocation/invalid limit, and explicit context-exhausted event. For contradiction use accepted operator judgment plus stale/unauthorized/missing-ref controls; assert exact termination/refusal cause. Proven-red obligation: Accept old epoch or omit context item and require stale-write/equivalence assertion failure. Treat UNBOUND as zero or as automatic termination, ignore bound Unavailable, or accept an unauthorized contradiction independently; corresponding policy-state/cause assertions must fail.

Order: Before implementation, pin executable fixture commands, inputs, expected outcomes and evidence schema against this node predicate; demonstrate intact/fault/restored discrimination for mechanical controls. Retain independent judgment where specified. S0 establishes the shared harness at current interfaces; all consumers wait for predecessor proof. No future fixture is claimed executed here.

Conditional proof: Operational fixtures require their separately authorized target and actual readback. Local substitutions must be labelled and cannot satisfy operational acceptance.

Evidence contract: Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.

## Evidence obligations

- Retain exact fixture/source/profile/candidate revisions, invocation and command, expected/observed result, exit status, immutable observation refs, independent verdict where required; never infer operational success from a local result.
- Retain predecessor candidate identities, proof fixture revision, full command/exit status, raw observations, negative-control application counts and independent verdicts separately. Preserve prior proof on repair; supersession needs explicit authority.
- Retain all authority dispositions and migration/rollback/readback receipts applicable to this node. Missing or unavailable measurements are explicit holds, never zero or PASS.

## Architecture constraints

- Apply the pinned shared contracts and linked contract sections within this node extent. No service or context boundary beyond the pinned design, RAI wiring or adopted withdrawn edge table.
- Preserve compatible interfaces, persisted resource identities, one-writer ownership and existing approved architecture checks.

## Execution packet and allocation

Allocation: `docs/evidence/wave2-execution-packets/WO-220303.allocation.json` — deterministic
Allocator branch; configured PRODUCER (Morty, Claude) and independent VERIFIER (JC, Codex);
concurrency 1; 3 execution cycles and 1 replacement worker per phase (`execution.biuLimits`,
bound before release); wall-clock bounded by systemd supervision (120 min runtime + 2 min
startup + 5 min stop grace); tokens and cost measured, never assumed zero. Remaining
allowance: 3 of 3 cycles.

Landing: SWF-19 — merge the accepted candidate branch directly into the baseline branch,
preserving the accepted SHA. **Do not open a pull request.**

Release authority is not granted by this document.

## Candidate custody

Future PRODUCER pins contract/input/baseline/profile and candidate branch+full SHA; publishes candidate to configured authorized remote and records exact branch/SHA on the Issue before RESULT=VERIFY. Fresh independent verifier retrieves that exact SHA in its own worktree; retain retrieval and verdict receipts.

Retain actor-owned branch/worktree through required closure; disposition LANDED, DISCARDED or PARKED with durable blocker. No direct main push by a BIU worker.

## Stop and escalation

Stop:
- Hold this BIU on unmet dependency proof, unbound budget/custody, authority gap/gate, source revision mismatch, unavailable evidence, failing discriminating probe or conflicting ownership. Preserve unrelated completed proof.

Escalate:
- Route unmet source/product authority through Program Director to SPECIFY; Founder alone disposes the directional boundary question. Pin decision and obtain reviewed affected design/DAG revision before implementation.
- Return interface/scope changes or defects in prerequisites to their named owner; do not repair them inside a capstone. Missing execution allocation goes to the authorized allocator; no invented budget.

## Pinned proof packet (FX-C3)

Pinned by Factory Director episode `factory-director-7a0b34a1ef72490b8f181f9c8bc8da09`, finalizing the owner-question triage recorded on
Issue #99 (https://github.com/AlienLogicLab/alienintent/issues/99#issuecomment-5824252372, episode
`factory-director-4370af4e4d824430a4f3a580cf207686`), from the draft
`docs/evidence/wave2-execution-packets/WO-220303.proof-packet.draft.md` (before its status line was updated to
`DRAFT_SUPERSEDED_BY_PIN`, prepared read-only against baseline `7be1218` by that same Issue-#99 triage). That triage
left U-3 and U-4 DELEGATED pending WO-220302 (#95) landing: "FX-C3 consumes only C2's landed signature, so U-3/U-4 and
the packet pin are finalized after #95 lands." WO-220302 (#95) and WO-220205 (#97) are both now DONE; this pin
finalizes U-3 and U-4 against the actual landed C2 source and re-verifies every other input digest at this baseline
(`555b76e`). Nothing here authorizes release, implementation, or live operation.

### Director dispositions (these control over the draft text below)

| Q | Disposition | Pinned answer and authority |
|---|---|---|
| U-1 episode aggregate as authority record | DELEGATED, unchanged | `#/contracts/8/identities`, `persistence`; `#/shared_contracts/operational_storage`. `episode:<objective>` as the `GuardVector.authority` aggregate, `deadline_utc` persisted alongside for restart. |
| U-2 "reaching" a limit (`>=`) | DECIDED, unchanged | `#/contracts/8/design_decisions[1]` "Reaching any limit ends"; `[4]` "used/limit >= 0.8". |
| U-3 "blocked" interval and reset | **DELEGATED, Director-confirmed, grounded in landed C2** | A continuous interval, from a durable `blocked_since`, during which the item matching the episode's own objective identity appears in the C2-reconstructed `blocked_set` (`work == objective`) and has no matching entry in `authorized_next_action_set`. Grounded directly in `derive()`, `src/alienintent/context_assembly/domain/reconstruction.py:211-250` (Director-reread and confirmed at this baseline): for any identity, membership in `blocked` (lines 230-236) and probing for `actions` (lines 238-241) are mutually exclusive by construction — a `continue` immediately after `blocked.append(...)` skips the actions loop entirely — so the predicate is well-formed against the real field shapes, not a hypothetical. An unblock resets only `blocked_since`; age/transition/usage/contradiction counters are unaffected (`#/contracts/8/design_decisions[0]`, separately named limits). No contract clause defines "blocked" more precisely than "maximum blocked duration 300 seconds" / SWF-27 "prolonged blocking" (`docs/decisions/2026-09-20-persistent-control-plane-bounded-coordinator-episodes.md:23`), confirming this is genuinely delegated engineering, not an already-decided clause. |
| U-4 relevant state-vector membership | **DELEGATED, Director-confirmed, grounded in landed C2, corrected** | The vector is `GuardVector.versions` (`src/alienintent/execution_coordination/ports/fenced_store.py:9-24`; Director-reread and confirmed at this baseline): the `{aggregate: version}` pairs from the C2 manifest's pinned live record set (`manifest["entries"]` — `factory:*`, `release:*`, `attention:*`, `decision-inbox`, `upstream:requirements:current`; `pin_manifest`/`validate_manifest`, `context_assembly/domain/reconstruction.py:113-148`) **plus** the episode's own authority aggregate `episode:<objective>`, which `GuardVector.__post_init__` (`fenced_store.py:15-24`) structurally requires to be a member of `versions` (`self.authority not in dict(versions)` raises `ReservationRejected`). **Correction from the draft:** the `context:manifest:<id>` pointer aggregate itself is *not* a vector member — it is content-addressed and immutable once installed (`reconstruction_service.py:78-107`), so re-checking it adds no discriminating power over checking the live aggregates it names directly; `_validate_vector` (`sqlite_store.py:333-355`) already validates each named aggregate's live version plus the authority row's `schema_version/active/epoch/invocation/expires_at`. |
| U-5 provider/model change signal | DELEGATED, unchanged | `design_decisions[1]`, `[2]` (SF-REQ-029 excluded). |
| U-6 applicability-revision carrier | DELEGATED, unchanged | `design_decisions[3]`. |
| U-7 operator surface for contradiction judgments | DELEGATED, unchanged | `#/contracts/8/composition`; C1 `ResolverGrant` precedent; FOUNDER branch does not apply since no new authenticated surface is needed. |
| U-8 BOUND usage fixture | DECIDED, unchanged | `composition` "same-invocation evidence adapter injection"; labelled-substitution rule. |
| U-9 C3 consumption of U5; directional authority | DECIDED, not FOUNDER, unchanged, condition re-verified at `555b76e` | DAG C3 carries no `authority_gap_refs`/`authority_gate_refs`. Binding condition: PRODUCER introduces no new cross-context module pair. Independently re-scanned at this baseline: `control_plane/{domain,ports,application,adapters}` import no `context_assembly` module; the reverse scan of `execution_coordination` for `context_assembly` imports is empty. The only `context_assembly` import is composition-layer (`src/alienintent/composition/control_plane_profile.py:5`), matching the precedent C2 itself established (`AttentionProfile.context`/`ContextProfile`). No new pair found; condition remains satisfiable as planned. |
| U-10 budget carried across epochs | DELEGATED, unchanged | `design_decisions[1]`. |
| U-11 contract 8 `authority_status` hold | DECIDED, unchanged | `authority_gap_refs` is `["R1-GAP-MONITOR-HOST"]` only, which propagates to C5, not C1–C4. |
| Admission | **DISCHARGED** | See "Pinned predecessors" below. |

**Pinned inputs (sha256, re-verified at baseline `555b76e`).**

| Pointer | File | sha256 |
|---|---|---|
| design-contracts | `docs/evidence/wave2-design-contracts.json` | `56676dd97cd09031882ee01e61af2a15071f1b92f6c914fde2169b682f056242` |
| dependency-dag | `docs/evidence/wave2-dependency-dag.json` | `be5f39f8d6d8c319dfd990ae9f52c32e84e7db6af13ef87f9d45b6cbeff20476` (whole-file digest has legitimately advanced past the draft's `f99c0a7a…`; node `C3` = `/nodes/16`, fixture `FX-C3` = `/proof_fixtures/16` re-read and unchanged) |
| candidate-bius | `docs/evidence/wave2-candidate-bius.json` | `5515b6ce24286dd83a40cd295f61dd74a6c55a5b1c407597c0a65db36504d1f1` (whole-file digest has legitimately advanced past the draft's `61f9324e…`; `bius[16]` canonical digest independently recomputed as `a1d3d62d871e253238ae44f7a8863d498013b731f82590dfc54bdb1a02b9adc6`, matching the candidate contract sha256 already pinned at the top of this document) |
| packet | `docs/evidence/wave2-execution-packets/WO-220303.packet.json` | `1248054d61d98b2087647dc51b23229b4e1b81d56f96b7b7f3d6a37bce8127aa` |
| allocation | `docs/evidence/wave2-execution-packets/WO-220303.allocation.json` | `164488d05e2fe4bd146a18c977b426c7bdc45ada69c1ae8eda387db628821d38` |

The contract file's own digest (this file) is not listed here, following the WO-220209/FX-U7 pin convention: a file
cannot byte-stably hash itself before its own edit is complete. The PRODUCER re-pins the contract/packet/allocation
digests from the commit that actually lands this pin.

Status `PINNED_PLANNED_NOT_EXECUTED`. Proof level `LOCAL_COMPOSED_OR_MECHANICAL`. Disposable local profile only.
Anything marked **PLANNED** does not exist at baseline `555b76e`: confirmed absent — `src/alienintent/control_plane/domain/episode.py`,
`ports/episode.py`, `application/episode_control.py`, `adapters/episode_repository.py` and `tests/control_plane/test_episode_control.py`
do not exist in this worktree.

**Pinned predecessors.**
- **C2 (WO-220302, #95): DONE.** Accepted candidate `46920041299d4fac28c3bbd65eb25c5ac1823787`, branch `b-disp/34680cbc-fb01-4160-a9b7-6b22e17a9506`. VERIFIER ACCEPT (JC) https://github.com/AlienLogicLab/alienintent/issues/95#issuecomment-5825255314; closure https://github.com/AlienLogicLab/alienintent/issues/95#issuecomment-5825323137. Landed by direct `--no-ff` merge `7b6e19c4a17f1d797f61c99edafadd4857940dd6`, confirmed an ancestor of this baseline. FX-C2 evidence: `docs/evidence/wave2-proof-fixtures/FX-C2/` (13/13 discriminating controls, predecessor/successor digest `sha256:7055676e6f5ca1df6f99dd61766f47a7d8fa38df281d6ea7115e889b140b6790`). Interfaces FX-C3 consumes, confirmed present at exactly the drafted signatures: `ContextAssembler.reconstruct(manifest_ref) -> ReconstructedContext | ContextHold` (`context_assembly/ports/context_assembler.py:9`); pointer prefix `context:manifest:` (`context_assembly/domain/reconstruction.py:29`); derivation-rule label `LABELLED_DERIVATION_RULE_v1` (`reconstruction.py:14`). C2's own U-3 pin (`docs/work-units/wave2/WO-220302.md` line 123) explicitly defers replacement-under-episode equivalence to C3 under the `LOCAL_PROCESS_BOUNDARY_SUBSTITUTE_FOR_EPISODE` label — FX-C3's `test_replacement_equivalence` probe re-proves it.
- **U5 (WO-220205, #97): DONE.** Accepted candidate `6518d1ef09fc99e8abccf1c6234106f860fce7e6` (after one JC REJECT of `c9a8699a…` for a stale-review-reuse defect, repaired and re-accepted), branch `b-disp/440039d5-1752-4c7a-940c-56672fd74445`. VERIFIER ACCEPT (JC) https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816278191; closure https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816356325. Landed by direct `--no-ff` merge `036c5fc2d2c7f1699b23ac171e9e019aa8d4fc98`, confirmed an ancestor of this baseline. Receipts: `docs/evidence/wo-220205-fx-u5.md`, `docs/evidence/wo-220205-fx-u5-predicate-mapping.json` (sha256 `e000d1f964ce628e50370ae6db5bff62959e7dd2642ae1d089887d0f7e4fef3a`), `docs/evidence/wo-220205-fx-u5/`, `docs/evidence/wo-220205-fx-u5-r2/` (25/25 QUALIFIED_KILL). U-9's binding condition (no new cross-context module pair) re-verified above against the U5-landed tree; no violation found.
- **S2 (WO-220103, #74): DONE, unchanged from the draft.** Accepted candidate `8e2547980ca1d95849460b9abf0c08857a6671b9`, landed by `901d174` (confirmed ancestor of this baseline). Receipts `docs/evidence/wave2-proof-fixtures/FX-S2/`. Verdict `issues/74#issuecomment-5791867997`; closure `#issuecomment-5791914826`.
- **C1 (WO-220301, #78): DONE, unchanged from the draft.** Candidate `8c810d2df815907cb3c1a383d6a645b3cddc7c6e`, landed by `d168b6a` (confirmed ancestor of this baseline). Verdict `#issuecomment-5794214610`; closure `#issuecomment-5794268196`.

C2/U5/S2/C1 source and proof stay unmodified by this pin.

**Reused substrate (existing, unchanged).**
- `execution_coordination/ports/fenced_store.py`: `GuardVector(versions, authority, epoch, invocation)` and `FencedOperationalStore`.
- `SQLiteOperationalStore._validate_vector` (`sqlite_store.py:333-355`): requires an authority aggregate with `schema_version 1`, `active`, `epoch`, `invocation` and a finite `expires_at`, read against an injected clock. Raises `VersionConflict` or `ReservationRejected("inactive or mismatched epoch/authority" | "authority expired")`.
- `StaleFence`, `StoreUnavailable`, `SchemaIncompatible`.
- `execution_coordination/domain/lifecycle.py` `transition` (`AuthorityBlocked`), `composition/fenced_profile.py`, S1 `LocalEvidenceRepository`.

**Planned surface (PLANNED; module splits are implementation-local; confirmed absent at `555b76e`).**
- `control_plane/domain/episode.py`: `TenurePolicy(max_age_s=3600, max_transitions=32, max_blocked_s=300, max_accepted_contradictions=1, usage=UNBOUND|BOUND(threshold=0.8), policy_digest)`; `EpisodeState` {ACTIVE, ENDED}; closed `EndCause`; pure `evaluate_tenure`, `validate_usage`, `admit_contradiction`.
- `control_plane/ports/episode.py`: `EpisodeControl.begin/submit/end`, `ContextUsageObservation.read(invocation_id, check_id) -> Usage | Unavailable`, `ContradictionObservation.record(...) -> AcceptedContradiction | InvalidJudgment`.
- `control_plane/application/episode_control.py`, plus a deadline tick driven by an injected timer.
- `control_plane/adapters/episode_repository.py`: aggregate `episode:<objective>` plus immutable S1 `episode-history`, written history first, then the pointer.
- `composition/control_plane_profile.py`: additive `EpisodeProfile(root, *, project, name, invocation, monotonic, utc_clock, timer, policy, usage=None, context_assembler, operators)`. This is the only place `context_assembly` may be imported by C3, matching the composition-layer precedent already established by C2's own `AttentionProfile.context`/`ContextProfile` addition (`composition/control_plane_profile.py:5,20-48`). `AttentionProfile` stays unchanged.

**Fixture inputs, Commands, Probes, Discriminating controls, Evidence layout, Boundaries** — unchanged from
`docs/evidence/wave2-execution-packets/WO-220303.proof-packet.draft.md` §§ "Fixture inputs" through "Boundaries",
reproduced there in full; nothing in those sections depended on U-3/U-4 phrasing beyond what this pin already
finalizes (`test_blocked_limit[below|at]` now resolves against the U-3 disposition above; `test_state_vector_mismatch`,
`test_replacement_equivalence`, `test_competing_epochs_cannot_overwrite`, `test_obsolete_revision_refused` and
`test_current_revision_still_authority_checked` now resolve against the U-4 disposition above). The retained probe
IDs, discriminating-control needles (★`old_epoch_accepted`, ★`context_item_omitted`, ★`unbound_as_zero`,
★`unbound_auto_terminates`, ★`bound_unavailable_ignored`, ★`unauthorized_contradiction_accepted`, and the rest) and
the `docs/evidence/wave2-proof-fixtures/FX-C3/` evidence layout are pinned as drafted.

**Release authority is not granted by this document.**
