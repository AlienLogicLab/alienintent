# WO-220101 — release record (SWF-35)

Date: 2026-09-22. Released by: resident bootstrap coordinator (temporary release duty, SWF-21 §mechanics), under the Founder's standing Wave 2 incremental release authority ([SWF-35](../../decisions/2026-09-22-wave2-incremental-release-authority.md)). Project #1 issue [#69](https://github.com/AlienLogicLab/alienintent/issues/69). **RELEASED — IMPLEMENT is authorized.**

**Baseline:** `ade44cb93f3f33a570e1cf1b4bda500a79cd95bb` (`origin/main`).

## SWF-35 conditions

| Condition | Evidence |
|---|---|
| Part of the approved Wave 2 plan | `docs/evidence/wave2-candidate-bius.json#/bius/0`, DAG node S0 (`wave2-dependency-dag.json#/nodes/0`) |
| Native Agent Ready READY at the exact current BIU/baseline | `docs/evidence/wave2-readiness-assessments/WO-220101.2026-09-22T110109.877043Z.assessment.json` — producer `agent-ready-cli 0.1.0rc1` @ `6b05681` (installed-package identity), `codex-cli 0.155.1` COMPATIBLE_UNVERIFIED/PASSED; input sha256 `ad4628afff366ef6…` = `docs/work-units/wave2/WO-220101.md` as committed at `b17a1be` (unchanged since) |
| Execution packet fully bound | `docs/evidence/wave2-execution-packets/WO-220101.packet.json`, `WO-220101.allocation.json` (limits stated as enforced; wall-clock declared NOT enforced by the bootstrap; tokens/cost measured) |
| Dependencies satisfied | none declared (root node) |
| No unresolved architecture/product/security/budget authority | `authority_gap_refs` = [] and `authority_gate_refs` = [] on the candidate; no numeric budget ceiling exists in any authority (declared, not assumed) |
| Release-admission checks pass | SWF-21 nine conditions + 2026-09-21 preconditions, run with `release_admission.py 69` before the transition (output retained below) |
| Candidate custody / evidence active | candidate custody policy and evidence obligations carried verbatim in the packet; fresh-worktree verifier retrieval of the exact SHA |
| No new scope / reinterpretation | released text = assessed text (same input fingerprint); scope restated verbatim in the Issue release comment |

## Admission run

(appended by the coordinator when `release_admission.py 69` admits)

```
$ python3 release_admission.py 69        (bootstrap module ~/.local/share/alienintent-bootstrap, 2026-09-22)
BIU #69: status=READY agent_ready=READY baseline=ade44cb93f3f33a570e1cf1b4bda500a79cd95bb resolves=True ancestral=True
ADMITTED: release record complete; READY -> IMPLEMENT may proceed.
```

Gate note: the bootstrap gate was extended test-first (21 tests) to recognise `WO-NNNNNN` BIUs and to
read the disposition only from a `ReadinessAssessment` record whose `outcome` is `ASSESSED` and whose
`producer` is the Agent Ready product (a compatible shape is not evidence). A first run correctly
REFUSED because the Issue body named the pre-rebase branch commit `dfeca645` as a "baseline"; the
wording was corrected so the only baseline named is the release baseline. Release record posted as
an Issue comment was refused by the operator harness ("permission grant"); the record therefore
lives in the Issue body and in this file.

Transition READY → IMPLEMENT performed by the coordinator (SWF-21 mechanics, SWF-35 authority); no
worker launched by the coordinator — the retained Node bootstrap receives the Project event.
