# Agent-Ready request — PY-08

Assess readiness of the Wave 1 implementation BIU `docs/work-units/python/PY-08.md`
("Operator Control Plane CLI (P0 minimum)") in repository `AlienLogicLab/alienintent`, on clean `main`.

Read the BIU contract in full before assessing. It is the controlling document.

## Authority to read

- `docs/decisions/alienintent-software-factory-plan.md` — Founder-approved product plan (SF-REQ-001..047).
- `docs/decisions/2026-09-19-software-factory-wave1-founder-decisions.md` — SWF-01..07.
- `docs/decisions/2026-09-20-wave1-plan-approval-d1-d2.md` — SWF-08..11 (sandbox environment, budget policy, PY-04 scripted-worker limit, PY-10 acceptance).
- `docs/decisions/2026-09-19-alienintent-work-management-execution-authority.md` — FD-01.
- `docs/decisions/2026-09-19-pre-python-gate-founder-decisions-fd02-fd06.md` — FD-02..FD-06.
- `docs/architecture/alienintent-architecture-authority-2026-09-19.md` — Architecture Authority.
- `docs/architecture/pre-python-gate/` — gate-reclassified candidate contracts (22 category A, 9 category B, 0 category C).
- `docs/work-units/sf-wave1-plan.md` — the approved Wave 1 dependency DAG and decomposition.
- `docs/work-units/python/PY-01.md` and the existing `src/alienintent` skeleton, `tools/fitness/`, `tests/`.

EOS v1.0 is the ratified conformance baseline. FD-01 through FD-06 are closed. The Founder-directed
pre-Python gate reclassification found zero genuinely unresolved Founder-level decisions.

## Dependencies

This BIU depends on: PY-07. Assess readiness of the contract itself; a predecessor that is not yet
implemented is a sequencing fact, not an owner clarification, unless the contract depends on a decision
nobody has made.

## What to assess

Whether an implementation agent could execute this BIU without inventing product or architecture
decisions: is the intent bounded, is scope separated from non-goals, is acceptance observable and
mechanically verifiable, are the requirement links honest about extent, and is verification real
executable proof rather than description?

Distinguish carefully:
- **owner clarifications** — decisions only the Founder/owner can make;
- **implementation unknowns** — normal engineering research the agent may resolve itself;
- **independent decision centers** — signs the BIU should be split.

Do not treat missing lower-level design detail as an owner clarification when approved architecture
already constrains it.

## Excluded from the work

No Node source, test, configuration, runtime or bootstrap change beyond the existing critical
bootstrap-fix rule. No FactoryChecks change. No Python Sovereignty claim and no Node retirement.
No Wave 2+ capability. Implementation is not authorized yet; this is a readiness assessment only.

## Output

Return ONLY a single JSON object, no prose and no code fence, with exactly these keys:

`disposition` (one of "READY", "NEEDS_CLARIFICATION", "SPLIT_RECOMMENDED", "BLOCKED"),
`governing_intent` (string), `summary` (string), `owner_clarifications` (array of strings),
`implementation_unknowns` (array of strings), `independent_decision_centers` (array of strings),
`semantic_split_recommendation` (array of strings), `verification_assessment` (string),
`expected_rework_locality` (one of "HIGH", "MEDIUM", "LOW"), `material_risks` (array of strings),
`next_action` (string), `rationale` (string).

Do not write, create or modify any file.
