# WO-220304 FX-C4 proof packet — DRAFT, NOT PINNED

Status: `DRAFT_OPEN_OWNER_QUESTIONS`. Factory Director preparation (episode `factory-director-ce7715d351be4761b06f68090eed8483`, Founder pipeline direction `founder-pipeline-direction-20260924T112523Z`). It was drafted read-only against baseline `669e5f1`.

The Founder has ruled that WO-220304 may not move to READY until its executable proof packet is concretely pinned. This draft is **not** that pin. The owner questions at the end must be dispositioned first. After that, the pinned section is appended to `docs/work-units/wave2/WO-220304.md` and re-assessed by native Agent Ready. Nothing here authorizes release.

## Draft proof packet (FX-C4)

Status `PLANNED_NOT_EXECUTED`. Proof level `LOCAL_COMPOSED_OR_MECHANICAL`. This is a service/inspection fixture only. It authorizes no live action, no deployment supervision and no host. Files marked **PLANNED** do not exist at baseline `669e5f1` (origin/main `4b94f2d` changes no C4 input or `src/alienintent` file).

**Pinned inputs (sha256).**
- `docs/evidence/wave2-design-contracts.json` `56676dd9…f056242`. Binding sections: `#/shared_contracts/monitor_liveness` (the record, the `health_rule`, and `observation_owner` naming `R1-GAP-MONITOR-HOST`), `#/contracts/8` and `#/contracts/9` port `MonitorHealth.read(profile)`, and enforcement `056-liveness-classification`.
- `docs/evidence/wave2-dependency-dag.json` `f99c0a7a…2b1e8d` (node C4, fixture FX-C4). `docs/evidence/wave2-candidate-bius.json` `61f9324e…f677f7` (`#/bius/17`).
- `docs/evidence/wave2-execution-packets/WO-220304.packet.json` `110e3b44…c79e19` and `…allocation.json` `b8c1ba48…aca46`. Both are untracked in the director worktree, so they must be committed before admission.
- Predecessor C1 (WO-220301, Issue #78): candidate `8c810d2df815907cb3c1a383d6a645b3cddc7c6e` (source `a8077b8`), landed by `d168b6a`. Its receipt is `docs/evidence/wave2-proof-fixtures/FX-C1/execution-record.json` `da3c7951…34be55`. That file records `independent_verdict: PENDING_FRESH_BIU_VERIFIER`, so the producer must cite the Issue #78 verifier verdict receipt. C1 code and proof stay unmodified.

**Reused substrate (existing, unchanged).**
- `execution_coordination.adapters.sqlite_store.SQLiteOperationalStore`: `read_state`, `commit` (expected-version CAS), `list_states`, `StoreUnavailable`, `SchemaIncompatible`, `VersionConflict`.
- S1 `evidence_learning.adapters.local_evidence_repository.LocalEvidenceRepository`, with `domain.records.Observation`/`Header`/`canonical_bytes` and `domain.refs.Ref`.
- History-then-pointer pattern: copy it from `control_plane/adapters/attention_repository.py`, which writes immutable history first and then a `schema_version=1` pointer.

**Planned surface (PLANNED; names are implementation-local unless noted).**
- `src/alienintent/control_plane/domain/monitor_health.py`: `MonitorPolicy(interval_seconds, policy_digest)`, rejecting non-positive or non-finite values. `MonitorHealth` has exactly the `monitor_liveness` record fields. `HealthStatus` is {HEALTHY, STALE, DEGRADED, UNVERIFIED}. Pure `classify(record|None, now|None, last_observed|None, policy)`.
- `src/alienintent/control_plane/ports/monitor_health.py`: `MonitorHealthPort.read(profile) -> MonitorHealth | Unavailable` and a repository protocol.
- `src/alienintent/control_plane/application/monitor_health.py`: `start` allocates a new instance/generation, plus `tick`, `scan_started`, `scan_finished(outcome)` and `inspect`.
- `src/alienintent/control_plane/adapters/monitor_health_repository.py`: aggregate `monitor:<profile>`. History `Observation` uses method `monitor-history`.
- `src/alienintent/execution_coordination/ports/scan_progress.py`: scan-progress observation, per `observation_owner`.
- `src/alienintent/composition/control_plane_profile.py`: add `MonitorProfile(root, *, project, name, invocation, clock, policy)` over `root/"monitor.sqlite"` and `root/"monitor-evidence"`. `AttentionProfile` stays unchanged.

**Fixture inputs.**
- Root: a disposable `tmp_path` or `tempfile.TemporaryDirectory`, holding a temporary SQLite store and the S1 store only. No production profile, service, timer unit, provider or network.
- Injected clock: `Callable[[], datetime | None]`, timezone-aware UTC with microsecond resolution. It is scripted per case. `None` or a raising clock means absent/unreadable.
- Injected timer: explicit `tick()` calls. There is no thread, loop or background waiter.
- Interval: I = 60 s (the default). A non-default I = 7 s proves I is read from the policy.
- Scan outcomes: `COMPLETE`, `FAILED(error)`, `EVIDENCE_HOLD`, or `STARTED` with no finish (incomplete). They are supplied by a fake scanner. There is no `Liveness.inspect`; that is L1.
- Other inputs: a model-launch spy (count must be 0), and an episode stand-in (see unknown U2).

**Commands.** All run under `rtk proxy` with `PYTHONPATH=src`.
1. PLANNED: `python3 -B -m pytest -q tests/control_plane/test_monitor_health.py`
2. PLANNED: `python3 -B tools/evidence/fx_c4_evidence.py --output /tmp/fx-c4-<run-id> --invocation <exact-invocation>`. The output directory must be new.
3. `python3 -B -m pytest -q`, `python3 -B tools/fitness/check_architecture.py --root src/alienintent --check all`, `python3 -B -m pytest -q tests/test_architecture_fitness.py` and `node scripts/check.mjs all`. Each must exit 0.

**Expected outcomes.** Each outcome maps to a PLANNED node id `tests/control_plane/test_monitor_health.py::…`. Take `t0` as the last tick and scan completion.

| Node id | Input | Expected |
|---|---|---|
| `test_tick_age_boundary[below]` | now = t0+2I−1µs | HEALTHY |
| `test_tick_age_boundary[at]` | now = t0+2I exactly | HEALTHY (still in bound) |
| `test_tick_age_boundary[above]` | now = t0+2I+1µs | STALE |
| `test_interval_from_policy` | I=7: now t0+14 s, then t0+14 s+1µs | HEALTHY, then STALE |
| `test_scan_overdue_boundary[at\|above]` | ticks keep advancing, scan completion frozen | HEALTHY at 2I, STALE above |
| `test_quiet_work_is_not_health` | no work, ticks continue | HEALTHY; ticks persist while idle |
| `test_frozen_monitor_while_work_quiet` | ticks stop, now > t0+2I | STALE, never HEALTHY |
| `test_failed_or_incomplete_scan[failed\|evidence_hold\|started_only]` | ticks fresh | DEGRADED; `last_scan_completed_at` does not advance |
| `test_no_record` | empty store | UNVERIFIED |
| `test_clock[absent\|unreadable\|regressing]` | clock returns `None`, raises, or is earlier than the last persisted observation | UNVERIFIED |
| `test_store_unavailable[unavailable\|schema]` | `StoreUnavailable`, or `SchemaIncompatible` pointer | UNVERIFIED (`Unavailable`) |
| `test_restart_new_generation` | reopen profile, `start()` | new instance_id and generation (generation+1); prior observations readable |
| `test_episode_end_preserves_monitor` | end episode stand-in, discard services, reopen | identical `monitor:` pointer and version, and history; launch spy 0 |
| `test_independent_of_model_and_workload` | no activation or episode bound | ticks and scans persist; launch spy 0 |
| `test_concurrent_writer_cas` | two profiles, one stale version | one `VersionConflict`, no lost observation |
| `test_policy_invalid_blocks_start[missing\|zero\|nan]` | invalid policy | `start` refuses; no record written |
| `test_monitor_profile_composed` | `MonitorProfile` end to end | read-only `inspect` equals the stored record |

**Discriminating controls.** The harness copies `tools/evidence/fx_c1_evidence.py`. Each fault is applied exactly once (`application_count == 1`) to a disposable copy of `src`/`tests`, by exact needle replacement. The needles are pinned in `CONTROLS` before the first run. A control passes only if:
- the intact run exits 0;
- the faulted run exits 1 and its output contains the named assertion text;
- the restored run exits 0.

| Control | Mutation | Failing node |
|---|---|---|
| `stale_boundary` | tick-age `>` becomes `>=` | `[at]` |
| `stale_bound_removed` | STALE tick branch deleted | `[above]`, `test_frozen_monitor_while_work_quiet` |
| `scan_overdue_removed` | overdue-scan check deleted | `test_scan_overdue_boundary[above]` |
| `interval_hardcoded` | policy interval replaced by `60` | `test_interval_from_policy` |
| `degraded_removed` | failed or incomplete scan treated as COMPLETE | `test_failed_or_incomplete_scan` |
| `incomplete_advances` | `scan_started` advances completion | `[started_only]` |
| `clock_regression_accepted` | regression guard deleted | `test_clock[regressing]` |
| `absent_clock_healthy` | `None` clock falls through to HEALTHY | `test_clock[absent]` |
| `unavailable_healthy` | `Unavailable` mapped to HEALTHY | `test_store_unavailable` |
| `no_record_healthy` | empty store mapped to HEALTHY | `test_no_record` |
| `generation_reused` | restart keeps generation, or overwrites history | `test_restart_new_generation` |
| `workload_gated_tick` | tick skipped when there is no work | `test_quiet_work_is_not_health` |
| `episode_end_resets` | episode end clears the `monitor:` aggregate | `test_episode_end_preserves_monitor` |
| `cas_bypassed` | commit reads the current version, not the expected one | `test_concurrent_writer_cas` |
| `composition_disconnected` | `MonitorProfile` stops wiring the repository | `test_monitor_profile_composed` |
| `adapter_import_added` | domain imports the adapter | the architecture check exits nonzero |

**Evidence to retain.** Retain under `docs/evidence/wave2-proof-fixtures/FX-C4/`, following the FX-C1 and FX-S1 conventions:
- **FX-C4.md:** the fixture contract.
- **execution-record.json:** `record_kind: ProofFixtureExecution`, `schema_version: 1`, `fixture_id: FX-C4`, the invocation and `source_revision`. It must also record:
  - an empty `source_status`, meaning the source is clean; a dirty source is a HOLD;
  - `admission_baseline` and `input_digests`, covering the inputs above, the planned files and the harness;
  - `policy`, holding the interval and the `policy_digest`;
  - `commands[]`, each with `command`, `exit_status` and `observation_ref`;
  - `holds[]`, `model_launch_count`, `independent_verdict: PENDING_FRESH_BIU_VERIFIER` and `live_proof: NOT_ESTABLISHED`;
  - `measurements`, with tokens, cost and provider calls set to `null` plus an `UNKNOWN` reason, never 0.
- **run-report.json:** maps each acceptance clause to its node ids, plus a composed readback of the `monitor:` aggregates and S1 `monitor-history` objects.
- **proven-red.json:** for each control, `control`, `application_count`, `source_digest`, `mutation`, `command`, `assertion`, `fault_exit`, `restored_exit`, `fault_ref`, `restored_ref` and `discriminates`.
- **observations/ and digest-manifest.json:** raw objects named by sha256, written with `xb`. A collision is an error, and nothing is overwritten on repair.

Definitions, observations and the verdict stay distinct. A missing measurement or readback is a HOLD, never a PASS.

**Boundaries.**
- The host stays unassigned (`R1-GAP-MONITOR-HOST`).
- There is no C5 supervised host, restart or alert, no C6 liveness reconciliation, and no L1 or SF-REQ-056 `Liveness.inspect`/`reconcile`.
- There is no G/C boundary work, no G+I detection claim, no POSTW1-DECIDE-008A dependency, no systemd or shell waiter, no EpisodeControl implementation (C3), and no live trajectory capture (B1).
- A profile constructor is not a supervisor.

**Unknowns needing an owner decision.** None of these is answered here.
- **U1 — "trajectory seam".** The title names it, but no predicate, port or evidence in the contract, DAG node, design contracts or packet defines it. Decide whether it is title-only or define its extent. B1 owns live trajectory capture.
- **U2 — episode end.** EpisodeControl belongs to C3, which is not a dependency of C4 and has not landed. Decide whether a labelled stand-in is acceptable proof of "end episode without losing local monitor state". The stand-in would be: discard the service or activation objects and reopen from the store.
- **U3 — overdue scan reference.** "Overdue last scan completion > 2*I" can be measured from `last_scan_completed_at` or from `next_scan_due`. The packet assumes `last_scan_completed_at`.
- **U4 — precedence.** When a record is both STALE and DEGRADED, the packet assumes the written rule order (STALE before DEGRADED). This needs confirmation.
- **U5 — regression reference.** Decide what "regressing clock" is compared against: the last persisted observation time across restart, or only the in-process monotonic reading.
- **U6 — policy scope and digest.** Decide whether C4 persists only I, or the full G/I/C policy digest. G and C are 056 policy, and 056 is still unratified.
- **U7 — scan owner in C4.** `observation_owner` gives scan progress to `execution_coordination`, but the real scan is L1. Confirm that C4 records outcomes from an injected fake scanner only.
- **U8 — predecessor verdict.** The C1 independent verifier verdict is not retained in the repository. The Issue #78 receipt must be identified before admission.
