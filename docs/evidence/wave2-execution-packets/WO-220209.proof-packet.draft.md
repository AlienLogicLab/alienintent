# WO-220209 FX-U9 proof packet — DRAFT, NOT PINNED

Status: `DRAFT_OPEN_OWNER_QUESTIONS`. Factory Director preparation, drafted read-only against baseline `ffbc9933e1f25e6e33e46ef95024bb5f13e9247f` (origin/main `ffbc993`, the reviewed WO-220209 candidate consistency repair) plus the uncommitted compiled artifacts for WO-220209 in the preparation worktree (`docs/work-units/wave2/WO-220209.md`, `WO-220209.packet.json`, `WO-220209.allocation.json`).

This is preparation only. It is not implementation, not an executed proof, not a readiness assessment and not release authority. `docs/work-units/wave2/WO-220209.md` ("Dependencies") sets two gates. WO-220209 must not move to READY until its executable proof packet is concretely pinned, and this draft is **not** that pin. WO-220209 must not be released before WO-220207 (#101) is DONE; #101 is READY and not released at this baseline. The draft becomes a pin only after the Director dispositions the owner questions below, appends the pinned section to `docs/work-units/wave2/WO-220209.md`, and native Agent Ready re-assesses the unit. The fixture authorizes no live action (`D#/proof_fixtures/11/environment`).

**Contract under proof.** `docs/work-units/wave2/WO-220209.md`. Its candidate contract sha256 is `0adf1ddfc35b430c91275b54faf26c40445c45b8d80c314d294ef96261add0f0`, the sorted compact JSON of `B#/bius/11`. It was recomputed at this baseline and matched, using the convention in the repair record's "Digests" section. The work unit was compiled from the reviewed repair `docs/evidence/wave2-revisions/2026-09-25-wo-220209-candidate-consistency-repair.md`.

**Input digests (sha256, whole file).** B, D, S and C are byte-identical at `ffbc993` and in the working tree; both were computed. The contract, packet and allocation are the uncommitted working-tree files. Their digests must be re-pinned from the commit that lands them.

| Pointer | File | sha256 |
|---|---|---|
| contract | `docs/work-units/wave2/WO-220209.md` | `c14a2a627d726d788210e00a70ea27124bf5bd5e52eb062ee1b317015fcbca4a` |
| packet | `docs/evidence/wave2-execution-packets/WO-220209.packet.json` | `91b972424f1ae8338ed0c235abddf026f792bb79b706ebc9ef48c302527288bb` |
| allocation | `docs/evidence/wave2-execution-packets/WO-220209.allocation.json` | `47b3a0ff9b7857a53ac50690c8baa4591b156eb7e76fa17774424777e961d13d` |
| B | `docs/evidence/wave2-candidate-bius.json` | `5515b6ce24286dd83a40cd295f61dd74a6c55a5b1c407597c0a65db36504d1f1` |
| D | `docs/evidence/wave2-dependency-dag.json` | `be5f39f8d6d8c319dfd990ae9f52c32e84e7db6af13ef87f9d45b6cbeff20476` |
| S | `docs/evidence/wave2-specified-requirements.json` | `332757c43b8754196e45f75a8ce2ee422e19382415e17ed22cd431f348d9b800` |
| C | `docs/evidence/wave2-design-contracts.json` | `56676dd97cd09031882ee01e61af2a15071f1b92f6c914fde2169b682f056242` |
| REPAIR | `docs/evidence/wave2-revisions/2026-09-25-wo-220209-candidate-consistency-repair.md` | `59ea62bd68bd0371cb03f2c9ebb17ad23736ac774511fb99c214ebb0d30e42f6` |
| RESP | `docs/evidence/wave2-revisions/2026-09-22-respecify-015-039.md` | `ad7308bbdcb45dd303bee44a44b4c29cb0a17e783b0f67501d1e88a913495a60` |
| M | `docs/evidence/wave1-agent-ready-outcome-matrix.json` (PY-10 `coercion_finding`, gap `G3`) | `f9730f9eccf4022e314f74bdf3d3ac257c0e07d00c76d6cf87419e472fe4e145` |
| PM | `docs/evidence/wave1-readiness-assessment-provenance.json` (producer manifest) | `a8f977e0bb26bd7f0b599042ccce853ff5d76dd9e00adff033990550408a8f21` |
| T | `docs/evidence/execution-trajectories/PY-10.jsonl` | `101ffc18714a16f9096bd559fcfa7b15814b80b31dc47c84e1de9fd392cff282` |

C and S are the same revisions the pinned FX-U7 packet reads (`docs/work-units/wave2/WO-220207.md` lines 131 and 134). D and B are the post-repair revisions: `b767305f…` → `be5f39f8…` and `6c6e5300…` → `5515b6ce…` (REPAIR "Digests").

## Draft proof packet (FX-U9)

Status `PLANNED_NOT_EXECUTED`. Proof level `LOCAL_COMPOSED_OR_MECHANICAL` (`D#/proof_fixtures/11/proof_level`). Disposable local profile only. The fixture uses no live store, provider, model launch, network, host or RAI. It uses the harness shape of FX-C1 and FX-U5 (`docs/evidence/wave2-proof-fixtures/FX-C1.md`, `tools/evidence/fx_c1_evidence.py`, `tools/evidence/fx_u5_evidence.py`, `docs/evidence/wo-220205-fx-u5.md`). Anything marked **PLANNED** does not exist at `ffbc993`. Anything marked **PLANNED-U7** depends on WO-220207 interfaces. Those interfaces exist only in the pinned FX-U7 packet and are not available substrate. They are pinned only after #101 is DONE.

**Governing extent.** The governing extent is `D#/nodes/11/completion_predicate`. It equals `D#/proof_fixtures/11/scenario`, `B#/bius/11/scope/extent` and `B#/bius/11/acceptance_criteria/0`:

> "Four dispositions; error/conflicting/unknown/malformed/missing terminal MCP payloads fail regardless of exit status; retain raw bytes and predecessor attempts. Changed inputs invalidate READY; separate release gate stays required. Replay PY-10 historical bootstrap-assessor immutable BLOCKED, SPLIT_RECOMMENDED and fresh READY refs, never label as Agent Ready. At actual profile boundary unbound/unauthorized stub yields CAPABILITY_PROVENANCE_HOLD before launch. Require established producer/package provenance, not schema shape; CLARIFY routes via SF-REQ-035 then reassessment; SPLIT routes to the SplitTransaction port and U9 refuses to proceed without a materialized result set; the transaction itself is U8 (DV-7), after which U9 lints/reassesses each result."

- **Acceptance IDs:** SF-REQ-015-AC-01..05 (`D#/proof_fixtures/11/acceptance_ids`; `D#/acceptance_trace/17..21`, owner U9, fixture FX-U9).
- **Enforcement:** `015-envelope-applicability` (`D#/enforcement_obligations/8`) and `015-composed-authority` (`D#/enforcement_obligations/9`). Both are BLOCKING and both are `python_unit_integration`.
- **`D#/nodes/11/proceeds_regardless`:** "Consumer semantics, retained-envelope parity and provenance/admission refusal tests at current interfaces; Agent Ready product ownership is settled by A1."
- **Node scope:** `D#/nodes/11/scope` is empty.

**Pinned predecessors.**
- **U5 (WO-220205, #97): DONE.**
  - Accepted candidate `6518d1ef09fc99e8abccf1c6234106f860fce7e6`, merged by `036c5fc`.
  - Receipts: `docs/evidence/wo-220205-fx-u5.md`, `docs/evidence/wo-220205-fx-u5-predicate-mapping.json`, `docs/evidence/wo-220205-fx-u5/`, `docs/evidence/wo-220205-fx-u5-r2/`.
  - JC ACCEPT https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816278191; closure https://github.com/AlienLogicLab/alienintent/issues/97#issuecomment-5816356325.
  - These are cited as recorded in the FX-U7 pin (`docs/work-units/wave2/WO-220207.md` lines 123 and 146–150). This draft did not re-read the Project.
- **U7 (WO-220207, #101): READY, not released, not implemented.**
  - The FX-U7 packet is pinned in `docs/work-units/wave2/WO-220207.md` § "Pinned proof packet (FX-U7)" at `5a79622`.
  - Its accepted candidate SHA, proof receipts and verdict do not exist yet. They are an admission prerequisite, not substrate.

### What FX-U9 proves, and what it must not claim

FX-U9 proves the **readiness consumer**:
- lint before assessment;
- consumer handling of direct and known-MCP results;
- the four disposition routes up to their handoff boundaries;
- immutable attempts with applicability kept separate;
- stale invalidation;
- refusal of provenance and admission at the actual composition boundary;
- the PY-10 historical replay.

FX-U9 must **not** claim:
- **U8 split/replan.** It does not claim `SplitTransaction.prepare`/`apply` or result materialization (`C#/contracts/2/ports/3`; `D#/nodes/10`, GAP_BLOCKED). The predicate states "the transaction itself is U8 (DV-7)".
- **U10 native-producer binding.** It does not claim CLI/MCP adapter conformance to the published contract, and it does not claim positive native-producer proof (`D#/nodes/12/completion_predicate`). "Test doubles are explicitly fixture producers and cannot discharge native-producer proof" (`D#/enforcement_obligations/9/rule`).
- **SF-REQ-015-AC-06 or AC-07.** They are in `S#/candidates/4/acceptance_criteria/5,6` and have no `D#/acceptance_trace` row (REPAIR "Recorded, not repaired"). They are out of FX-U9 scope unless a DAG revision assigns them. The run report records `ac_06_07_trace_ownership: UNASSIGNED_IN_DAG`. A FX-U9 PASS is never counted toward them.
- **SF-REQ-029 retained-assessment serialization.** SF-REQ-029 owns it (`C#/contracts/4/ownership_statement`); no D node lists SF-REQ-029.
- **Release.** READY yields eligibility only. `admit_release` stays the separate SF-REQ-002 gate.
- **Anything about Agent Ready itself.** FX-U9 does not implement, import or copy Agent Ready (`C#/contracts/4/non_goals/specified/0`).

### Fixture inputs (durable state only; `tmp_path` or `--output` root, never a live profile)

1. **Store and evidence.** A real temporary `SQLiteOperationalStore` (`src/alienintent/execution_coordination/adapters/sqlite_store.py:49`) and a `LocalEvidenceRepository` (`src/alienintent/evidence_learning/adapters/local_evidence_repository.py:15`, `put` `:51`, `get` `:80`). Both are composed through `UpstreamProfile` (`src/alienintent/composition/upstream_profile.py:22`), which is the "actual profile boundary" (`C#/contracts/4/composition`: "composition/upstream_profile.py injects lint, ReadinessAssessment CLI or MCP adapter and retained-evidence consumer").
2. **Design gate (U5, existing).** `UpstreamProfile.design_readiness` (`upstream_profile.py:59`) is driven into four states:
   - CURRENT_VERIFIED;
   - STALE after a vector change;
   - held with no independent review;
   - absent.

   `DesignReadiness.admit` (`src/alienintent/context_assembly/application/design_admission_service.py:254`) is the only read.
3. **Candidate BIU documents (AC-01).**
   - One complete candidate built through `contract_from_document` (`src/alienintent/composition/sandbox_run_profile.py:97`) into `BiuContract` (`src/alienintent/execution_coordination/domain/contract.py:49`).
   - One-fault variants: missing `fixed_decisions` (decision); missing `authorized_scope`/`excluded_scope` (boundary); missing `verification_obligations`; design gate not CURRENT_VERIFIED (architecture constraint, per U-5); a declared dependency absent from the dependency snapshot, or declared edges disagreeing with the snapshot (dependency).
   - Missing required fields raise `ContractValidationError` at `contract.py:75-90`. Lint must catch that and convert it into an attributable hold that names the field.
4. **Retained native direct assessments (existing; read by path and digest, never modified).**

   | Role | Path under `docs/evidence/wave2-readiness-assessments/` | Outcome / disposition | sha256 |
   |---|---|---|---|
   | direct READY | `WO-220207.2026-09-25T001654.663618Z.assessment.json` | ASSESSED READY; `provenance.input_sha256` `0e05a120…` equals sha256 of `docs/work-units/wave2/WO-220207.md` at `5a79622` (measured) | `2862acb88dec9d240bd2c436cc2aae63ec704c0ef7663beeb5b96240948bd1ff` |
   | direct HOLD | `WO-220102.2026-09-23T041739.192319Z.assessment.json` | ASSESSED HOLD | `e8c6d3da41ee96843e93f6f1149a65874cefc7a9f88eb7be4d1f7f0af98a3a71` |
   | fresh READY after HOLD | `WO-220102.2026-09-23T053013.882036Z.assessment.json` | ASSESSED READY | `4df4717c74b957c6f4b21d527c8ec3c93471ecc64e0db56e70d6352641fcb167` |
   | provider failure | `FDH-01.2026-09-24T021848.446095Z.assessment.json` | EXECUTION_FAILURE, exit 1, `PROVIDER_OR_PRODUCT_FAILURE` | `b4f70369c8066a097b012e45a48c4a3b7f54942d8fc4c45c64bc5d236d6356d6` |

   At this baseline, `looks_native_agent_ready` (`tools/evidence/check_assessment_producer.py:92`) returns True for the `assessment` objects of the first three records (measured). This is negative-shape evidence only (`C#/contracts/4/design_decisions/2`).

   No retained native CLARIFY or SPLIT result exists, and neither does any MCP envelope. The 22 retained records are all `agent-ready-cli` and are ASSESSED READY/HOLD or EXECUTION_FAILURE (measured). See U-6.
5. **Known MCP envelopes (synthetic; labelled `SYNTHETIC_MCP_ENVELOPE_FROM_RETAINED_DIRECT`).** Each wraps the byte-identical retained `assessment` object from input 4. It uses the keys `content`/`structuredContent`/`isError` that the existing checker recognises (`check_assessment_producer.py:96-98`) and follows `C#/contracts/4/design_decisions/0` ("collect structuredContent disposition and parseable JSON text-content dispositions"). Variants:
   - structured-only;
   - text-only;
   - both agreeing;
   - structured READY with text HOLD (conflict);
   - `isError: true` carrying a READY-shaped body;
   - unknown disposition `BLOCKED`;
   - malformed text JSON;
   - no terminal result (empty content, no structuredContent);
   - two text items that disagree;
   - the disposition nested only inside the envelope, where top-level parsing sees none.

   Each variant is combined with exit status 0 and non-zero, and there is also a timeout attempt with no bytes.
6. **Fixture producers (labelled `FIXTURE_PRODUCER_NOT_NATIVE`).** These are scripted `ReadinessAssessment` doubles that return CLARIFY, SPLIT, READY or HOLD bodies and count calls. They are used only to drive routes and reassessment calls. They can never discharge native-producer proof (`D#/enforcement_obligations/9/rule`).
7. **Provenance bindings (`015-composed-authority`).** Each is supplied to the composition root:
   - unbound (no adapter configured);
   - configured executable whose distribution metadata is absent, so `product_version` is UNKNOWN;
   - wrong package identity;
   - a schema-perfect surrogate READY with no invocation custody;
   - a result carrying `provider_evidence` copied from input 4;
   - a response whose input fingerprint or attempt ID differs from its invocation;
   - an intact identified binding. U-7 defines what "intact identified" means inside FX-U9.

   The rules come from `C#/contracts/4/provenance_contract` (text and `version_source`) and `C#/contracts/4/identities`.
8. **PY-10 historical bootstrap-assessor history (AC-05; read-only; `SURROGATE_READINESS_HISTORY_NOT_AGENT_READY`).** The runner extracts these exact Git blobs with `git cat-file blob`. A digest mismatch is a HOLD before any probe runs.

   | Step | Revision : path | blob | sha256 (raw blob) | disposition (verbatim) | assessed baseline | trajectory event (T) |
   |---|---|---|---|---|---|---|
   | 1 | `c32830b931a06562fa12f273d83e55f4b7c1816c:docs/work-units/python/PY-10.assessment.json` | `3c53cd3dc751c76eb167bb142d30f61825d3259a` | `093a9d2c212593812e67edb5a7b04a36ac075970cfe970c60ade16cb3506c232` | `BLOCKED` | `e25da1f` | `wave1-terminal-PY-10-9` (T line 9) |
   | 2 | `1f16b816cb0d30681eacbb2ea178eb2cf92ddd02:…/PY-10.assessment.json` | `fe1e3efe3523f4376b2de66a650dca31fecdbfad` | `1fc95f25b6c2d928fcb325525b46c1850bb56463c2428f0ea56afc99f181ebb4` | `SPLIT_RECOMMENDED` | `85b606037e144e977c1bc1f7b0aa795d97e19d83` | `wave1-terminal-PY-10-10` (T line 10) |
   | 3 | `693fef995f03ab3eca43cb74ad5692cdcd69ffcf:…/PY-10.assessment.json` | `30ea7c451505c5f6bfeba5b6496fff2b4fff01e4` | `9561bf71baaea6d0c6caf26eee87a23ab03beb6c83be68bbc5847447cde6e694` | `READY` | `93dd8b1` | `wave1-terminal-PY-10-11` (T line 11) |

   **Measured at this baseline (preparation read, not proof):**
   - The three commits are the complete `git log --follow` history of `docs/work-units/python/PY-10.assessment.json`. The working-tree file equals blob 3.
   - The sha256 of each blob's stripped text equals `M#/coercion_finding/historical_assessments/0..2/sha256_of_stripped_text`: `0e074b50…`, `e77cfb36…` and `69f53f56…`.
   - The step 2 blob is the same prior-assessment blob that FX-U7 pins (`WO-220207.md`, fixture 5).
   - `93dd8b1` is "Merge accepted PY-09B candidate …" and is an ancestor of `693fef9`. This is the prerequisite completion that makes step 3 a fresh assessment. M `coercion_finding/reason` states: "fresh READY at 93dd8b1 after PY-09B completed".
   - Step 3's `provider_evidence.assessment_history` names the BLOCKED (`e25da1f`) and SPLIT_RECOMMENDED (`85b6060`) predecessors.
   - PM declares the file `producer: surrogate-bootstrap-assessor`, `native_agent_ready: false`, `disposition_vocabulary: alienintent-bootstrap-assessor`.
   - `looks_native_agent_ready` is False for all three blobs, because their `provider_evidence` carries runner-asserted keys.
9. **Dependency snapshot.**
   - `factory:<id>` aggregates are written through `FactoryCoordinator`'s existing encoding (`src/alienintent/execution_coordination/application/factory_coordinator.py:48`, `state` `:80`, `_is_done` `:310`, aggregate names `:367`/`:369`).
   - The declared-edge precedent is `sandbox_run_profile.py:208`, which raises "upstream dependency edges disagree with the BIU contract".
10. **Decision Inbox (CLARIFY route, SF-REQ-035; existing).**
    - `UpstreamProfile.inbox` (`upstream_profile.py:37`) is a `DecisionInbox` (`src/alienintent/control_plane/application/decision_inbox.py:21`, `register` `:31`, `list_open` `:37`, `show` `:44`, `submit` `:53`).
    - The precedent adapter is `DecisionInboxQuestions.open` / `.validate` (`src/alienintent/context_assembly/adapters/decision_resolution.py:12`/`:23`). It registers a `HumanDecisionRequired` (`src/alienintent/execution_coordination/domain/escalation.py:17`) with `("resolve","defer")` and "no default answer is assumed".
11. **Split handoff (SPLIT route).**
    - A recording fake implements the **consumer side** of the SplitTransaction port (`C#/contracts/2/ports/3`).
    - Case (a): it returns no materialized result set.
    - Case (b): it returns a fixture-supplied result set labelled `FIXTURE_MATERIALIZED_RESULT_SET_NOT_U8_PROOF`. The set holds children C1 and C2 and a retained Integration Parent O, each a complete `BiuContract`, plus an invalidation set.
    - Types that FX-U7 defines (`FrozenSplitProposal`, `ValidationReport`, `CompilationHold`) are **PLANNED-U7** (U-10).
12. **Release gate (SF-REQ-002; existing).**
    - `admit_release` is at `src/alienintent/execution_coordination/domain/release.py:57`. Its readiness check is at `:69-70` (`readiness_digest != contract.content_digest` raises "stale readiness reference").
    - `ReleaseRequest` is at `:27`, and `FactoryCoordinator._run` calls it at `factory_coordinator.py:173`.
    - A spy counts `admit_release` calls and `release:`/`factory:` writes.

### Interfaces under test

**EXISTING (verified at `ffbc993`; consumed, not modified):**
- **U5 design gate.** `DesignReadiness.admit(design_key, current_vector) -> ReadinessDecision` (`design_admission_service.py:248`/`:254`; `ReadinessDecision` at `:242`). The domain values `CurrentVerified`/`Held`/`Stale` are at `src/alienintent/context_assembly/domain/design_admission.py:348`/`:356`/`:362`.
- **Composition.** `UpstreamProfile` (`upstream_profile.py:22`) with `inbox` `:37`, `questions` `:38`, `ambiguity` `:39` and `design_readiness` `:59`.
- **Contract.** `BiuContract` (`contract.py:49`) with required-field validation at `:75-90` and `content_digest` at `:73`/`:92`. `contract_from_document` is at `sandbox_run_profile.py:97`.
- **Store.** `OperationalStore` (`src/alienintent/execution_coordination/ports/operational_store.py:58`) with `commit` `:64`, `read_state` `:67` and `list_states` `:68`. `VersionConflict` is at `:13` and `StoreUnavailable` at `:9`.
- **Evidence.** `EvidenceRepository`/`LocalEvidenceRepository`, `Observation` (`src/alienintent/evidence_learning/domain/records.py:60`) and `Ref` (`src/alienintent/evidence_learning/domain/refs.py:7`).
- **Decision Inbox and escalation values.** `DecisionInbox`, `DecisionInboxQuestions`, `HumanDecisionRequired`, `DecisionSubmission` (`escalation.py:40`) and `DecisionRecord` (`:63`).
- **Release and coordination.** `admit_release`/`ReleaseRequest` and `FactoryCoordinator`.
- **Architecture fitness.** `tools/fitness/check_architecture.py` and `tests/test_architecture_fitness.py`. The contract requires extending the latter (`C#/contracts/4/architecture_fitness`).

**PRIOR ART (programme tooling outside `src/alienintent`; not product interfaces; not imported; recorded in the run report):**
- `tools/orchestration/readiness_assessment.py` (sha256 `8753f48b…09b3`). `AgentReadyCliAdapter` is at `:55`, `_package_identity` (pip-show version and editable checkout commit) at `:62`, and `assess` at `:88`. `assess` maps non-zero exit to `PROVIDER_OR_PRODUCT_FAILURE` (`:128-129`), unparsable output to `MALFORMED_OUTPUT` (`:133-135`), and a non-native shape to `NOT_AGENT_READY_SHAPE` (`:137-141`). `C#/contracts/4/design_decisions/2`: an "interim programme adapter/provenance example, not the product implementation or proof of trustworthy provenance by itself".
- `tools/evidence/check_assessment_producer.py` (`7447d1ae…b586`). `looks_native_agent_ready` is at `:92`; its MCP branch at `:96-98` reads `structuredContent` only. `is_native_provider_evidence` is at `:75`. It "supplies negative shape evidence, not positive producer authentication" (`design_decisions/2`).
- `tools/orchestration/factory_director_inputs.py` (`8adb3aa6…8e81`). `ASSESSMENT_MARKER` is at `:49`. `has_retained_assessment` at `:313` accepts any string `disposition` from an operator-authored comment.
- `tools/live/release_admission.py` (`6770284b…fc32`). `disposition_from_record` at `:249` trusts a self-declared `producer.startswith("agent-ready")`. `NATIVE_COMMENT` is at `:269` and `disposition_from_native_comment` at `:272`.

  These two modules show the classes of failure that FX-U9 negative controls target: a self-declared producer string, and string-only disposition acceptance. FX-U9 does not change them.

**PLANNED (ports per `C#/contracts/4/ports`; location per U-4):**
- `ReadinessAdmission.lint(candidate, design_applicability, proof_plan, dependency_snapshot) -> LintReport`. A failure is a `LintHold` naming the duty and field. It is "not a fabricated Agent Ready HOLD disposition" (`C#/contracts/4/transitions/0`).
- `AssessmentConsumer.observe(raw_artifact, attempt_metadata, recognized_shape) -> SemanticAssessment | AttemptFailure` and `consume(assessment, current_input_fingerprint) -> ReadinessEligibility | Hold` (`C#/contracts/4/ports/1`).
- `ReadinessAssessment.assess(candidate_work_unit) -> ReadinessAssessment | AttemptFailure` (`C#/contracts/4/ports/2`). FX-U9 binds only fixture producers or the provenance-refusal variants.
- `execution_coordination/adapters/assessment_consumer.py`. It reads retained direct objects and known MCP envelopes and stores the raw bytes privately before normalization (`C#/contracts/4/adapters/0`).
- An immutable `AssessmentAttempt` with the fields in `C#/contracts/4/state`. Its attempt UUID is persisted before invocation (`C#/contracts/4/identities`). A separately versioned applicability pointer is kept per candidate.
- A compatible composition addition to `composition/upstream_profile.py`. It validates the configured product identity, package and schema binding before admission; if unbound or unestablished, the result is `CAPABILITY_PROVENANCE_HOLD`, never READY (`C#/contracts/4/composition`).
- **PLANNED-U7:** any consumption of the U7 `ValidationReport`/`CompilationHold` in lint (U-5), and the U7 split value types (U-10).

### Commands (all under `rtk proxy`, `PYTHONPATH=src`; PLANNED except regression)
1. PLANNED: `python3 -B -m pytest -q tests/<location per U-4>/test_readiness_consumer.py`.
2. PLANNED: `python3 -B tools/evidence/fx_u9_evidence.py --output <new-dir> --invocation <exact-invocation>`. The output directory must be new. The runner extracts and digest-checks the PY-10 blobs (input 8) and the retained records (input 4). It applies each control mutation exactly once to a disposable source copy, as `fx_u5_evidence.py` does.
3. Regression (existing): `python3 -B -m pytest -q`; `python3 -B tools/fitness/check_architecture.py --root src/alienintent --check all`; `python3 -B -m pytest -q tests/test_architecture_fitness.py`; `node scripts/check.mjs all`. Each exits 0.

### Probes and expected observables (test ids PLANNED)

Hold and failure codes form a labelled vocabulary (U-12). They are not Agent Ready dispositions and not lifecycle states.

**SF-REQ-015-AC-01.** Criterion (`S#/candidates/4/acceptance_criteria/0/criterion`): "A complete pinned BIU may proceed to assessment; fixtures missing a decision, boundary, verification obligation, architecture constraint or dependency produce an attributable lint hold before implementation." Design probe (`D#/acceptance_trace/17/source_design_probe`): "Missing lint duties yield pre-assessment hold and zero calls. 015-composed-authority covers unbound/surrogate composition with CAPABILITY_PROVENANCE_HOLD and no release."

| # | Probe | Fixture | Expected observable | Negative / proven-red control | Binds to |
|---|---|---|---|---|---|
| P01 | `test_complete_candidate_proceeds_to_assessment` | input 3 complete; design CURRENT_VERIFIED | `LintReport` pass; exactly one `ReadinessAssessment.assess` call on the fixture producer; the attempt is persisted before the call | K01 `lint_bypassed`: skip lint, so P02 sees ≥1 call | `contract.py:49`; `design_admission_service.py:254` |
| P02 | `test_missing_duty_lint_hold[decision\|boundary\|verification\|architecture\|dependency]` | input 3 variants | `LintHold` naming the duty and field (for example `fixed_decisions`), or the U5 `reason_code` verbatim for architecture; **zero** assess calls (spy); no `factory:`/`release:` write; the observation is `readiness.lint_held` | K02 `lint_duty_dropped[<duty>]`: skip one duty, so that parameter fails. K03 `contract_error_swallowed`: continue past `ContractValidationError` | `contract.py:75-90`; `sandbox_run_profile.py:208`; `factory_coordinator.py:80` |
| P03 | `test_lint_hold_is_not_agent_ready_hold` | any P02 variant | The persisted record carries no `disposition` field, and its type is `LintHold` (`C#/contracts/4/transitions/0`) | K04 `lint_hold_as_disposition`: emit `disposition: HOLD` | — |
| P04 | covered by P20–P23 (`015-composed-authority`) | — | — | — | — |

**SF-REQ-015-AC-02.** Criterion (`S#/candidates/4/acceptance_criteria/1/criterion`; identical to `D#/acceptance_trace/18/source_design_probe` up to its final sentence): READY → SF-REQ-002 eligibility only; CLARIFY → SF-REQ-035 Decision Inbox, then reassess; SPLIT → authorized SF-REQ-013 Split Transaction, invalidate stale applicability, lint and reassess each materialized result including the retained Integration Parent; HOLD → satisfy the prerequisite, then reassess. The design probe adds: "Probe all four routes, stale applicability and reassessment of every split result; no generic planning hold substitutes for the handoff."

| # | Probe | Fixture | Expected observable | Negative / proven-red control | Binds to |
|---|---|---|---|---|---|
| P05 | `test_ready_yields_eligibility_only` | input 4 direct READY replayed as a fixture-producer body with applicable fingerprint | `ReadinessEligibility`; **zero** `admit_release` calls and no `release:`/`factory:` write by U9; a `ReleaseRequest` built from the eligibility still passes only through `admit_release` (`release.py:57`) | K05 `ready_releases`: call `admit_release` or write `release:` on READY | `release.py:27`, `:57`, `:69-70` |
| P06 | `test_clarify_routes_to_decision_inbox_then_reassess` | fixture producer CLARIFY with two `owner_clarifications` entries | Each entry is registered verbatim as one `HumanDecisionRequired` (`list_open` shows 2) with options `("resolve","defer")` and no default answer; no further assess call before an attributable `DecisionRecord` (U-9); after `submit` by the configured actor, exactly one fresh assessment with a new attempt UUID and a predecessor link to the CLARIFY attempt; the CLARIFY attempt bytes are unchanged | K06 `clarify_auto_reassess`: reassess without a decision. K07 `clarify_generic_hold`: emit a generic planning hold instead of an inbox entry | `decision_inbox.py:31`/`:53`; `decision_resolution.py:12`/`:23` |
| P07 | `test_split_without_result_set_refuses` | fixture producer SPLIT; input 11 case (a) | One handoff to the SplitTransaction port carrying the raw assessment (`C#/contracts/2/ports/3`: "Recommendation includes raw Readiness Assessment"); U9 returns `SPLIT_RESULT_SET_NOT_MATERIALIZED`; zero lint/assess calls on any child; no graph or lifecycle write by U9 | K08 `split_proceeds_without_set`: continue with the original unit. K09 `split_generic_hold`: generic planning hold with no handoff recorded | planned port; `C#/contracts/2/transitions/4` |
| P08 | `test_split_result_set_linted_and_reassessed_each` | input 11 case (b) | Exactly C1, C2 and O are each linted and then assessed (3 assess calls); the prior applicability of O is marked inapplicable (stale), and its prior assessment bytes are unchanged; each new attempt links the SPLIT attempt as predecessor | K10 `integration_parent_skipped`: skip O. K11 `stale_applicability_kept`: keep O's prior pointer current | planned; `C#/contracts/2/transitions/4` |
| P09 | `test_hold_requires_prerequisite_then_reassess` | input 4 direct HOLD (`WO-220102…041739`), then the prerequisite is marked satisfied | Before: `Hold`, zero assess calls on re-consume; after: one fresh assessment linked to the HOLD attempt | K12 `hold_reassess_loop`: reassess while the prerequisite is still open | — |
| P10 | `test_no_non_ready_authorizes_implementation[clarify\|split\|hold\|attempt_failure\|lint_hold]` | the above | No `ReadinessEligibility` and no `factory:` transition for any non-READY outcome | K13 `non_ready_eligible`: treat any disposition as eligible | `factory_coordinator.py:48` |

**SF-REQ-015-AC-03.** Criterion (`S#/candidates/4/acceptance_criteria/2/criterion`): "After a material contract, baseline, governing decision or prerequisite change, a fresh assessment links the prior immutable assessment. Resolving a blocker cannot rewrite the old verdict to READY." Design probe (`D#/acceptance_trace/19`): "Input fingerprint change appends new assessment with immutable predecessor, old READY inapplicable."

| # | Probe | Fixture | Expected observable | Negative / proven-red control | Binds to |
|---|---|---|---|---|---|
| P11 | `test_fingerprint_change_invalidates_ready[contract\|baseline\|decision\|prerequisite\|design]` | a READY attempt, then one fingerprint component changed (`C#/contracts/4/identities`) | `consume` returns `Hold(STALE_ASSESSMENT)`; the old READY is reported inapplicable; the old attempt object digest is unchanged | K14 `fingerprint_component_dropped[<c>]`: omit one component from the hash | `contract.py:92` (`content_digest`) |
| P12 | `test_fresh_assessment_links_predecessor` | P11, then reassess | A new attempt with a new UUID and `predecessor` = old ref; history returns both; the pointer moves by CAS (`OperationalStore.commit` expected-version) | K15 `overwrite_prior_outcome`: update the old attempt in place (the `015-envelope-applicability` red obligation) | `operational_store.py:64`; `local_evidence_repository.py:51` |
| P13 | `test_resolved_blocker_cannot_rewrite_verdict` | the `WO-220102` HOLD → READY retained pair (input 4) replayed as two attempts | The HOLD attempt bytes and disposition are unchanged after READY; READY is a distinct attempt | K15 (as above) | — |
| P14 | `test_duplicate_and_conflicting_response` | the same attempt UUID with identical bytes, then with different bytes | Identical: no-op. Different: conflict hold (`C#/contracts/4/concurrency`) | K16 `last_write_wins` | `operational_store.py:13` |

**SF-REQ-015-AC-04.** Criterion (`S#/candidates/4/acceptance_criteria/3/criterion`): "A direct readiness object and a known MCP envelope carrying the same disposition yield the same semantic handling while preserving raw evidence. Conflicting text/structured values, unknown disposition, missing terminal result, timeout or provider failure produce an attempt failure and hold, never synthesized readiness." Design probe (`D#/acceptance_trace/20`): "Known direct/MCP parity; conflict, timeout, error, unknown, missing terminal all failure/hold."

| # | Probe | Fixture | Expected observable | Negative / proven-red control | Binds to |
|---|---|---|---|---|---|
| P15 | `test_direct_mcp_parity[ready\|hold][structured\|text\|both]` | input 4 direct objects and input 5 envelopes | Identical `SemanticAssessment` (disposition, normalized fields) for direct and envelope; raw bytes stored byte-for-byte before normalization, and retrievable by digest | K17 `top_level_only_parsing`: read `disposition` at top level only, so the envelope variants fail (the `015-envelope-applicability` red obligation) | `check_assessment_producer.py:96-98` (shape keys only) |
| P16 | `test_mcp_failure_cases[is_error\|conflict_struct_text\|conflict_two_texts\|unknown_disposition\|malformed_text\|no_terminal]` | input 5 variants × exit 0 and non-zero | `AttemptFailure` with no disposition, then `Hold`; raw envelope retained | K18 `trust_exit_zero`: treat exit 0 as success when there is no terminal value (red obligation). K19 `first_value_wins`: take the first supplied value without checking agreement | `C#/contracts/4/design_decisions/0` |
| P17 | `test_timeout_and_provider_failure_hold` | a timeout attempt (no bytes); input 4 `FDH-01` failure | `AttemptFailure(TIMEOUT)` / `AttemptFailure(PROVIDER_FAILURE)`; never READY; the predecessor attempt is retained | K20 `timeout_as_ready` | `readiness_assessment.py:101-104`, `:128-129` (prior art for classes) |
| P18 | `test_zero_exit_without_semantic_result_fails` | exit 0, empty stdout | `AttemptFailure` (`C#/contracts/4/failure_modes/3`) | K18 | — |

**SF-REQ-015-AC-05.** Criterion (`S#/candidates/4/acceptance_criteria/4/criterion` = `D#/acceptance_trace/21/source_design_probe`): "Replaying the retained PY-10 bootstrap-assessor history preserves verbatim BLOCKED → SPLIT_RECOMMENDED → READY, with READY a distinct fresh assessment after prerequisite completion and all prior revision references retrievable. These are historical Surrogate Readiness Assessments, not an Agent Ready sequence; retention does not coerce or relabel historical verdicts."

| # | Probe | Fixture | Expected observable | Negative / proven-red control | Binds to |
|---|---|---|---|---|---|
| P19 | `test_py10_history_replay` | input 8 (three blobs by revision) | Three historical records in commit order, with disposition strings byte-equal to `BLOCKED`, `SPLIT_RECOMMENDED` and `READY`; each labelled `SurrogateReadinessAssessment` / producer `surrogate-bootstrap-assessor` (PM); the READY record is distinct (own revision and blob) and its assessed baseline `93dd8b1` descends from the PY-09B acceptance merge; all three revision refs retrievable (`git cat-file` digests match); `consume` of any of them as a current Agent Ready assessment returns `AttemptFailure`/`Hold` (`BLOCKED`/`SPLIT_RECOMMENDED` are not dispositions; the READY lacks native provenance); no historical byte changed | K21 `history_relabelled`: map `BLOCKED`→`HOLD` or `SPLIT_RECOMMENDED`→`SPLIT`. K22 `surrogate_ready_admitted`: accept the historical READY as Agent Ready READY. K23 `history_digest_unchecked`: skip the blob digest check, so the corrupted-blob run must exit 2 (HOLD), not PASS | M `coercion_finding`; PM entry `docs/work-units/python/PY-10.assessment.json`; T lines 9–11 |

**Enforcement `015-envelope-applicability`** (`D#/enforcement_obligations/8`, BLOCKING). Rule: "Equivalent retained direct and known MCP shapes have identical consumer handling; all non-READY/failure/stale cases hold." Proven-red: "Use top-level-only parsing, trust exit zero or overwrite prior outcome; corresponding fixture must fail." Probes P15–P18 and P11–P13 cover it. The required red controls are **K17** (top-level-only parsing; fails P15 envelope variants), **K18** (trust exit zero; fails P16/P18) and **K15** (overwrite prior outcome; fails P12/P13).

**Enforcement `015-composed-authority`** (`D#/enforcement_obligations/9`, BLOCKING, promotion LRN-002 "unexecuted-composition-path", `docs/evidence/wave1-learning-ledger.md` § LRN-002). Rule: "Reuse LRN-002 at actual readiness construction/admission with local transport and real temporary SQLite: unbound or unestablished product/package provenance returns CAPABILITY_PROVENANCE_HOLD; a supported product binding reaches the consumer, preserving raw bytes and complete provenance. Test doubles are explicitly fixture producers and cannot discharge native-producer proof."

| # | Probe | Fixture | Expected observable | Negative / proven-red control |
|---|---|---|---|---|
| P20 | `test_unbound_or_unestablished_provenance_holds_before_launch[unbound\|version_unknown\|wrong_package]` | `UpstreamProfile` constructed with a real temp SQLite and input 7 variants | `CAPABILITY_PROVENANCE_HOLD` at construction or admission; launch spy 0; no attempt disposition; no release | K24 `provenance_check_bypassed`: skip binding validation, so the refusal assertion fails ("Bypass provenance checking independently") |
| P21 | `test_surrogate_and_copied_evidence_refused[schema_perfect_surrogate\|copied_provider_evidence\|disconnected_response]` | input 7 variants | Native admission refused (`CAPABILITY_PROVENANCE_HOLD` or `AttemptFailure(PROVENANCE)`), even though `looks_native_agent_ready` is True for the body | K25 `shape_trusted`: admit on `looks_native_agent_ready`. K26 `copied_evidence_trusted`: treat a `provider_evidence` match as producer proof. K27 `correlation_unchecked`: skip invocation/attempt/fingerprint correlation |
| P22 | `test_intact_binding_reaches_consumer_without_release_bypass` | input 7 intact binding (per U-7) | Raw bytes and the complete `provenance_contract` field set are retained as distinct immutable evidence; `product_version` and `version_source` are recorded; `contract_version` is literally "package-release-bound; no result-level identifier (agent-ready#1)"; model is `UNKNOWN`; zero `admit_release` calls | K05 (release bypass) |
| P23 | `test_composition_path_executed` | the profile's own composition root, not a directly constructed consumer | The consumer object reached by P20–P22 is the one `UpstreamProfile` composes (LRN-002) | K28 `composition_disconnected`: the profile composes no consumer, so P22 fails |
| P24 | architecture check (command 3) | — | Exit 0 intact; nonzero under K29 | K29 `adapter_import_added` (domain imports adapter); K30 `agent_ready_private_import` (fixture module imports `agent_ready`; `C#/contracts/4/architecture_fitness`) |

**Clause map (`D#/nodes/11/completion_predicate`):**
- four dispositions: P05–P10;
- MCP failure regardless of exit: P16, P18;
- raw bytes and predecessor attempts: P12, P15, P17;
- changed inputs invalidate READY: P11;
- separate release gate: P05, P22;
- PY-10 replay, never Agent Ready: P19;
- CAPABILITY_PROVENANCE_HOLD before launch: P20;
- provenance, not schema shape: P21;
- CLARIFY via SF-REQ-035: P06;
- SPLIT port and refusal without a result set: P07;
- lint and reassessment of each result: P08.

### Discriminating controls

Each control runs intact (exit 0), then with the fault (exit 1 at the named assertion), then restored (exit 0), with `application_count` 1. The controls are K01–K30 above. A non-discriminating mutation, an `application_count` other than 1, or a missing observation is a HOLD, not a PASS. The digest-mismatch HOLD for the input 8 blobs and the input 4 records is itself asserted once, by corrupting an extracted blob, and must exit 2.

### Evidence to retain (`docs/evidence/wave2-proof-fixtures/FX-U9/`, FX-C1 layout)
- **`execution-record.json`.** Records:
  - `record_kind: ProofFixtureExecution`, `schema_version: 1`, `fixture_id: FX-U9`;
  - `invocation`, `source_revision`, `source_status` (must be `""`) and `admission_baseline`;
  - `input_digests`: all rows of the table above plus the input 4 records, the input 8 blob digests, and the test, runner and source files;
  - `commands[{id, command, exit_status, observation_ref}]`;
  - `labels`: `SYNTHETIC_MCP_ENVELOPE_FROM_RETAINED_DIRECT`, `FIXTURE_PRODUCER_NOT_NATIVE`, `FIXTURE_MATERIALIZED_RESULT_SET_NOT_U8_PROOF`, `SURROGATE_READINESS_HISTORY_NOT_AGENT_READY`, `CONTRACT4_NOT_INDEPENDENTLY_DESIGN_VERIFIED`;
  - `residuals`: `NATIVE_PRODUCER_POSITIVE_PROOF_U10_EXTENT`, `SPLIT_TRANSACTION_U8_EXTENT`, `AC_06_07_TRACE_OWNERSHIP_UNASSIGNED_IN_DAG`, `SF_REQ_029_SERIALIZATION_NOT_CLAIMED`;
  - `independent_verdict: PENDING_FRESH_BIU_VERIFIER` and `live_proof: NOT_ESTABLISHED`;
  - model-launch and provider-call counts, which must be 0;
  - tokens and cost as `null` with an `UNKNOWN` reason, never 0.
- **`run-report.json`.** Maps AC-01..05, both enforcement IDs and each predicate clause to their probes, with the observed codes and refs. It records `ac_06_07_trace_ownership: UNASSIGNED_IN_DAG` and "READY semantics counted separately from released work" (`C#/contracts/4/observability`).
- **`proven-red.json`**, **`fixture-inputs/`** (extracted blobs named by blob ID), **`observations/<sha256>`** and **`digest-manifest.json`**. All are immutable. Collisions are refused, and prior proof is preserved on repair.

### Boundaries and non-goals
The following are out of scope:
- U8 split/replan transaction and materialization;
- U10 native CLI/MCP binding and published-contract conformance;
- AC-06/07 ownership;
- SF-REQ-029 serialization;
- Agent Ready implementation, rubric or private imports;
- any live Agent Ready, provider, model, network or GitHub call;
- Issue/Project writes and lifecycle transitions;
- release;
- modification of historical PY-10 records, retained Wave 2 assessments or the producer manifest;
- changes to U5 behaviour, `BiuContract`, `DecisionInbox`, `admit_release` or the programme tools cited as prior art.

A local PASS is not operational acceptance. The historical assessment `POSTW1-READY-013:WO-220209:1` (BLOCKED, baseline `01af974`; `docs/evidence/wave2-agent-ready-assessments.json` line 936) remains history for its own baseline and does not apply to this candidate (REPAIR "Pinned consumers").

### Owner questions

Dispositions:
- **DECIDED:** answered by the cited source.
- **DELEGATED:** an engineering choice the Director may decide.
- **FOUNDER_RESERVED:** a genuine product, architecture, security, budget or live-operation matter.
- **PINNED-AFTER-#101-DONE:** depends on unimplemented WO-220207 interfaces.

| Q | Question and candidate answer | Source | Disposition |
|---|---|---|---|
| U-1 | **Governing extent.** The stale candidate (OWNER_ASSIGNMENT_HOLD) or the revised node? The revised node governs: `B#/bius/11/scope/extent` = `D#/nodes/11/completion_predicate` = `D#/proof_fixtures/11/scenario`. | REPAIR "Changes"; independent review ACCEPT_WITH_NOTES; landed `ffbc993`; RESP ("D U9 now demands CAPABILITY_PROVENANCE_HOLD") | **DECIDED** |
| U-2 | **SF-REQ-015-AC-06/07.** They are out of FX-U9 scope, and no FX-U9 result counts toward them. Trace ownership is a DAG revision "before U10 or A is scheduled", not a pin prerequisite. | REPAIR "Recorded, not repaired" bullet 1; `D#/proof_fixtures/11/acceptance_ids` | **DECIDED** (assignment itself: Program Director DAG revision; not Founder) |
| U-3 | **Contract 4 is `DESIGN_CANDIDATE; independent Phase 10 verification outstanding`.** Does FX-U9 wait for it? Candidate: no. `D#/nodes/11/completion_status` is `PROCEEDS_REGARDLESS`, and `/proceeds_regardless` names exactly these tests "at current interfaces". Label `CONTRACT4_NOT_INDEPENDENTLY_DESIGN_VERIFIED`. The DV hold stays preserved (`D#/nodes/11/revision_2026_09_22/preserved_holds`). | FX-U7 N-1 precedent (`WO-220207.md` line 116) | **DECIDED** by DAG node text; Director to confirm |
| U-4 | **Module location and import direction.** `C#/contracts/4/bounded_context` is `execution_coordination` (legacy location), and `ports/0` has lint consume a `context_assembly.domain` value. At `ffbc993`, **no** `execution_coordination` → `context_assembly` import exists (AST scan of `src/alienintent`). Existing pairs from `context_assembly` are `execution_coordination` (domain/ports), `evidence_learning` (domain/ports) and `control_plane.application` (via `decision_resolution.py:4`). Candidate: lint and CLARIFY/SPLIT routing in `context_assembly` (Requirements / Planning, `C#/contracts/4/ownership_statement`); consumer, port and `adapters/assessment_consumer.py` in `execution_coordination` with no `context_assembly` import; wiring only in `composition/upstream_profile.py`. **Binding condition (FX-U7 D-1):** no new cross-group module pair. Any new pair is a stop and returns the directional question under `R2-GAP-051-EDGE-AUTHORITY`. | `C#/contracts/4/bounded_context`, `/domain_owner`, `/ports/0`, `/adapters/0`, `/composition` ("No new application import edges are authorized"); `C#/shared_contracts/context_dependency_direction` (DESCRIPTIVE_ONLY); `WO-220207.md` line 122 | **DELEGATED** with binding condition; becomes FOUNDER_RESERVED only if a new pair is proposed |
| U-5 | **Lint duty sources (AC-01).** Candidate mapping:<br>• decision / boundary / verification: required `BiuContract` fields (`contract.py:75-90`), caught and named;<br>• architecture constraint: U5 `DesignReadiness.admit` must be `CURRENT_VERIFIED`, otherwise the U5 reason code verbatim;<br>• dependency: every declared dependency resolves in the snapshot and declared edges agree (`sandbox_run_profile.py:208` precedent).<br>Whether lint also consumes the U7 `ValidationReport`/`CompilationHold` for the candidate (`C#/contracts/2/ports/2`: "SF-REQ-015 alone consumes it for lint/assessment"; U9 requires `compilation_validation_hold`) depends on U7's landed signature. | `C#/contracts/4/ports/0`; `D#/nodes/11/proof_requires_capabilities`; `D#/nodes/11/depends_on` | **DELEGATED** (`LINT_DUTY_SOURCES_v1`); U7 part **PINNED-AFTER-#101-DONE** |
| U-6 | **Fixture sources for dispositions and envelopes.** No native CLARIFY/SPLIT result and no MCP envelope is retained (measured: 22 records, all CLI READY/HOLD/failure). Candidate: use the retained native direct records where they exist (input 4); build MCP envelopes by wrapping those bytes (input 5); drive CLARIFY/SPLIT with labelled fixture producers (input 6). Exact published envelope conformance is AC-06/U10 (`C#/contracts/4/adapters_note`: "the conformance fixture pins them"). | `C#/contracts/4/adapters/0` ("retained direct objects and known MCP envelopes for consumer tests"); `/design_decisions/0` | **DELEGATED** (`FX_U9_FIXTURE_SOURCES_v1`) |
| U-7 | **What "a supported product binding reaches the consumer" means inside FX-U9.** The fixture authorizes no live action. A real Agent Ready invocation launches a provider (`C#/contracts/4/adapters/1,2`: provider codex/claude), and test doubles "cannot discharge native-producer proof". Candidate: FX-U9's intact path is the composed binding validated against provenance **values**: a configured executable path; distribution metadata of a disposable local fixture package with the product name, labelled; a correlated attempt UUID and input fingerprint; and the retained `WO-220207` record's provenance pattern (input 4, `input_sha256` matched to `WO-220207.md` at `5a79622`). It proves "reaches consumer, raw bytes plus complete provenance, no release bypass" mechanically. Positive native-producer proof is residual `NATIVE_PRODUCER_POSITIVE_PROOF_U10_EXTENT` (`D#/nodes/12/completion_predicate`). Running the real Agent Ready inside FX-U9 would be a provider/model call outside this fixture's authority and budget. | `D#/enforcement_obligations/9`; `D#/proof_fixtures/11/environment`; `D#/nodes/11/proceeds_regardless`; `D#/nodes/12` | **DELEGATED** (scoping, `INTACT_BINDING_WITHOUT_LIVE_INVOCATION_v1`). The alternative (live invocation in FX-U9) would be FOUNDER_RESERVED/separately authorized and is not proposed. |
| U-8 | **"Established" provenance rule.** `product_version` comes from installed-distribution metadata resolved from the configured executable or server path; UNKNOWN blocks admission. `contract_version` is the literal string. `provider_evidence` is optional, retained unmodified and never authenticates. Model is UNKNOWN, never inferred. An invocation-to-result correlation (attempt UUID persisted before invocation, plus input fingerprint) is required. | `C#/contracts/4/provenance_contract` (text, `version_source`), `/identities`, `/security`, `/design_decisions/2` | **DECIDED** |
| U-9 | **CLARIFY route mechanics.** Candidate: each entry of the assessment's `owner_clarifications` (a native contract field, `check_assessment_producer.py:49-53`) is registered verbatim as one `HumanDecisionRequired` through the existing `QuestionChannel` pattern (`decision_resolution.py:12-21`) with `("resolve","defer")` and no default answer. `implementation_unknowns` are not routed. U9 makes no materiality judgment of its own; Agent Ready owns assessment semantics. Reassessment follows only an attributable `DecisionRecord` accepted by `validate` (`:23-37`). | `C#/contracts/4/transitions/1`; `/ownership_statement`; SF-REQ-035 (`docs/decisions/alienintent-software-factory-plan.md` § SF-REQ-035) | **DELEGATED** (`CLARIFY_ROUTE_v1`) |
| U-10 | **SPLIT handoff shape.** The SplitTransaction port is U8's (`C#/contracts/2/ports/3`; `D#/nodes/10` GAP_BLOCKED). Candidate: U9 owns only a consumer-side port. It hands off the raw assessment plus recommended boundaries. A missing `SplitCandidateSet` gives `SPLIT_RESULT_SET_NOT_MATERIALIZED`. A supplied set is linted and reassessed per child and per Integration Parent, and stale applicability is invalidated (`C#/contracts/2/transitions/4`). Any reuse of U7 value types (`FrozenSplitProposal`, `ValidationReport`) waits for their landed signatures. | `D#/nodes/11/completion_predicate` (DV-7); `2026-09-22-dv-005-repairs.md` line 13 | **DELEGATED** (`SPLIT_HANDOFF_CONSUMER_PORT_v1`); U7 types **PINNED-AFTER-#101-DONE** |
| U-11 | **Persistence and applicability pointer.** Raw bytes and attempts are immutable `EvidenceRepository` records. The applicability pointer is a profile-scoped `OperationalStore` aggregate with expected-version CAS. The aggregate name is implementation-local (candidate `readiness:<biu>`). SF-REQ-029 serialization is not claimed. | `C#/contracts/4/persistence`, `/state`, `/concurrency`, `/implementation_local_freedom` | **DECIDED** (store/evidence); naming **DELEGATED** |
| U-12 | **Hold/failure vocabulary.** The codes are `LINT_HOLD`, `ATTEMPT_FAILURE(<class>)`, `CAPABILITY_PROVENANCE_HOLD` (source-named), `STALE_ASSESSMENT`, `SPLIT_RESULT_SET_NOT_MATERIALIZED` and `CLARIFY_PENDING_DECISION`. None is an Agent Ready disposition. | `C#/contracts/4/ubiquitous_language/Disposition`, `/transitions/0,2` | **DELEGATED** (`READINESS_CONSUMER_HOLD_CODES_v1`) |
| U-13 | **PY-10 replay expected observable.** The three revisions, blobs and digests in input 8 were located in the repository, not invented. The raw-blob sha256 is pinned, and the stripped-text digest is cross-checked against M. The replay classifies the records as Surrogate Readiness history, and consuming them as current Agent Ready results fails. | M `coercion_finding`; PM; T lines 9–11; `C#/contracts/4/recovery`, `/evidence` | **DECIDED** (sources located); Director to confirm the refusal observable in P19 |
| U-14 | **Evidence path and harness.** `docs/evidence/wave2-proof-fixtures/FX-U9/`, FX-C1 layout, plus `fixture-inputs/`. | `D#/proof_fixtures/11/evidence_contract` (content, not path) | **DELEGATED** (`FX_C1_EVIDENCE_LAYOUT`) |
| U-15 | **Release-separation observable.** Zero `admit_release` calls and no `release:`/`factory:` writes by U9. `admit_release`'s own readiness-digest check (`release.py:69-70`) is unchanged. | `C#/contracts/4/transitions/1`, `/operator_surface` ("Release remains existing control-plane authority") | **DELEGATED** (`NO_RELEASE_OBSERVABLE_v1`) |
| Admission | **Predecessors.** U5 is DONE (receipts cited above; Project status to be re-read at pin). U7 (#101) is READY and not implemented. Its accepted candidate SHA, FX-U7 receipts and JC verdict must be pinned, and #101 DONE, before WO-220209 is released. | `WO-220209.md` "Dependencies" | **OPEN** (blocker by rule, not a Founder question) |

**Outcome.**
- DECIDED (or DECIDED pending Director confirmation): U-1, U-2, U-3, U-8, U-11 (store/evidence part) and U-13.
- DELEGATED with labels: U-4 (binding condition), U-5, U-6, U-7, U-9, U-10, U-11 (naming), U-12, U-14 and U-15.
- PINNED-AFTER-#101-DONE: the U7-dependent parts of U-5 and U-10.
- FOUNDER_RESERVED: none unconditionally. U-4 becomes Founder-reserved only if an implementation needs a new cross-group import pair (directional authority, `R2-GAP-051-EDGE-AUTHORITY`). U-7's rejected alternative, a live Agent Ready invocation inside FX-U9, would need separate live/budget authorization.
- Admission blocker: WO-220207 (#101) must reach DONE before release.

Before pinning, the Director should explicitly confirm U-3, U-4, U-7 and U-13, because each fixes an expected observable or an admission reading.
