# WO-220303 FX-C3 proof packet — DRAFT, SUPERSEDED BY PIN

Status: `DRAFT_SUPERSEDED_BY_PIN`. Superseded by Factory Director episode `factory-director-7a0b34a1ef72490b8f181f9c8bc8da09`, which finalized U-3 and U-4 against the now-landed WO-220302 (#95) and WO-220205 (#97), and appended the pinned packet to `docs/work-units/wave2/WO-220303.md` § "Pinned proof packet (FX-C3)" at baseline `555b76e`. This file is retained unchanged below as the reviewed draft that pin was built from. Factory Director preparation, drafted read-only against baseline `7be12188edf632710fe842d95fc70b444e2ed279` (origin/main `7be1218`).

The Founder has ruled that WO-220303 may not move to READY until its executable proof packet is concretely pinned. This draft is **not** that pin. `docs/work-units/wave2/WO-220303.md` adds a second gate: WO-220303 may not move to READY until WO-220302 (#95) and WO-220205 (#97) are DONE. The owner questions at the end must be dispositioned first. After that, the pinned section is appended to `docs/work-units/wave2/WO-220303.md` and re-assessed by native Agent Ready. Nothing here authorizes release.

## Draft proof packet (FX-C3)

Status `PLANNED_NOT_EXECUTED`. Proof level `LOCAL_COMPOSED_OR_MECHANICAL`. Disposable local profile only. There is no live store, provider, model launch, network, host or RAI. The harness shape is the one FX-C1 and FX-S2 use (`tools/evidence/fx_c1_evidence.py`, `docs/evidence/wave2-proof-fixtures/FX-S2.md`). Anything marked **PLANNED** does not exist at baseline `7be1218`.

**Pinned inputs (sha256).**
- `docs/evidence/wave2-design-contracts.json` `56676dd97cd09031882ee01e61af2a15071f1b92f6c914fde2169b682f056242`. Binding sections:
  - `#/contracts/8`: ports `EpisodeControl`, `ContextUsageObservation`, `ContradictionObservation`; `state`, `transitions`, `identities`, `persistence`, `concurrency`, `failure_modes`, `recovery`, `evidence`, `composition`, `design_decisions`, and enforcement `053-tenure-fence`.
  - `#/shared_contracts/atomic_fencing_extension`, `operational_storage`, `failure_protocol` and `context_dependency_direction`.
- `docs/evidence/wave2-dependency-dag.json` `f99c0a7a392f8dd1f7889bb99d926f0a5a9de0948cb09845686f9d7e6f2b1e8d`. This covers node C3 (`/nodes/16`, `proceeds_regardless` "Local episode/fencing/authorized judgment tests"), fixture FX-C3 (`/proof_fixtures/16`) and the `053-tenure-fence` obligation.
- `docs/evidence/wave2-candidate-bius.json` `61f9324e7d1b7f0937bbc1aff7cbaba77c50192204e109cad5d78edb04f677f7` (`#/bius/16`). The candidate contract is `a1d3d62d…b9adc6`.
- `docs/evidence/wave2-execution-packets/WO-220303.packet.json`, `WO-220303.allocation.json` and `docs/work-units/wave2/WO-220303.md` land in the same commit as this draft. Their digests are pinned from that commit when the packet is pinned.
- Predecessor S2 (WO-220103, #74): accepted candidate `8e2547980ca1d95849460b9abf0c08857a6671b9`, landed by `901d174`.
  - Receipts: `docs/evidence/wave2-proof-fixtures/FX-S2/` (execution-record `c941168b…2c9d`, fixture-plan `24cbaceb…a502`).
  - Independent verdict: `issues/74#issuecomment-5791867997`. Closure: `#issuecomment-5791914826`.
  - The in-repo record still says `independent_verdict: PENDING`, so the Issue receipts are authoritative.
- Predecessor C1 (WO-220301, #78; `durable_attention` is in `needs_capabilities`): candidate `8c810d2d…a6c6e`, landed by `d168b6a`. Verdict and closure receipts are as cited in the WO-220302 pin.

**Admission prerequisites (not landed; do not assume shapes beyond the pinned contract).**
- **C2 (WO-220302, #95, READY, not implemented).** Its pinned packet names `ContextAssembler.reconstruct(manifest_ref) -> ReconstructedContext | ContextHold`, the `context:manifest:<id>` pointer and `LABELLED_DERIVATION_RULE_v1`. Those are PLANNED until an independently verified candidate lands. FX-C3 consumes only the landed signature. C2's pin defers replacement-under-episode equivalence to C3 (C2 U-3 disposition), so C3 must re-prove it.
- **U5 (WO-220205, #97, IMPLEMENT).** `design_admission_mechanics` and `ARCHITECTURE_AUTHORITY_HOLD` are PLANNED. How C3 consumes them is open (U-9).
- S2/C1 source and proof stay unmodified. C3 routes every authority-bearing episode command through `commit_guarded`/`claim_guarded` (`#/shared_contracts/atomic_fencing_extension`, "mandatory path for episode/liveness effects").

**Reused substrate (existing, unchanged).**
- `execution_coordination/ports/fenced_store.py`: `GuardVector(versions, authority, epoch, invocation)` and `FencedOperationalStore`.
- `SQLiteOperationalStore._validate_vector`. It requires an authority aggregate with `schema_version 1`, `active`, `epoch`, `invocation` and a finite `expires_at`, read against an injected clock. It raises `VersionConflict` or `ReservationRejected("inactive or mismatched epoch/authority" | "authority expired")`.
- `StaleFence`, `StoreUnavailable` and `SchemaIncompatible`.
- `execution_coordination/domain/lifecycle.py` `transition` (`AuthorityBlocked`), `composition/fenced_profile.py`, and the S1 `LocalEvidenceRepository`.

**Planned surface (PLANNED; module splits are implementation-local).**
- `control_plane/domain/episode.py`:
  - `TenurePolicy(max_age_s=3600, max_transitions=32, max_blocked_s=300, max_accepted_contradictions=1, usage=UNBOUND|BOUND(threshold=0.8), policy_digest)`. It rejects non-finite or non-positive values.
  - `EpisodeState` {ACTIVE, ENDED}, a closed `EndCause`, and pure `evaluate_tenure`, `validate_usage` and `admit_contradiction`.
- `control_plane/ports/episode.py`: `EpisodeControl.begin/submit/end`, `ContextUsageObservation.read(invocation_id, check_id) -> Usage | Unavailable`, and `ContradictionObservation.record(...) -> AcceptedContradiction | InvalidJudgment`.
- `control_plane/application/episode_control.py`, plus a deadline tick driven by an injected timer.
- `control_plane/adapters/episode_repository.py` (design-named): aggregate `episode:<objective>` plus immutable S1 `episode-history`, written history first, then the pointer.
- `composition/control_plane_profile.py`: a compatible addition, `EpisodeProfile(root, *, project, name, invocation, monotonic, utc_clock, timer, policy, usage=None, context_assembler, operators)`. `AttentionProfile` stays unchanged.

**Fixture inputs.**
- One `tmp_path` root with a temporary SQLite store and an S1 store.
- The C2 manifest and seed aggregates (`factory:`, `release:`, `decision-inbox`, `attention:`) from the FX-C2 fixture shape.
- Scripted clocks: monotonic seconds and aware UTC with µs resolution.
- Explicit `tick()` calls with no thread.
- A fake usage source with scripted `Usage`/`Unavailable` per `check_id`.
- An operator grant table: one authorized actor and one unauthorized actor.
- A model-launch spy, which must stay at 0.
- Two OS processes (`sys.executable`, `PYTHONPATH=src`) for the competing-epoch probes.

**Commands** (all under `rtk proxy`, `PYTHONPATH=src`).
1. PLANNED `python3 -B -m pytest -q tests/control_plane/test_episode_control.py`
2. PLANNED `python3 -B tools/evidence/fx_c3_evidence.py --output <new-dir> --invocation <exact-invocation>`. The output directory must be new. Each control is applied exactly once to a disposable copy.
3. Regression: `python3 -B -m pytest -q`; `python3 -B tools/fitness/check_architecture.py --root src/alienintent --check all`; `python3 -B -m pytest -q tests/test_architecture_fitness.py`; `node scripts/check.mjs all`. Each must exit 0.

**Probes and expected observables.** Each ID below is a PLANNED node id `tests/control_plane/test_episode_control.py::…`. `t0` is begin. "Ends" means the episode is ENDED with the exact `EndCause`, and the next `submit` of that epoch is refused with no store mutation (the `read_state` bytes are unchanged).

| Node id (PLANNED) | Input | Expected |
|---|---|---|
| `test_age_limit[below\|at]` | t0+3600 s−1 µs; t0+3600 s | ACTIVE; ends `AGE_LIMIT` (reaching ends; U-2) |
| `test_transition_limit[31\|32\|33]` | admitted `submit` count | ACTIVE; 32nd admitted then ends `TRANSITION_LIMIT`; 33rd refused |
| `test_blocked_limit[below\|at]` | blocked since tb: tb+300 s−1 µs; tb+300 s | ACTIVE; ends `BLOCKED_LIMIT` (U-3) |
| `test_one_bius_only` | `submit` naming a second BIU | refused, authority hold; episode stays bound to one BIU |
| `test_immediate_end[terminal\|objective\|authority\|provider\|model\|explicit\|context_exhausted]` | the corresponding durable event or explicit `end` | ends immediately with that cause (U-5, U-6) |
| `test_state_vector_mismatch` | `submit` with an older expected vector | `VersionConflict` → typed stale hold; ends `STALE_VECTOR`; newer state byte-equal |
| `test_contradiction[accepted\|stale_epoch\|stale_vector\|unauthorized\|missing_refs\|unresolvable_ref\|empty_rationale]` | `ContradictionObservation.record` | accepted → ends `CONTRADICTION`; every other case → `InvalidJudgment(<exact reason>)`, ACTIVE, accepted count 0 |
| `test_usage_unbound` | no usage source | `usage_binding: UNBOUND` recorded; usage `null`, never 0; begin is not terminated; age and transition limits still end |
| `test_usage_bound_threshold[below\|at]` | BOUND; used/limit = 0.7999…, then 0.8 | ACTIVE; ends `CONTEXT_USAGE_LIMIT` |
| `test_usage_bound_invalid[unavailable\|prior_check\|wrong_invocation\|nan_used\|negative_used\|zero_limit\|inf_limit]` | BOUND | ends `CONTEXT_USAGE_UNAVAILABLE`/`INVALID` (conservative) |
| `test_deadline_timer_independent_of_model` | only `tick()` past the deadline; no result or event | ends `AGE_LIMIT`; launch spy 0 |
| `test_restart_utc_deadline[expired\|regression]` | reopen in a new process | past the persisted UTC deadline → ends; UTC regression → typed HOLD `CLOCK_REGRESSION`, no admission |
| `test_no_automatic_renewal` | after any end | no new epoch without an explicit authorized `begin`; unauthorized `begin` → Hold |
| `test_renewal_new_epoch_retains_budget` | authorized `begin` | epoch+1, fresh C2 reconstruction, budget accounting carried, not reset (U-10) |
| `test_replacement_equivalence` | ended episode and successor epoch over the same manifest | equal `authorized_next_action_set`/`blocked_set`/`lifecycle` (via C2) |
| `test_competing_epochs_cannot_overwrite` (AC-03) | two processes, epoch n and n+1, one result each | epoch n refused (`inactive or mismatched epoch/authority`); epoch n+1 state byte-equal |
| `test_obsolete_revision_refused` (AC-03) | result against an obsolete vector | refused without overwrite; no effect row |
| `test_current_revision_still_authority_checked` (AC-03) | current vector, missing or insufficient authority | authority hold / `AuthorityBlocked`; no mutation |
| `test_expiry_does_not_cancel_admitted` | epoch ends with a claimed effect | effect stays `unknown`, reservation retained; no fabricated cancellation |
| `test_episode_profile_composed` | `EpisodeProfile` end to end | read-back pointer and history equal the stored record |

**Discriminating controls.** Intact exit 0 → fault exit 1 at the named assertion → restored exit 0, with `application_count` 1. The needles are pinned in `CONTROLS` before the first run. The ★ controls discharge the `053-tenure-fence` proven-red obligation.

| Control | Mutation (disposable copy) | Failing node |
|---|---|---|
| ★`old_epoch_accepted` | skip the epoch comparison on episode admission | `test_competing_epochs_cannot_overwrite` |
| ★`context_item_omitted` | successor drops one manifest item before comparison | `test_replacement_equivalence` |
| ★`unbound_as_zero` | UNBOUND records usage 0 / "measured safe" | `test_usage_unbound` |
| ★`unbound_auto_terminates` | missing binding ends tenure | `test_usage_unbound` |
| ★`bound_unavailable_ignored` | `Unavailable` keeps the episode ACTIVE | `test_usage_bound_invalid[unavailable]` |
| ★`unauthorized_contradiction_accepted` | skip the actor/authority check | `test_contradiction[unauthorized]` |
| `age_boundary` / `transition_boundary` / `blocked_limit_removed` | `>=` → `>`; limit+1; branch deleted | `[at]`, `[32]`, `test_blocked_limit[at]` |
| `threshold_boundary` / `prior_check_accepted` | `>=` → `>`; `check_id` match removed | `[at]`, `[prior_check]` |
| `vector_check_removed` / `authority_skipped_on_current` | stale-vector guard removed; authority validation bypassed at current revision | `test_obsolete_revision_refused`, `test_current_revision_still_authority_checked` |
| `immediate_cause_ignored` / `auto_renew` / `budget_reset` / `clock_regression_accepted` | provider change ignored; end starts epoch+1; counter reset; guard removed | respective nodes |
| `adapter_import_added` | domain imports the adapter | architecture check nonzero |

A non-discriminating mutation, an `application_count` other than 1, or a missing observation is a HOLD, not a PASS. Hang-only controls are residuals.

**Evidence** is retained under `docs/evidence/wave2-proof-fixtures/FX-C3/` in the FX-C1 layout:
- **`FX-C3.md`:** the fixture contract.
- **`execution-record.json`:** `ProofFixtureExecution`, `schema_version: 1`, `fixture_id: FX-C3`, the invocation and `source_revision`. It must also record:
  - an empty `source_status`, plus `admission_baseline` and `input_digests`;
  - `policy{limits, usage_binding, policy_digest}`;
  - `commands[]`, `labels`, `residuals` and `holds[]`;
  - `model_launch_count`, `independent_verdict: PENDING_FRESH_BIU_VERIFIER` and `live_proof: NOT_ESTABLISHED`;
  - tokens and cost as `null` with an `UNKNOWN` reason, never 0.
- **`run-report.json`:** maps AC-02, AC-03 and `053-tenure-fence` to node ids. It also records the end-cause table and a composed readback of `episode:` pointers and `episode-history`.
- **`proven-red.json`:** the FX-C1 fields for each control.
- **`observations/<sha256>` and `digest-manifest.json`:** immutable, written with `xb`. A collision is refused and prior observations are preserved on repair.

The fixture proves admission and termination of a contradiction judgment, never its semantic truth. Neither the fixture nor a context-exhausted event closes LRN-023.

**Boundaries.** The following are out of scope:
- bootstrap handoff or retirement (POSTW1-DECIDE-006A);
- unattended activation, the monitor host (R1-GAP-MONITOR-HOST), and C4/C5 monitor work;
- SF-REQ-008 durability elaboration and SF-REQ-029 provider normalization;
- changes to C1, C2, S2 or U5 behaviour.

The 3600/32/300 values are proposed configurable defaults, not historical canonical thresholds. A local PASS is not operational acceptance.

### Unknowns requiring an owner decision (not answered here)

The "Likely" column is a suggestion only. The owner decides.

| Q | Question and candidate answer | Clause that may decide it | Likely |
|---|---|---|---|
| U-1 | **Episode aggregate as S2 authority record.** May `episode:<objective>` be the `GuardVector.authority` aggregate (`active/epoch/invocation/expires_at` read against the store's injected monotonic clock), with the UTC deadline persisted alongside it for restart? The alternative is a separate authority aggregate. Candidate: the same aggregate plus a persisted `deadline_utc`. | `#/contracts/8/identities`, `persistence`, `concurrency`; `#/shared_contracts/operational_storage` | DELEGATED |
| U-2 | **"Reaching" a limit.** Does the limit apply at `>=` (age 3600 s, 32nd transition, 300 s blocked, 0.8), and does the 32nd admission itself end tenure? Candidate: yes. | `design_decisions[1]` "Reaching any limit ends"; `[4]` "used/limit >= 0.8" | DECIDED |
| U-3 | **"Blocked" definition and reset.** Candidate: a continuous interval, from a durable `blocked_since`, during which the C2-reconstructed `authorized_next_action_set` for the BIU is empty and `blocked_set` is non-empty; an unblock resets only this interval. This depends on the unlanded C2 shape. | `#/contracts/8/state` "limits/usage"; C2 `LABELLED_DERIVATION_RULE_v1` | DELEGATED |
| U-4 | **Membership of the relevant state vector.** Candidate: the C2 manifest record set plus the `episode:` pointer. | `concurrency` "entire relevant state vector"; C2 pin U-2 | DELEGATED |
| U-5 | **Provider/model change signal.** No normalized telemetry exists. Candidate: provider and model identity bound at `begin`; any differing identity or an explicit event ends tenure. | `design_decisions[2]` (029 excluded), `[1]` | DELEGATED |
| U-6 | **Durable carrier for the "applicability revision".** Also: which external sources are "freshness-uncheckable", and therefore hold? Candidate: the version of the authority/objective aggregate inside the guard vector. | `design_decisions[3]` | DELEGATED |
| U-7 | **The "authenticated existing operator surface" for contradiction judgments.** Candidate: an injected grant table like C1's `ResolverGrant`, exercised in-process and labelled as a local substitute. | `#/contracts/8/composition`, `security` | DELEGATED; FOUNDER if a new authenticated surface is needed |
| U-8 | **BOUND usage fixture.** Is a scripted fake `ContextUsageObservation` acceptable as a labelled local substitute? Candidate: yes, and it does not close LRN-023. | `composition` "BOUND requires same-invocation evidence adapter injection"; `conditional_proof` | DECIDED |
| U-9 | **How C3 consumes U5.** Is U5 an admission gate on C3's own design, or a runtime check? Does new `control_plane` → `execution_coordination` fenced-store import direction trigger `ARCHITECTURE_AUTHORITY_HOLD` while R2-GAP-051-EDGE-AUTHORITY is open? `observed_edges` already lists that edge but is descriptive only. Not answered here. | DAG C3 `depends_on` U5; `#/shared_contracts/context_dependency_direction`, `authority_boundaries` | FOUNDER (directional); gate shape DELEGATED |
| U-10 | **What "budget accounting" is carried across epochs.** Candidate: the counters of the explicit `begin(budget)` carry over; tenure counters are per epoch. | `design_decisions[1]`; candidate `budget.tenure_policy` | DELEGATED |
| U-11 | **Contract authority status.** `#/contracts/8/authority_status` is "DESIGN_CANDIDATE_WITH_SPECIFY_HOLD; independent repair review outstanding". Confirm the hold is discharged for the C3 extent, as it was for C1/C2, and cite the discharge. | `#/contracts/8/authority_status`; DAG C3 `completion_status: PROCEEDS_REGARDLESS` | FOUNDER / Program Director readback |
| Admission | **Prerequisite readbacks (not design choices).** Required before READY: C2 and U5 accepted candidates, verdicts and closure receipts, the S2/C1 Issue receipts cited above, and the committed WO-220303 packet, allocation and work-unit digests. | work unit § Dependencies | — |
