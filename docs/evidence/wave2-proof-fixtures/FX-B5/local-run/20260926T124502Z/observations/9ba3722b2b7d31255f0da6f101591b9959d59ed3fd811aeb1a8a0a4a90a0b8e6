{"argv":["/usr/bin/python3","-B","-m","pytest","-q","-p","no:cacheprovider","tests/control_plane/test_attention_acknowledgement.py::test_zero_duplicate_handling"],"cwd":"/tmp/fx-b5-controls-zdry7s2l","exit_status":1,"stderr":"","stdout":"F                                                                        [100%]\n=================================== FAILURES ===================================\n_________________________ test_zero_duplicate_handling _________________________\n\ntmp_path = PosixPath('/tmp/pytest-of-netmarine/pytest-6415/test_zero_duplicate_handling0')\n\n    def test_zero_duplicate_handling(tmp_path):\n        channel = Channel()\n        p = build(tmp_path, notifier=channel)\n        first = p.attention.handle(origin())\n        again = build(tmp_path, notifier=channel).attention.handle(origin())\n        assert again == first and channel.calls == 1, \"a repeated origin must not create or notify twice\"\n        assert len(p.attention.list_pending()) == 1\n        acknowledged = p.attention.acknowledge(first.identity, HUMAN, first.version, \"ack\")\n        reason = refusal(lambda: p.attention.acknowledge(first.identity, HUMAN, acknowledged.version, \"ack again\"))\n>       assert reason == \"ALREADY_ACKNOWLEDGED\", \"a second acknowledgement must be refused\"\nE       AssertionError: a second acknowledgement must be refused\nE       assert None == 'ALREADY_ACKNOWLEDGED'\n\ntests/control_plane/test_attention_acknowledgement.py:143: AssertionError\n=========================== short test summary info ============================\nFAILED tests/control_plane/test_attention_acknowledgement.py::test_zero_duplicate_handling\n1 failed in 0.30s\n"}