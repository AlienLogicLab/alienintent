{"argv":["/usr/bin/python3","-B","-m","pytest","-q","tests/context_assembly/test_context_reconstruction.py::test_fresh_invocations_equal"],"cwd":"/tmp/fx-c2-controls-6z96y7pp","exit_status":1,"stderr":"","stdout":"F                                                                        [100%]\n=================================== FAILURES ===================================\n_________________________ test_fresh_invocations_equal _________________________\n\ntmp_path = PosixPath('/tmp/pytest-of-netmarine/pytest-1786/test_fresh_invocations_equal0')\n\n    def test_fresh_invocations_equal(tmp_path):\n        from alienintent.context_assembly.domain.reconstruction import FIELDS\n        root, cwd = tmp_path / \"profile\", tmp_path / \"cwd\"\n        root.mkdir(), cwd.mkdir()\n        seed(root)\n        status, first = predecessor(root, cwd)\n        assert status == 0 and first[\"status\"] == \"RECONSTRUCTED\", first\n        outputs = [first]\n        for label in (\"a\", \"b\"):\n            status, output = successor(root, first[\"manifest_ref\"], cwd, label)\n            assert status == 0 and output[\"status\"] == \"RECONSTRUCTED\", output\n            outputs.append(output)\n        documents = [json.dumps(o[\"document\"], sort_keys=True, separators=(\",\", \":\")).encode() for o in outputs]\n        assert documents[0] == documents[1] == documents[2], \"predecessor and successors must be byte-equal\"\n        assert len({o[\"digest\"] for o in outputs}) == 1\n        assert compare(cwd, *outputs)[0] == 0\n        document = first[\"document\"]\n>       assert set(document) == set(FIELDS), \"context item omitted from the reconstructed document\"\nE       AssertionError: context item omitted from the reconstructed document\nE       assert {'authorized_...ed_decisions'} == {'authorized_...tention', ...}\nE         Extra items in the right set:\nE         'evidence_refs'\nE         Use -v to get more diff\n\ntests/context_assembly/test_context_reconstruction.py:141: AssertionError\n=========================== short test summary info ============================\nFAILED tests/context_assembly/test_context_reconstruction.py::test_fresh_invocations_equal\n1 failed in 1.08s\n"}