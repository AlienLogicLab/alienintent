{"argv":["/usr/bin/python3","-B","-m","pytest","-q","tests/execution_coordination/test_fenced_store.py::test_expired_authority_cannot_claim_or_deliver"],"cwd":"/tmp/fx-s2-controls-2bnu_glw","exit_status":1,"stderr":"","stdout":"FF                                                                       [100%]\n=================================== FAILURES ===================================\n____________ test_expired_authority_cannot_claim_or_deliver[claim] _____________\n\ntmp_path = PosixPath('/tmp/pytest-of-netmarine/pytest-1265/test_expired_authority_cannot_0')\nboundary = 'claim'\n\n    @pytest.mark.parametrize('boundary', ['claim', 'consume'])\n    def test_expired_authority_cannot_claim_or_deliver(tmp_path, boundary):\n        \"\"\"A delayed sender cannot use authority after expiry even if revisions match.\"\"\"\n        now = [199.999]\n        store, locks, vector = setup_store(tmp_path / 'store.sqlite', lambda: now[0])\n        intent(store, locks, vector)\n        post = replace(vector, versions=(('job', 1), ('authority', 1), ('premise', 1)))\n        if boundary == 'consume':\n            store.claim_guarded(PROFILE, 'effect-1', locks, post)\n        now[0] = 200\n>       with pytest.raises(ReservationRejected):\nE       Failed: DID NOT RAISE <class 'alienintent.execution_coordination.ports.operational_store.ReservationRejected'>\n\ntests/execution_coordination/test_fenced_store.py:126: Failed\n___________ test_expired_authority_cannot_claim_or_deliver[consume] ____________\n\ntmp_path = PosixPath('/tmp/pytest-of-netmarine/pytest-1265/test_expired_authority_cannot_1')\nboundary = 'consume'\n\n    @pytest.mark.parametrize('boundary', ['claim', 'consume'])\n    def test_expired_authority_cannot_claim_or_deliver(tmp_path, boundary):\n        \"\"\"A delayed sender cannot use authority after expiry even if revisions match.\"\"\"\n        now = [199.999]\n        store, locks, vector = setup_store(tmp_path / 'store.sqlite', lambda: now[0])\n        intent(store, locks, vector)\n        post = replace(vector, versions=(('job', 1), ('authority', 1), ('premise', 1)))\n        if boundary == 'consume':\n            store.claim_guarded(PROFILE, 'effect-1', locks, post)\n        now[0] = 200\n>       with pytest.raises(ReservationRejected):\nE       Failed: DID NOT RAISE <class 'alienintent.execution_coordination.ports.operational_store.ReservationRejected'>\n\ntests/execution_coordination/test_fenced_store.py:126: Failed\n=========================== short test summary info ============================\nFAILED tests/execution_coordination/test_fenced_store.py::test_expired_authority_cannot_claim_or_deliver[claim]\nFAILED tests/execution_coordination/test_fenced_store.py::test_expired_authority_cannot_claim_or_deliver[consume]\n2 failed in 0.18s\n"}