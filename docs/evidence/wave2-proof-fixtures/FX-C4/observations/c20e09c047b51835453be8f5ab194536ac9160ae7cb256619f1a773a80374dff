{"argv":["/usr/bin/python3","-B","-m","pytest","-q","-p","no:cacheprovider","tests/control_plane/test_monitor_health.py::test_scan_overdue_boundary[above]"],"cwd":"/tmp/fx-c4-controls-x2_f4xv1","exit_status":1,"stderr":"","stdout":"F                                                                        [100%]\n=================================== FAILURES ===================================\n______________________ test_scan_overdue_boundary[above] _______________________\n\ntmp_path = PosixPath('/tmp/pytest-of-netmarine/pytest-1923/test_scan_overdue_boundary_abo0')\noffset = 120000001, expected = 'STALE'\nmessage = 'an overdue last scan completion is STALE'\n\n    @pytest.mark.parametrize((\"offset\", \"expected\", \"message\"), [\n        (2 * I, \"HEALTHY\", \"at exactly 2*I the scan completion stays in bound\"),\n        (2 * I + 1, \"STALE\", \"an overdue last scan completion is STALE\"),\n    ], ids=[\"at\", \"above\"])\n    def test_scan_overdue_boundary(tmp_path, offset, expected, message):\n        clock = Clock()\n        p = started(tmp_path, clock)\n        for now in range(T0 + I, T0 + offset + 1, I // 2):\n            clock.now = now\n            p.monitor.tick()\n        clock.now = T0 + offset\n        p.monitor.tick()\n        report = status_at(p, clock, T0 + offset)\n        assert report.record.last_monitor_tick == T0 + offset\n>       assert report.status == expected, message\nE       AssertionError: an overdue last scan completion is STALE\nE       assert <HealthStatus...HY: 'HEALTHY'> == 'STALE'\nE         - STALE\nE         + HEALTHY\n\ntests/control_plane/test_monitor_health.py:144: AssertionError\n=========================== short test summary info ============================\nFAILED tests/control_plane/test_monitor_health.py::test_scan_overdue_boundary[above]\n1 failed in 0.24s\n"}