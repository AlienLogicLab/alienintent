{"argv":["/usr/bin/python3","-B","-m","pytest","-q","-p","no:cacheprovider","tests/control_plane/test_episode_control.py::test_usage_bound_threshold[at]"],"cwd":"/tmp/fx-c3-controls-87ckp8d5","exit_status":1,"stderr":"","stdout":"F                                                                        [100%]\n=================================== FAILURES ===================================\n________________________ test_usage_bound_threshold[at] ________________________\n\nworld = PosixPath('/tmp/pytest-of-netmarine/pytest-5168/test_usage_bound_threshold_at_0/profile')\ncase = 'at'\n\n    @pytest.mark.parametrize(\"case\", [\"below\", \"at\"])\n    def test_usage_bound_threshold(world, case):\n        from alienintent.control_plane.domain.episode import TenurePolicy, UsageBinding\n        usage = ScriptedUsage(\"valid\", used=7.999999 if case == \"below\" else 8.0, limit=10.0)\n        profile = episode_profile(world, Clocks(), policy=TenurePolicy(usage=UsageBinding.BOUND), usage=usage)\n        start(profile)\n        profile.episodes.tick(OBJECTIVE)\n        assert usage.calls == [(\"fx-c3-a\", \"check:1:1\")], \"the sample is requested for this invocation and check\"\n        if case == \"below\":\n            assert str(record_of(profile).state) == \"ACTIVE\"\n        else:\n>           assert_ended(profile, world, \"CONTEXT_USAGE_LIMIT\")\n\ntests/control_plane/test_episode_control.py:411: \n_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ \n\nprofile = <alienintent.composition.control_plane_profile.EpisodeProfile object at 0x761068bd6540>\nroot = PosixPath('/tmp/pytest-of-netmarine/pytest-5168/test_usage_bound_threshold_at_0/profile')\ncause = 'CONTEXT_USAGE_LIMIT', objective = 'item-verify'\n\n    def assert_ended(profile, root, cause, objective=OBJECTIVE):\n        \"\"\"Ended with the exact cause; the same epoch's next result is refused with no store mutation.\"\"\"\n        from alienintent.control_plane.ports.episode import Refused\n        record = record_of(profile, objective)\n>       assert (str(record.state), str(record.cause)) == (\"ENDED\", cause), f\"expected ENDED {cause}, got {record.state} {record.cause}\"\nE       AssertionError: expected ENDED CONTEXT_USAGE_LIMIT, got ACTIVE None\nE       assert ('ACTIVE', 'None') == ('ENDED', 'CO..._USAGE_LIMIT')\nE         At index 0 diff: 'ACTIVE' != 'ENDED'\nE         Use -v to get more diff\n\ntests/control_plane/test_episode_control.py:123: AssertionError\n=========================== short test summary info ============================\nFAILED tests/control_plane/test_episode_control.py::test_usage_bound_threshold[at]\n1 failed in 0.49s\n"}