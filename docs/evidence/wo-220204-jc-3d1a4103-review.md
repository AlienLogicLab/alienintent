# WO-220204 independent JC repair verification

Verdict: **ACCEPT** for producer candidate `c8ed707efdb0cd70294bfdb6814ec9f9eb71216d`,
published branch `b-disp/78edebf8-e497-4d28-ad79-0f194c86703a`.
Invocation: `AlienLogicLab/alienintent#94:VERIFIER:3d1a4103-93fc-496c-a63a-23cddf8adb1a`.

## Authority and custody

Reconstructed from Issue #94's body, RELEASED/continuation records, prior independent
rejection 5814072415 and repair submission 5814469321; the pinned WO-220204 contract,
packet, allocation, SF-REQ-014 design and final VERIFIED design assessment govern.
Scope is U4/FX-U4 only, local composed proof. Identity verified as JC / jc-worker.
Remote candidate readback matched. The initially clean assigned runtime worktree was
at `d6b5846` (main plus subsequent unrelated packet preparation); its assigned branch
was repointed to the exact fetched candidate for review without altering main or
combining unrelated work. Admission baseline is `4b94f2d1d7d40c1889af79596e8f92c737e55d7e`.

Retrieved independent predecessor receipts 5792843472 (#76), 5808965906 (#80),
5811071942 (#81). Exact accepted candidates `43ea5e2b745be02df6aebaf5ba1e328f4c75fa5e`,
`d0eacfb6dd8626b07ff1d99e9c633294f13d9de4`, and
`3e8dc7032dc7e61aac48b66ea9d08a3e789757ad` are ancestors. Their retained proof is
consumed through the pinned mappings; no predecessor implementation was changed.

## Findings and acceptance

R1 is closed. The reported structured history loss now holds. Length/version and
retained-chain checks also reject truncated or padded histories. The original prior
verifier reproduction runs verbatim and exits 1 at the expected EvidenceHold, rather
than admitting the missing judgment. This nonzero reproduction is evidence of repair,
not a passing test command. Independent supplementary probes exit 0: two initial
holds, plan, hold, plan remain admissible; erasure at the same SQLite version holds;
restoration retains the prior obligation and unauthorized deletion holds.

Mapping-faithfulness **JUDGMENT**, reviewer JC under the independent verifier allocation:
inspected the predicate mapping, pinned FX-U4 contract, specified SF-REQ-014 and verified
design probes, implementation, tests and retained observations. The runnable bindings
faithfully express those probes within this bounded local capability. The mapping and
expected behavior were pinned in `875eba6` before implementation `1396a16`; the mapping
is byte-identical today. Four isolation observables remain distinct and use the U3
capability premise; no impossible credential-denial oracle is substituted. Judgment
records retain reviewer, inputs and decision reference without mechanical PASS.
This document and the exact-SHA Issue receipt record the independent decision.

AC-01 through AC-04 and the three BLOCKING enforcement obligations are satisfied.
Advisory ordering controls discriminate and the ordered run has no diagnostics.
No remaining blocking finding was identified.

## Fresh observations

All commands and exit codes are retained in `wo-220204-jc-3d1a4103/commands.json`.

- Python tests: exit 0, **602 passed in 63.03s**.
- Architecture fitness: exit 0, PASS. Node required checks: exit 0. Diff check: exit 0.
- FX-U4: exit 0, **31/31 QUALIFIED_KILL**; 93 phase logs independently hash-verified,
  intact/restored exit 0, fault exit 1, application counts 0/1/0. Disconnected
  composition fails its target while domain tests stay green.
- Five-obligation plan digest remains `sha256:b85538ecc19947ee9fa7030919ebcc95bbe24a614f1f61f214abd852db57484a`.
- Producer R4 manifest: 195 entries verified. Original immutable-object manifest:
  88 entries verified. All 91 original R3 files are byte-identical to rejected
  candidate `53833f5`; original 29 controls and evidence remain preserved.
- Verifier raw logs and 94 immutable objects are exported alongside this review;
  `sha256.json` provides their custody manifest.

## Limits and disposition

The OperationalStore remains authoritative for version and pointer. Whole-row deletion
or restoration of both row and version is outside this history-consistency repair's
detectability; acceptance does not certify tamper-proof storage or live recovery.
The harness plan root retains its compiled-in producer invocation; phase observations
and report use this verifier invocation. This independent receipt supplies attribution
and verdict rather than treating harness metadata as a reviewer decision.
Tokens and cost UNKNOWN. Local proof makes no operational claim.

Only verifier evidence is added. The runtime-managed branch/worktree is retained for
BIU closure under the Factory Director, who owns SWF-19 merge of the accepted producer
SHA and evidence disposition/cleanup. No PR, baseline push, merge, live operation,
other-repository change or lifecycle mutation is performed by this verifier.
