# WO-220201 / FX-U1 pinned execution contract

## Intent and authority

Issue #76 and its RELEASED comment 5792028317 authorize only requirements_inventory.
Morty owns runtime-managed branch `b-disp/d2edd0f0-1ad0-41d6-911b-5c28c931895d`
for invocation `AlienLogicLab/alienintent#76:PRODUCER:c03b4f78-22a9-43d1-829e-26042274efc8`.
Admission baseline: `901d17444aa0aa1e5098017c3726d0d0daad1442`; clean at admission,
matching remote main; Git and GitHub identity verified as Morty / morty-worker.
The active profile binds #76 to three cycles and one replacement per phase.
No live operation, authority promotion, definition rewrite, sibling work or RAI wiring.

Reuse the existing Python context_assembly location, EvidenceRepository and
OperationalStore. This does not resolve the separate FD-02 semantic/module mapping.
No new bounded context or directional permission table is established.

## Inputs and expected outputs (pinned before implementation)

Source: `wave2-design-contracts.json#/contracts/0/identifier_contract/historical_fixture_manifest`,
POSTW1-011-HISTORICAL-CORPUS-R2, 17 Git blobs at
`d83e87e6f2bb4ff90ff4f4f0e3940582f8d5c998`. All 17 SHA-256 values were independently
read and checked before implementation. The manifest's exact referenced set and
every `(path,line,form,requirement_id)` definition locator are the oracle, not counts.
Expect 56 referenced, 53 defined, undefined 048/052/056, unique 050, retired 054.
The historical fixture carries no production authority. Later 056 is a separate revision.

Synthetic inputs exercise each of the three forms with suffixes, extended namespaces,
malformed/empty/ambiguous identifiers, acceptance IDs, compounds, possessives and links.
Expect exact raw tokens, kinds, operands, locators and typed issues; no salvaged prefixes.
Equivalent semantic/authority revisions merge format aliases; conflicts hold affected
IDs only; reference-only records never resolve to definitions. Changed revisions retain
history and stale downstream links; retirement is irreversible without source resolution.
Missing/digest-mismatched blobs hold the source. Excluded documents have no effect.
Immutable manifest/snapshot observations must be installed and read back before CAS
updates only the derived current pointer. A stale version must fail without blind retry.

## Commands and evidence schema

- `PYTHONPATH=src python3 -m unittest tests.context_assembly.test_inventory -v`
- `PYTHONPATH=src python3 tools/evidence/fx_u1_evidence.py --output /tmp/wo-220201-fx-u1`
- `python3 tools/fitness/check_architecture.py --root src/alienintent --check all`
- `PYTHONPATH=src python3 -m unittest discover -s tests`
- `node scripts/check.mjs all`

Normal commands must exit 0. The FX runner must execute independently mutated copies
for disabled prose parsers, conflicting definitions, weakened grammar, suffix stripping,
malformed suppression, wrong kind, compound operand loss, excluded scanning and digest
mismatch. Each selected assertion must pass intact, fail faulted, and pass restored.
Record schema: schema_version, fixture, baseline, invocation, source_manifest_digest,
command, expected, observed, exit_status, raw_output_sha256, candidate revision, and
control application count. Definitions, observations and independent verdict remain
distinct. Token/cost measurements are UNKNOWN unless supplied by measured telemetry.

## Execution and custody

Test first, implement domain/parser/source/persistence boundaries, run discrimination
and regression checks, repair findings, commit and publish exact candidate branch/SHA.
Independent verification is a fresh invocation retrieving that SHA. Retain this
runtime-managed candidate for BIU closure; do not delete it or push directly to main.
On a real blocker, record exact evidence and disposition on #76.
