# Wave 2 re-SPECIFY: SF-REQ-011 and SF-REQ-013

Date: 2026-09-22. Session A of B. This is a requirements/design revision, not implementation, independent Design Verification, readiness assessment or release authority.

## Authority, baseline and execution plan

Direct task: revise the two specification and contract objects plus the named DAG predicates, preserve unrelated obligations, run six checks, make one local commit and do not push. Reconciliation input, read first: `/tmp/claude-1000/-mnt-d-Projects-alienintent/98be9ff8-7334-433b-86cf-c9b80a665408/scratchpad/wave2-recon.codex.md` (`RE_SPECIFY_REQUIRED`).

Binding authority: factory plan SF-REQ-011/013 amendments of 2026-09-22; Architecture Authority A2/A3 and amendment (b); Ubiquitous Language v0.1; hexagonal RequirementSource, ReadinessAssessment and AssessmentFeedback port rows; FD-02 refinement; SWF-25 Design Verification and Reuse Before Build. Phase 5 split/replan is candidate design, with binding SWF-33 as the worked precedent.

Baseline: `764e53216ba56c95e9ae87e05105fa1f606aabee`. Branch: `wave2/respecify-2026-09-22`. Worktree: `/mnt/d/Projects/alienintent/.worktrees/wave2-respecify`. Git identity: `netmarine <sanuk.du@gmail.com>`. Initial tree clean; subsequent edits are KNOWN_AUTHORIZED session A work. No new worktree was created.

Completed execution sequence: read delta and authority; retain baseline obligations while revising two specifications; reconcile their contracts and shared exclusions; revise only named DAG annotations without topology changes; render the two Markdown entries; run required checks and compare scope/preservation against baseline; prepare one local commit. No provider inference, network, lifecycle operation or runtime implementation was performed.

## Obligation trace

Pointers below are JSON pointers. S denotes `docs/evidence/wave2-specified-requirements.json`; C denotes `docs/evidence/wave2-design-contracts.json`; D denotes `docs/evidence/wave2-dependency-dag.json`.

| Amended obligation | Revision and location |
|---|---|
| 011: distinct requirement-information and work-state roles, even for one vendor | S `/candidates/0/scope/2`, `/candidates/0/acceptance_criteria/4`; C `/contracts/0/ports/0`, `/contracts/0/ports/3`, `/contracts/0/adapters/0`: RequirementSource versus WorkManagement, separate adapters and conformance refusals. |
| 011: all supported source kinds | S `/candidates/0/scope/3`; C `/contracts/0/adapters/0`: GitHub Issues/Projects, Jira, Linear, Azure DevOps, GitLab, local files, structured product documents, native proposals and imported prototypes/artifacts. This adds contract coverage, not new waves or claims of live integrations. |
| 011: translate Source Records into the neutral internal model | S `/candidates/0/intent`, `/candidates/0/scope/4`; C `/contracts/0/ports/0`, `/contracts/0/ports/1`, `/contracts/0/ubiquitous_language`: raw vendor data remains outside the domain; immutable neutral records feed inventory. |
| 011: seven-field Requirement Provenance | S `/candidates/0/scope/4`, `/candidates/0/acceptance_criteria/4`; C `/contracts/0/state`, `/contracts/0/evidence`, `/contracts/0/failure_modes/5`: retain external source, identity, revision, link, ingestion timestamp, authority status and synchronization/projection semantics; independently omit each field in negative probes. |
| 011: vendor objects are not domain identities | S `/candidates/0/scope/5`, `/candidates/0/acceptance_criteria/5`; C `/contracts/0/identities`: Jira Epic ≠ Requirement, GitHub Issue ≠ BIU, Linear Project ≠ Wave; preserve project/source scope. |
| 011: replace sources/providers without rewriting domain | S `/candidates/0/acceptance_criteria/5`; C `/contracts/0/architecture_fitness`, `/contracts/0/deterministic_enforcement_opportunities/1`, `/contracts/0/acceptance_design_trace/5`: two adapter implementations per role, two Projects, unchanged identity/meaning and domain code. |
| 011/013: Requirements / Planning semantic owner | S `/candidates/0/intent`, `/candidates/2/intent`; C `/contracts/0/domain_owner`, `/contracts/2/domain_owner`, `/shared_contracts/architecture_scope`: explicit semantic ownership. Legacy `bounded_context` implementation-location fields remain; FD-02 package mapping is not decided here or delegated to implementers. |
| 013: own split/replan and preserve judgment/mutation boundary | S `/candidates/2/title`, `/candidates/2/selection_reason`, `/candidates/2/non_goals/0`, `/candidates/2/authority_gaps/0`; C `/contracts/2/ports/3`, `/contracts/2/transitions/3`, `/contracts/2/composition`: SF-REQ-013 performs authorized mutation, Agent Ready supplies judgment/boundaries, SF-REQ-015 lints and assesses results. |
| 013: freeze and map 100% of four obligation categories | S `/candidates/2/scope/2`, `/candidates/2/acceptance_criteria/1`, `/candidates/2/acceptance_criteria/4`; C `/contracts/2/state`, `/contracts/2/evidence`: requirements, acceptance, verification and evidence obligations; forward and reverse mapping, with every shared extent required. |
| 013: no lost, invented or weakened intent/proof | S `/candidates/2/acceptance_criteria/4`; C `/contracts/2/failure_modes/2`, `/contracts/2/failure_modes/4`, `/contracts/2/deterministic_enforcement_opportunities/0`: negative probes for omissions, weaker proof, invented scope and widened budgets/capabilities. |
| 013: deterministic dependency identity rewrites | S `/candidates/2/scope/3`, `/candidates/2/acceptance_criteria/6`; C `/contracts/2/design_decisions/4`: preserve old predicates and integration boundary, copy incoming prerequisites conservatively, no judgment redirection, cycles or transitive pruning; inter-child ordering requires explicit authority. |
| 013: record lineage | S `/candidates/2/observability_evidence/1`; C `/contracts/2/state`, `/contracts/2/evidence`: original/results, parent/children, split_from, operation identity, before/after revisions and graph; suffixes do not encode ancestry. |
| 013: invalidate, materialize and resubmit each result | S `/candidates/2/scope/4`, `/candidates/2/acceptance_criteria/7`; C `/contracts/2/transitions/4`, `/contracts/2/persistence`, `/contracts/2/recovery`: each child and retained Integration Parent, plus otherwise affected assessments; preserve raw old records, record stale applicability separately, no stale READY admission. |
| 013: explicit grammar, never sorting-derived identity | S `/candidates/2/acceptance_criteria/6`, `/candidates/2/operational_constraints/2`; C `/contracts/2/identities`: Phase 5 fixed-width PG/PY/WO grammar carried forward as candidate, explicit configured family binding, persistent reservation per semantic unit, collisions/exhaustion fail closed, PY-09B preserved. |
| 013: compiler derives initial decomposition | S `/candidates/2/scope/0`, `/candidates/2/acceptance_criteria/5`; C `/contracts/2/ports/0`, `/contracts/2/transitions/0`, `/contracts/2/design_decisions/0`: positive composed compilation without a mandatory external mapping; supplied-candidate validation is insufficient. |
| 013: obligation mapping versus execution Allocation | S `/candidates/2/scope/1`, `/candidates/2/operational_constraints/2`; C `/contracts/2/ubiquitous_language/obligation mapping`, `/contracts/2/state`, `/contracts/2/security`, `/contracts/2/observability`: remove the compile-time Allocation definition and usage. Historical gap IDs retain their exact spelling. |
| 013: reconcile Phase 5 and SWF-33 | S `/candidates/2/provenance/2`, `/candidates/2/provenance/3`; C `/contracts/2/design_decisions/1`, `/contracts/2/design_decisions/4`, `/contracts/2/design_decisions/5`: candidate mechanisms distinguished from binding ownership/invariants; historical bootstrap-assessor sequences explicitly labelled. |
| Reuse Before Build and actual producer provenance | C `/contracts/0/design_decisions/3`, `/contracts/2/design_decisions/2`, `/contracts/2/ports/3`, S `/candidates/2/acceptance_criteria/7`: ask for existing authoritative capabilities, use supported interfaces and check identity/version rather than output shape; no local readiness substitute. Feedback timing remains unsettled under SF-REQ-030. |

All added acceptance IDs are appended; existing IDs remain. SF-REQ-011 AC-01 through AC-04 are unchanged. SF-REQ-013 AC-01, AC-03 and AC-04 are unchanged; AC-02 strengthens total mapping coverage and AC-05 replaces only the amendment-superseded replan exclusion. Every amended acceptance criterion is linked in the corresponding `source_specification` and `acceptance_design_trace`.

## Shared contracts and DAG

C `/shared_contracts/amendments` now includes SF-REQ-013's binding amendment while preserving unrelated pending amendments. `/shared_contracts/authority_boundaries` removes only the compiler-derivation hold. `/shared_contracts/architecture_scope` and `/shared_contracts/alternatives_and_selection` remove the same contradictory assumption. No changes to contracts 4 or 6.

D `/nodes/9` (U7) now validates both initial and conserved split candidates instead of refusing all replans. `/nodes/10` (U8) requires compiler derivation and complete transaction proof; its surviving edge-authority hold remains. `/nodes/13/completion_predicate` requires integrated split/result-assessment proof and no longer treats derivation as unresolved. Allocation-gap propagation is removed from `/nodes/10/authority_gap_refs`, `/nodes/10/direct_authority_gap_refs`, `/nodes/13/authority_gap_refs`, `/nodes/30/authority_gap_refs`, `/nodes/33/authority_gap_refs`, `/nodes/41/authority_gap_refs` and `/nodes/42/authority_gap_refs`. Node IDs, dependency edges, capability ownership, fixture references and unrelated predicates remain unchanged. U8 and downstream completion statuses remain held by surviving gates; this is not a completion/release claim.

Each revised specification, contract, shared-contract container and DAG node carries `revision_2026_09_22` with reason, delta path, authority, changed pointers and preserved holds. The Markdown specification renders the revised entries and marks its unchanged Phase 8 evaluations as baseline history.

The explicitly scoped JSON edits leave original top-level repair/validation/provenance records and other candidates untouched. In particular, original SPLIT-REPLAN evaluation, Phase 8 pending-amendment inventory and Phase 9 root R1 allocation-gap record are retained baseline records, superseded for current SF-REQ-013 by the revised objects and this note. They must not be used to reopen the settled ownership/derivation questions. Original design/DAG prose companions and fixture catalog narratives outside the named nodes were not rewritten in session A. Session B remains responsible for its specified 015/039 objects and assessment-owner annotations.

## Holds and candidate-design reconciliation

Removed: **SPLIT-G1** (SF-REQ-013 owns the transaction) and **R1-GAP-013-ALLOCATION** (compiler derives initial decomposition), both settled by the factory-plan SF-REQ-013 amendment of 2026-09-22. The general-replan exclusion, `INITIAL_ONLY` and `OUT_OF_SCOPE_REPLAN` behavior are superseded.

Preserved: source conflicts/unavailable inputs, exact authorized revisions, verified design, fixed decisions, capability/access/budget/retry bounds, graph endpoint/cycle/predicate checks, evidence custody, normal readiness/release/closure prerequisites, and exact-payload planning authorization. The amendment assigns product ownership; it does not authorize every future mutation.

Unrelated holds preserved: R2-GAP-051-EDGE-AUTHORITY, R1-GAP-039-ORCHESTRATION, R1-GAP-039-REAL-OUTCOME, R1-GAP-MONITOR-HOST, G+I operational proof, SF-REQ-056 ratification, release/transport/cutover/notification/resident-tenure authority and remaining pending amendments. Session B's G1/G2 text and related 015 annotations are unchanged, not reasserted as fresh unresolved Founder ownership decisions by this revision.

Phase 5's frozen/reverse obligation inventory, retained Integration Parent, conservative graph rewriting, identity reservation, exact-payload authorization, atomic apply/readback, projection fencing and recovery are carried as **candidate design**, subject to independent revised Design Verification. The amendment does not itself approve their detailed algorithms. Original authority-gap claims and bootstrap vocabulary are not carried forward as current semantics. No active invocation is split in place, no DONE work is reopened, no new external fields are assumed and no original integrated proof is weakened. No Phase 5 or other historical evidence bytes were changed.

## Topology, decisions and UNKNOWNs

**Topology change judged necessary: none.** U7 validation and U8 compilation already precede A via its existing dependencies; extending their completion/proof predicates covers the amended 013 behavior. The runtime reassessment loop is a port interaction, not an implementation-DAG dependency cycle. The checked graph retains all 46 nodes and every original edge and capability owner. No new substrate owner or node is introduced.

**New Founder decision required for this reconciliation: none.** The named amendments settle source intake, Requirements / Planning ownership, compiler derivation and split/replan ownership. Candidate mechanism verification and the previously recorded architecture-edge/039/monitor holds remain distinct.

- **UNKNOWN:** current runtime conformance and live availability of each Requirement Source adapter; fixtures and prose are prospective proof obligations.
- **UNKNOWN:** fresh Agent Ready CLI/MCP compatibility and producer/version provenance for actual resulting candidates; no inference or readiness assessment was run.
- **UNKNOWN:** independent Design Verification outcome for revised contracts, including Phase 5 candidate mechanism suitability and FD-02 package mapping. No self-approval or implementation readiness is claimed.
- **UNKNOWN:** AssessmentFeedback emission maturity point, intentionally unsettled in the authority; this revision does not decide it.

## Validation

All commands used the `rtk proxy` prefix. Required checks on the revised artifacts:

| Command | Exit | Exact result line |
|---|---:|---|
| `python3 tools/evidence/check_wave2_specify.py docs/evidence/wave2-specified-requirements.json` | 0 | `result     : PASS  (0 failure(s))` |
| `python3 tools/evidence/check_design_contracts.py docs/evidence/wave2-design-contracts.json` | 0 | `result    : PASS  (0 failure(s))` |
| `python3 tools/evidence/check_wave2_dag.py` | 0 | `result     : PASS  (0 failure(s))` |
| `python3 tools/evidence/check_wave2_bius.py` | 0 | `result   : PASS  (0 failure(s))` |
| `python3 tools/evidence/check_canonical_vocabulary.py` | 0 | `result    : PASS  (0 failure(s))` |
| `python3 -m pytest tools/evidence -q` | 0 | `161 passed in 1.28s` |

No checker rejected these revisions; no checker or test changes were needed. These checks establish their structural/evidence contracts, not execution of the newly designed acceptance probes. Scope comparison additionally checks unchanged nonselected objects, unchanged DAG topology/capability ownership, preserved historical identifier fixture, trace completeness and only the five authorized deliverable files changed. Whitespace check: `git diff --check`.

## Disposition and handoff

**PARKED** on `wave2/respecify-2026-09-22`, owned by the session A Requirements / Planning analyst for handoff to session B. Unique content: the two revised specification entries and rendering, their design contracts/shared text, seven DAG node annotations and this note. Direct user instruction prohibits push; LAND/publication authority is absent. Next action: session B revises SF-REQ-015/039 on this branch, followed by separately authorized independent verification and publication/disposition. Keep this supplied worktree for that continuation. No remote state is claimed.

### Local commit blocker

All six checks passed again on the final artifact content (pytest: `161 passed in 1.28s`); the scope comparison and `git diff --check` also passed. Staging the five deliverables failed with exit 128:

```text
fatal: Unable to create '/mnt/d/Projects/alienintent/.git/worktrees/wave2-respecify/index.lock': Read-only file system
```

The worktree files are writable, but its Git metadata lives outside the writable roots. This session prohibits permission escalation. No index change, commit or push was made; HEAD remains the baseline above. Session A content is therefore PARKED **uncommitted**, with this concrete external prerequisite: permit writes to the worktree's Git metadata in an authorized environment, then stage these five files and create the requested single local commit. Prepared commit message: `/tmp/wave2-respecify-011-013-commit-message.txt`. The user-authorized commit is still outstanding; no SHA exists for the revision. After committing, continue session B; do not push without separate authority.
