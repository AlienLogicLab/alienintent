# Design Verification round 2 — 2026-09-22

Reviewed branch `wave2/respecify-2026-09-22` at `d120455ac3d73cda31a4b3e3d912c69594c63ca2`, independently against the named authority. Worktree clean before and after. No files modified; no provider inference run.

Pointers below use:

- **S** = [wave2-specified-requirements.json](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-specified-requirements.json)
- **C** = [wave2-design-contracts.json](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-design-contracts.json)
- **D** = [wave2-dependency-dag.json](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-dependency-dag.json)
- **B** = [wave2-candidate-bius.json](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-candidate-bius.json)
- **MD** = [wave2-specified-requirements.md](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-specified-requirements.md)

## Repair verification

| Finding | Status | Evidence | New defect? |
|---|---|---|---|
| DV-1 | **SATISFIED** | `D#/authority_gaps/2/resolution_2026_09_22` and equivalent C pointer record `SETTLED_BY_AMENDMENT`, preserving original text. `D#/authority_policy/wave2a_completion_blocker` names `R2-GAP-051-EDGE-AUTHORITY`, matching node A. Statistics correctly recount **15 PROCEEDS_REGARDLESS, 23 GAP_BLOCKED, 8 SEPARATE_AUTHORITY_REQUIRED**. The new checker rejects the previous artifact with four failures. | Checker coverage limitation: DV-12. Live register is correct. |
| DV-2 | **PARTIAL** | All ten selected inventory titles/reasons match S; MD:828 matches `S#/lane_semantics/READY/failure`. MD is included in `ACTIVE_ARTIFACTS`; the previous full Markdown now fails vocabulary checking. The contracts companion has an explicit historical/superseded banner at lines 3–9. However, active prose remains inconsistent: MD:106 retains the superseded AC-05, and MD:15 still calls settled Agent Ready ownership an open decision. | New AC-05 divergence: DV-9. Ownership prose is an additional pre-existing omission. |
| DV-3 | **PARTIAL** | `S#/candidates/0/acceptance_criteria/4` and `C#/contracts/0/deterministic_enforcement_opportunities/1` implement the two-kind proof and extension obligations. All nine named source kinds remain in `S#/candidates/0/scope/3`; seven-field provenance remains in `/scope/4`. But `C#/contracts/0/acceptance_design_trace/4/design_probe` still requires fixtures for **all listed kinds**. | Yes: contradictory acceptance representations, DV-9. |
| DV-4 | **SATISFIED** | `C#/shared_contracts/architecture_scope` explicitly adds SF-REQ-015 lint and `ReadinessAssessment` to the SF-REQ-051/FD-02 mapping follow-up. `C#/contracts/4/domain_owner` preserves Requirements / Planning ownership and labels `bounded_context` a legacy implementation location. | None found. |
| DV-5 | **PARTIAL** | Both canonical targets are complete: `/provenance_contract/text` preserves the previous provenance paragraph exactly; `/ownership_statement/text` preserves the previous ownership statement exactly. Referencing state/evidence/acceptance fields retain their distinct meanings. S dependency and prerequisite fields are corrected. However, ownership copies and numeric adapter limits remain, and cross-file fragments omit their target filename. | Reference ambiguity introduced; dedup incomplete: DV-10. |
| DV-6 | **SATISFIED** | `C#/contracts/6/ports/0` names `authority-block / HumanDecisionRequired`, forbids a new outcome kind, and makes AC-09 conditional if a protocol change proves necessary. `S#/candidates/6/acceptance_criteria/8/carrier` agrees. Existing `HumanDecisionRequired` has recommendation/options/tradeoffs and attribution fields; `WorkerOutcome.authority_block` carries it, and the coordinator registers that escalation. | None found. This establishes design compatibility, not executed scenario proof. |
| DV-7 | **SATISFIED** | `D#/nodes/11/completion_predicate` now names the `SplitTransaction` handoff, refusal without materialized results, U8 transaction ownership, and subsequent lint/reassessment. `/proceeds_regardless` confines independent work to consumer/current-interface tests. | None found; no edge change needed for that boundary obligation. |
| DV-8 | **PARTIAL** | `C#/contracts/4/provenance_contract/version_source/cli` correctly names installed-distribution metadata and no CLI `--version`. `/mcp` correctly warns that handshake version is the SDK version. But it does not explicitly provide a product-version acquisition mechanism for the MCP adapter. | The new transport-specific mapping leaves MCP product-version establishment ambiguous: DV-11. |

The **two-kind proof is faithful to amended SF-REQ-011** as a port-conformance obligation: the support list remains intact, source/work roles remain separate, all seven provenance fields remain mandatory, and downstream intake fixtures become extension obligations. This fits the substitution target and G27’s two-implementation stability criterion without assigning downstream implementation to Wave 2. The remaining defect is failure to propagate that decision consistently.

## New findings

**DV-9 — MATERIAL — The repaired AC-05 conflicts with its design trace and active Markdown.**

`S#/candidates/0/acceptance_criteria/4` now requires two heterogeneous kinds, while:

- `C#/contracts/0/acceptance_design_trace/4/design_probe` ([line 472](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-design-contracts.json:472)) still requires “fixtures for all listed kinds.”
- [MD:106](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-specified-requirements.md:106) still requires “every named source kind.”

These locations agreed before the repair; they now prescribe incompatible acceptance scope. Separately, [MD:15](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/docs/evidence/wave2-specified-requirements.md:15) retains the pre-existing, incorrect ownership hold.

**Exact repair:** synchronize the AC-05 design probe and Markdown criterion/verification with S, including extension obligations. Refresh the SF-REQ-015 dependency/prerequisite rendering and ownership introduction. Add a focused consistency check with the stale AC-05 as its negative control; vocabulary checking alone cannot detect this contradiction.

**DV-10 — ADVISORY — Deduplication is incomplete, and external pointer targets are implicit.**

The ownership paragraph remains at:

- `S#/candidates/4/scope/4`
- `C#/contracts/4/source_specification/authority_conditions/0`
- `C#/shared_contracts/authority_boundaries`

This contradicts `/ownership_statement/dv_repair`, which claims the latter locations reference it. `C#/contracts/4/adapters/2` still repeats the 100,000-character and 400,000-byte limits despite `/adapters_note` claiming otherwise.

New fragments in S’s AC-07/observability fields and `D#/nodes/12/completion_predicate` point to `#/contracts/4/...` without naming C; that pointer does not resolve within S or D.

**Exact repair:** replace remaining ownership copies with role-labelled references; qualify cross-document references as `wave2-design-contracts.json#/contracts/4/...`; move repeated limits/syntax to the package-bound conformance fixture while retaining adapter behavior and failure semantics.

**DV-11 — ADVISORY — MCP handshake version is identified correctly but cannot establish product version.**

Agent Ready’s [README:90](/mnt/d/Projects/agent-ready/README.md:90) and [UL:124](/mnt/d/Projects/agent-ready/docs/ubiquitous-language.md:124) require package-release identity. Static inspection confirms the distinction: Agent Ready constructs `Server("agent-ready")` without a product version; the installed SDK’s [server.py:183](/mnt/d/Projects/agent-ready/.venv/lib/python3.12/site-packages/mcp/server/lowlevel/server.py:183) defaults to `pkg_version("mcp")`.

The repair correctly prohibits substitution. Nevertheless, the only mechanism specifically assigned to `/version_source/mcp` yields the wrong kind of version; installed-distribution metadata is listed only under `/cli`.

**Exact repair:** explicitly obtain Agent Ready’s package version from the configured MCP server environment’s installed-distribution metadata, bound to the launched server identity. Record handshake SDK version separately. Preserve UNKNOWN admission blocking when product provenance cannot be established.

**DV-12 — ADVISORY — `root_register_consistent` detects the original failures but permits other register contradictions.**

At [check_wave2_dag.py:144](/mnt/d/Projects/alienintent/.worktrees/wave2-respecify/tools/evidence/check_wave2_dag.py:144), any dated resolution key counts as settlement regardless of its contents. The blocker check requires only that *some* node carries the gap.

Independent in-memory mutations all incorrectly passed:

1. Change allocation resolution status back to `RETURN_TO_SPECIFY`.
2. Change Wave 2A’s blocker to `R1-GAP-MONITOR-HOST`, which A does not carry.
3. Reintroduce the settled allocation gap onto A.

**Exact repair:** validate terminal resolution status, reject settled gaps retained as active node holds, and validate the Wave 2A blocker against A’s applicable gap references. Add these three negative controls. This limitation does not invalidate the correctly repaired live register.

## Checks

All commands used `rtk proxy`; Python bytecode writes were disabled.

| Check | Observed result |
|---|---|
| `check_wave2_specify.py docs/evidence/wave2-specified-requirements.json` | **PASS**, exit 0; 41 evaluated, 10 selected |
| `check_design_contracts.py docs/evidence/wave2-design-contracts.json` | **PASS**, exit 0; 10 contracts |
| `check_wave2_dag.py` | **PASS**, exit 0; 46 nodes |
| `check_wave2_bius.py` | **PASS**, exit 0; 46 BIUs |
| `check_canonical_vocabulary.py` | **PASS**, exit 0; 17 active artifacts, none absent |
| `TMPDIR=/tmp python3 -m pytest tools/evidence -q -p no:cacheprovider` | **Blocked before collection**, exit 1: no writable temporary directory |
| Same pytest command with `-s` | **167 passed, 1 setup error**, exit 1 |
| `git diff --check HEAD~1 HEAD` | **PASS**, exit 0 |

The setup error affected `test_an_assessment_file_missing_from_the_manifest_is_rejected`. Its unused `tmp_path` fixture could not initialize. Invoking its unchanged assertions directly with `tmp_path=None` passed; this is **not** a full pytest-suite pass.

**Meaningful red controls:** the current DAG checker rejects the actual `HEAD~1` DAG with four failures; the current vocabulary checker rejects the actual previous full Markdown with one failure. Both pass current artifacts for the intended reasons. The added test assertions ran successfully in the capture-disabled run.

**Conservation:** parsed comparison of `HEAD~1..HEAD` found changes only to candidate/contract indices 0, 4 and 6, DAG nodes 11 and 12, and the identified shared/root records. Node identities, edges, capability ownership, proof requirements, gap references, completion statuses and proof fixtures are unchanged. Original provenance/ownership text survives intact. No unrelated obligation removal was found; DV-9 is a consistency regression.

**S0 / WO-220101:** `B#/bius/0` is semantically identical. The entire BIU JSON file is byte-identical across the repair.

**Founder classification:** the [main-checkout MONITOR-HOST row:61](/mnt/d/Projects/alienintent/docs/operations/post-wave1-program/reports/2026-09-22-founder-decision-bundle-classification.md:61) matches round 1 answer 8: SWF-27/SF-REQ-053 and SF-REQ-037/038 supply authority; AA §14 is explicitly excluded; contract revision, supervisor/doctor detail and independent DV remain necessary. All **12 DAG holds remain**. This resolves the authority classification, not operational proof.

## Evidence I could not obtain

- A successful run of the exact requested pytest command: sandbox permissions prevent temporary-file creation.
- Runtime evidence from a configured Agent Ready CLI/MCP installation. Version conclusions use local documentation and static source, not a live handshake.
- Executed source-adapter, split-scenario or admission-conformance proof. These remain designed obligations; no provider inference was performed.

No newly identified issue requires a Founder design decision; the remaining work is bounded repair.

DV: REPAIR_REQUIRED