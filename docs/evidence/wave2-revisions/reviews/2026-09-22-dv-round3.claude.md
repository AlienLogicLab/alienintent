# Design Verification round 3 — 2026-09-22

Reviewed branch `wave2/respecify-2026-09-22` at `74516f4`, read-only, worktree clean. No files modified, no provider inference. Pointers: **S** = `docs/evidence/wave2-specified-requirements.json`, **C** = `docs/evidence/wave2-design-contracts.json`, **D** = `docs/evidence/wave2-dependency-dag.json`, **B** = `docs/evidence/wave2-candidate-bius.json`, **MD** = `docs/evidence/wave2-specified-requirements.md`. The full report is also saved at `/home/netmarine/.claude/plans/independent-design-verification-buzzing-feigenbaum.md`.

## Repair verification

| Finding | Status | Evidence | New defect? |
|---|---|---|---|
| DV-9 | **PARTIAL** | AC-05 is consistent in all three places: `S#/candidates/0/acceptance_criteria/4`, `C#/contracts/0/acceptance_design_trace/4/design_probe` (C:472) and MD:106. MD:15 now states ownership settled under A1 with POSTW1-DECIDE-004A closed, and that closure is authorized (`docs/operations/post-wave1-program/program-state.json:252-286`, DONE / RESOLVED). New rule `markdown_render_equivalent` (check_wave2_specify.py:128-160) rejects the HEAD~1 Markdown with 50 mismatches and passes the live one. **Not done:** round 2's "refresh the SF-REQ-015 dependency/prerequisite rendering". MD:359, MD:372, MD:386 and MD:400 still render pre-repair text against `S#/candidates/4/scope/4`, `/dependencies/4`, `/authority_gaps/0` and `/observability_evidence/1`. | Yes: DV-13. The rule covers only inventory rows and acceptance criteria, so it passes SF-REQ-015 for the wrong reason outside those fields. |
| DV-10 | **PARTIAL** | JSON dedup complete: `S#/candidates/4/scope/4`, `C#/contracts/4/source_specification/authority_conditions/0` (C:1634) and `C#/shared_contracts/authority_boundaries` (C:128) reference `#/contracts/4/ownership_statement`; only C:1749 carries the paragraph (the `session_b` notes are revision history). Cross-document pointers in S and `D#/nodes/12` (U10, D:909) are qualified with the filename; no unqualified `#/contracts/4` remains in S, D or MD. `C#/contracts/4/adapters/1-2` no longer restate limits or exit codes; behaviour and failure semantics retained. | The Markdown still carries three ownership copies (MD:359, 372, 386) and one provenance copy (MD:400). Same root cause as DV-13. |
| DV-11 | **SATISFIED** | `C#/contracts/4/provenance_contract/version_source` (C:1741-1744): `product_version` from installed-distribution metadata, resolved from the configured executable or MCP server command path, for CLI and MCP alike; `mcp_handshake` recorded separately as `sdk_version`, never a substitute; UNKNOWN still blocks admission. Distribution name is correct (`/mnt/d/Projects/agent-ready/pyproject.toml:6` `name = "agent-ready"`); `agent_ready/mcp_server.py:38` constructs `Server("agent-ready")` without a product version and the CLI has no `--version`. Matches README:90-92 and UL:124. | None found. Design-level only; no live handshake. |
| DV-12 | **SATISFIED** (advisory) | check_wave2_dag.py:145-167: settlement needs a dated record with terminal status; a node carrying a settled gap fails; the blocker is validated against `wave2a_completion_node` = "A" (D:3734; A carries `R2-GAP-051-EDGE-AUTHORITY`). Round 2's three live-DAG mutations now fail with the intended messages; three matching tests pass; live statistics (23/15/8) match the nodes. | Advisory: DV-14. |

The two-kind AC-05 reading remains faithful to the amended factory plan (docs/decisions/alienintent-software-factory-plan.md:920 lists supported kinds and a substitution design target; no per-kind fixture count).

## New findings

**DV-13 — MATERIAL — The active Markdown still contradicts the JSON for SF-REQ-015 outside the acceptance criteria, and the new render rule cannot detect it.**

- MD:359 vs `S#/candidates/4/scope/4`: MD renders the full ownership paragraph; S renders the pointer.
- MD:372 vs `S#/candidates/4/dependencies/4`: MD lists the ownership paragraph as a dependency; S names "Agent Ready product (Architecture Authority A1)".
- MD:386 vs `S#/candidates/4/authority_gaps/0`: MD presents the ownership statement as the authority gap; S says "None settled-pending; surviving prerequisites are …". This is the same "ownership still open" misreading DV-9 fixed at MD:15, one section lower.
- MD:400 vs `S#/candidates/4/observability_evidence/1`: MD retains the duplicated provenance paragraph DV-5/DV-10 removed from S.

Verified blind spots in `check_render` (check_wave2_specify.py:143-158): mutating `scope/4` still passes; appending an MD-only `SF-REQ-015-AC-99` still passes (one-directional). The commit message's claim that the rendering "can no longer contradict the JSON" is overstated.

**Exact repair:** regenerate the SF-REQ-015 Scope, Dependencies, Authority gaps and Observability blocks from S. Extend `check_render` to every selected candidate's list-valued spec fields and to the reverse direction for acceptance IDs. Negative controls: the live MD:386 line as a stale authority-gap fixture, and an MD-only extra acceptance line. Prove red on the live Markdown before regenerating.

**DV-14 — ADVISORY — `root_register_consistent` degrades silently when the completion node is absent or unknown.** Deleting `wave2a_completion_node` with a wrong blocker passes; a nonexistent node id passes (check_wave2_dag.py:161-165 falls back to "some node carries it"). `D#/authority_gaps/2` also keeps top-level `RETURN_TO_SPECIFY` beside a terminal resolution, tolerated by design. **Exact repair:** when a blocker is set, require the node key to exist and resolve; add both negative controls.

**DV-15 — ADVISORY — POSTW1-DECIDE-004A remains open inside S while MD:15 and program state call it closed.** `S#/authority_gates/1` has no resolution record; `S#/candidates/13/rejection_reason` (MD:40) still defers "pending POSTW1-DECIDE-004A" with `canonical_owner: UNASSIGNED`; `D#/proof_fixtures/12` (FX-U10) still says the decision "assigns G1/G2". Pre-existing; the deferral itself stays correct, but its recorded reason is now wrong. **Exact repair:** add a dated RESOLVED record citing Founder decisions v0.1 §2 / A1 and program state; reword the rejection reason to out-of-scope under A1 / SF-REQ-029; regenerate MD:40; reword FX-U10. Keep original text as history.

## Checks

| Check | Result |
|---|---|
| `check_wave2_specify.py …specified-requirements.json` | PASS, exit 0; 41 evaluated, 10 selected; 8 checks |
| `check_design_contracts.py …design-contracts.json` | PASS, exit 0; 10 contracts |
| `check_wave2_dag.py` | PASS, exit 0; 46 nodes |
| `check_wave2_bius.py` | PASS, exit 0; 46 BIUs |
| `check_canonical_vocabulary.py` | PASS, exit 0; 17 active artifacts, none absent |
| `TMPDIR=/tmp python3 -m pytest tools/evidence -q -p no:cacheprovider -s` | **175 passed**, exit 0 (not blocked this run) |
| `git diff --check HEAD~1 HEAD` | PASS, exit 0 |

**Red controls.** `check_render` against the HEAD~1 Markdown: 50 failures. `check_dag` against the pre-round-1 DAG (HEAD~2): 4 failures; against HEAD~1: pass, because that artifact was already correct and only lacked the new key. Round 2's three DAG mutations and render mutations of a criterion, title or added criterion: all red.

**Conservation.** Nine files changed in HEAD~1..HEAD. S: three pointer qualifications and one ownership reference. C: six locations, original provenance and ownership text preserved verbatim at C:1737 and C:1749. D: one pointer qualification plus the new key; no node, edge, capability, gap ref, status or fixture changed. MD: intro sentence, AC-05, AC-07, and additive "Verification:" suffixes on 49 criteria; nothing removed. `B` is byte-identical across both repair commits; `B#/bius/0` (WO-220101) differs from `main` only through the earlier re-SPECIFY commit 56d539e, outside this round.

**Round 2's open items.** DV-3 and DV-8 closed. DV-2 and DV-5 closed at the lines round 2 cited, but the same SF-REQ-015 section still holds four stale renderings (DV-13), so not fully closed.

## Evidence I could not obtain

- Runtime evidence from a configured Agent Ready CLI/MCP installation; version conclusions rest on static source and public docs.
- Executed source-adapter, split-scenario or admission-conformance proof; these remain designed obligations.

**Readiness for a fresh native Agent Ready assessment of WO-220101:** not yet. The active Markdown for SF-REQ-015, the requirement defining the assessment integration, still presents ownership as an open authority gap and lists stale dependencies. The repair is a bounded regeneration plus checker extension; no Founder decision is required.

DV: REPAIR_REQUIRED
