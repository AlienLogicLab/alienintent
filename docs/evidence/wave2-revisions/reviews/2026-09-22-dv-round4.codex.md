# Design Verification round 4 — 2026-09-22

Reviewed `wave2/respecify-2026-09-22` at `dfeca645fc015cb7cf7f4facdc7991f9ae55adea`, against parent `74516f4`. Worktree clean before and after. No files modified; no provider inference.

Evidence shorthand, all under `docs/evidence/`:

- **S**: `wave2-specified-requirements.json`
- **MD**: `wave2-specified-requirements.md`
- **C / CMD**: `wave2-design-contracts.json / .md`
- **D**: `wave2-dependency-dag.json`
- **B**: `wave2-candidate-bius.json`

## Repair verification

| Finding | Status | Evidence | New defect? |
|---|---|---|---|
| DV-13 | **PARTIAL** | The four stale fields are repaired: MD:351, 364, 378, 392 match `S#/candidates/4/{scope/4,dependencies/4,authority_gaps/0,observability_evidence/1}`. Current checker rejects parent Markdown with exactly four corresponding failures. Stale authority-gap and MD-only AC-99 controls fail correctly. However, field placement and contradictory duplicate criteria remain unchecked; see DV-16. | Regeneration loses source navigation, and existing negative tests lose discrimination: DV-17/DV-18. |
| DV-14 | **SATISFIED** | `tools/evidence/check_wave2_dag.py:158–167` requires a resolvable completion node when a blocker exists. Both live-DAG mutations—missing key and nonexistent node—fail specifically on `wave2a_completion_node`. Tests: `test_check_wave2_dag.py:213–222`. Live policy names A, which carries the stated blocker. | None found. |
| DV-15 | **SATISFIED at the cited locations** | `S#/authority_gates/1/resolution_2026_09_22` records RESOLVED; `S#/candidates/13` and MD:40 replace pending ownership with A1-based exclusion. `D#/proof_fixtures/12/scenario` acknowledges settled ownership. Original rejection/scenario text is retained verbatim in history. Authority agrees with Architecture Authority:650–659 and program-state:252–286. | The repaired fixture now differs from its active compiled BIU copy; stale downstream ownership gates also remain. See DV-19. |

**DV-9/DV-10:** their specifically identified stale SF-REQ-015 Markdown renderings are now closed. All ten selected candidate sections exactly match the current renderer. I found no remaining substantive contradiction between those sections and S.

CMD still contains contrary historical wording—for example CMD:873 says ownership assignment is pending—but CMD:3–8 explicitly supersedes that companion with the revised JSON. This is marked history, not an additional active design contradiction.

## New findings

**DV-16 — MATERIAL — `markdown_render_equivalent` still accepts materially contradictory renderings.**

`tools/evidence/check_wave2_specify.py:209–217` searches each expected line anywhere inside the candidate section; `_heading` is unused. Lines 219–223 reject unknown acceptance IDs but permit contradictory duplicates of known IDs.

Two in-memory mutations of the live Markdown both returned **PASS, zero failures**:

1. Exchange SF-REQ-015’s `Dependencies` and `Non goals` headings. Required dependencies become non-goals.
2. Add `SF-REQ-015-AC-01` with “Always admit an unauthenticated READY stub,” retaining the genuine AC-01 below it.

These are remaining checker blind spots, not contradictions currently present in MD.

**Exact repair:** compare each named subsection with its complete expected contents, or compare the complete canonical section after restoring provenance rendering. Reject duplicate/conflicting acceptance entries. Add both controls with passing unmodified baselines.

**DV-17 — ADVISORY — Regeneration weakens provenance navigation.**

The commit removes all ten selected candidates’ `Sources:` blocks and replaces four concrete revision pointers and linked revision notes with `/candidates/<i>/revision_2026_09_22` and a directory name.

Evidence: MD:136, 272, 405, 530; renderer at `tools/evidence/check_wave2_specify.py:172–176`; parent-to-HEAD Markdown diff.

The authoritative JSON provenance survives, so this is not loss of underlying source evidence. It nevertheless fails the requested “nothing pre-existing lost or weakened” standard for the rendering.

**Exact repair:** render source links from each candidate’s provenance and supply its actual JSON index and revision-note link. Verify those locators resolve.

**DV-18 — ADVISORY — Existing rendering regression tests now pass for the wrong reason.**

`tools/evidence/test_check_wave2_specify.py:192–206` still uses `_rendered`, which emits no candidate sections. The checker exits candidate processing at `check_wave2_specify.py:202–204`.

For the stale-AC-05 test, I reproduced identical results before and after mutation: five “no section” failures, with no criterion failure. The inventory test similarly permits unrelated missing-section failures to satisfy its assertion.

**Exact repair:** use complete, initially passing renderings for both tests; assert the specific mutated criterion or inventory diagnostic. Retain the intentional missing-section test separately.

The actual checker still rejects a stale AC-05 inserted into the live Markdown; the regression is in durable test discrimination.

**DV-19 — MATERIAL — Closed ownership remains an active prerequisite in compiled WO-220210.**

The closure is not propagated consistently:

- `D#/nodes/12`: empty authority gates; `completion_status = PROCEEDS_REGARDLESS`.
- `D#/proof_fixtures/12/scenario`: ownership settled.
- `B#/bius/12` (`WO-220210`): `authority_gate_refs` and `release_policy/authority_gate_refs` still contain `POSTW1-DECIDE-004A`; `planning_eligibility = SEPARATE_AUTHORITY_REQUIRED`.
- Its `scope/extent`, `acceptance_criteria/0`, and `verification_obligations/proof_fixture/scenario` still require G1/G2 assignment (B:4882, 4906, 4916). `partial_work_that_proceeds` says transport/schema implementation waits on an assigned owner.

The stale candidate gate predates this repair; the revised DAG fixture now also diverges from its compiled copy. B identifies itself as authoritative candidate compilation, not a superseded historical rendering.

**Exact repair:** reconcile WO-220210 and affected downstream copies/gate propagation with the recorded closure. Preserve old text as history and retain capability, producer/version, contract-compatibility, release and operational prerequisites. Keep `B#/bius/0` unchanged. Add a consistency control for the settled gate and fixture copies.

## Checks

All shell invocations used `rtk proxy`.

| Check | Observed result |
|---|---|
| `python3 tools/evidence/check_wave2_specify.py docs/evidence/wave2-specified-requirements.json` | PASS; exit 0; 41 evaluated, 10 selected, eight checks |
| `python3 tools/evidence/check_design_contracts.py docs/evidence/wave2-design-contracts.json` | PASS; exit 0; 10 contracts |
| `python3 tools/evidence/check_wave2_dag.py` | PASS; exit 0; 46 nodes |
| `python3 tools/evidence/check_wave2_bius.py` | PASS; exit 0; 46 BIUs |
| `python3 tools/evidence/check_canonical_vocabulary.py` | PASS; exit 0; 17 active artifacts, none absent |
| Requested pytest, with `PYTHONDONTWRITEBYTECODE=1` added | **179 passed, one setup error; exit 1** |
| `git diff --check HEAD~1 HEAD` | PASS; exit 0 |

**Negative controls:** parent Markdown produces four intended failures; individual scope/dependency/observability mutations, stale authority-gap replacement, extra AC-99 and live stale AC-05 each fail specifically. Missing and unknown DAG completion nodes each fail specifically. Live artifacts pass. DV-16 records the controls that incorrectly pass.

**Conservation:** eight files changed. S changes only candidate 13’s disposition/owner/history and the gate-resolution record. D changes only FX-U10’s scenario/history; nodes, edges and planning extents are unchanged. Both historical strings are preserved verbatim. C, B and historical assessment/verdict artifacts are untouched. B is byte-identical to its parent, including `B#/bius/0` / WO-220101. Full conservation is qualified by DV-17 and DV-18.

**Readiness for fresh native assessment of WO-220101:** I found no new S0-specific scope or authority defect, and its preserved extent remains independent of the U10 ownership gate. However, I cannot mark the overall design baseline verified while DV-16 and DV-19 remain. These require bounded consistency repairs, not a new Founder decision. A future native assessment must receive pinned actual candidate text through Agent Ready’s supported interface; this review does not establish runtime availability or authorize release.

## Evidence I could not obtain

- A complete passing pytest run: `test_an_assessment_file_missing_from_the_manifest_is_rejected` could not create its temporary fixture. Python reported no usable temporary directory, including `/tmp`, under the read-only sandbox.
- Historical proof that the author ran tests before regeneration. I independently reproduced the current checker’s red result against parent Markdown.
- Configured Agent Ready runtime identity, CLI/MCP execution, or a native assessment result; provider inference was prohibited.
- Executed implementation/conformance proofs; the artifacts describe planned obligations.

DV: REPAIR_REQUIRED