The verification is complete. The full report is saved at `/home/netmarine/.claude/plans/independent-design-verification-swirling-sunrise.md` (the only writable path in plan mode). Nothing in the repository was modified. The report follows in full.

# Design Verification — Wave 2 re-SPECIFY 2026-09-22

Reviewer: fresh, read-only, branch `wave2/respecify-2026-09-22` at `56d539e` (merge base with `main` = `764e532`; the only `main`-side commit since is `96a331b`, the decision-bundle classification). No files modified, no provider inference run. Pointers: S = `docs/evidence/wave2-specified-requirements.json`, C = `docs/evidence/wave2-design-contracts.json`, D = `docs/evidence/wave2-dependency-dag.json`, B = `docs/evidence/wave2-candidate-bius.json`, MD = `docs/evidence/wave2-specified-requirements.md`, plan = `docs/decisions/alienintent-software-factory-plan.md`, AA = `docs/architecture/alienintent-architecture-authority-2026-09-19.md`.

## Findings

**DV-1 — MATERIAL — The DAG's own root register still says the settled allocation gap blocks Wave 2A, and its statistics no longer match its nodes.**
FACT: `D#/authority_policy/wave2a_completion_blocker` = `"R1-GAP-013-ALLOCATION"`; `D#/authority_gaps[2]` still has `status: RETURN_TO_SPECIFY`, `blocks: "Initial decomposition implementation and Wave 2A completion claims"`; `C#/authority_gaps[2]` likewise (`docs/evidence/wave2-design-contracts.json:3000-3004`). Meanwhile `D#/nodes/13/completion_predicate` says "compiler derivation is settled" and no node references the gap (grep over nodes: zero hits). FACT: `D#/statistics` still reports `proceeds_regardless: 14`, `separate_authority_only_nodes: 9`; recounted from `completion_status` the values are 15 and 8, because U10 moved SEPARATE_AUTHORITY_REQUIRED → PROCEEDS_REGARDLESS. `check_wave2_dag.py` does not recount statistics or validate `authority_policy`, so it stayed green. INFERENCE: a reader or tool consulting the root register (as FINAL-REPORT §4 and `program-state.json` do) is told the gap is open and blocking; the same artifact says it is settled. Session A's "historical record retained" rationale does not apply: `#/authority_gaps` and `#/statistics` are the artifact's current status register, not immutable evidence.
Repair: (1) append `resolution_2026_09_22` {status: SETTLED_BY_AMENDMENT, authority: plan SF-REQ-013 amendment} to `D#/authority_gaps[2]` and `C#/authority_gaps[2]` without deleting the original text; (2) set `wave2a_completion_blocker` to `R2-GAP-051-EDGE-AUTHORITY` (the gap that actually still holds A per `D#/nodes/13/authority_gap_refs`); (3) recompute `D#/statistics`; (4) add a recount rule to `check_wave2_dag.py` (proven red by the current file).

**DV-2 — MATERIAL — The Markdown rendering was not regenerated, contradicts the JSON in three places, and the vocabulary checker never reads it.**
FACT: `MD:31` still renders SF-REQ-039 as "Fake-agent / offline factory proof" (S `/candidates/6/title` = "Deterministic Test Worker"); `MD:29` renders SF-REQ-015's old selection reason "Defer schema ownership rather than invent it" (S `/candidates/4/selection_reason` now says A1 settles it); `MD:826` READY-lane failure reads "BLOCKED, NEEDS_CLARIFICATION, SPLIT_RECOMMENDED, stale verdict…" with no historical label, while `S#/lane_semantics/READY/failure` reads "HOLD, CLARIFY, SPLIT, stale verdict…". FACT: `tools/evidence/check_canonical_vocabulary.py:37-52` `ACTIVE_ARTIFACTS` lists the two JSON files, not the Markdown, so the green `agent_ready_dispositions_exact` result does not cover MD. Session B's note claims "Markdown entry/inventory terminology refreshed" (`2026-09-22-respecify-015-039.md:31`); that is not true for the inventory table. Related, ADVISORY: `docs/evidence/wave2-design-contracts.md` (companion prose, unchanged on this branch) carries 14 hits for `OWNER_ASSIGNMENT_HOLD | Fake-agent | INITIAL_ONLY | R1-GAP-013-ALLOCATION`.
Repair: regenerate MD's inventory table and lane section from S; add MD (and the contracts companion, or a "superseded, see JSON" banner) to `ACTIVE_ARTIFACTS`, or add a render-equivalence check that fails when a selected candidate's title/selection_reason in MD differs from S.

**DV-3 — MATERIAL — SF-REQ-011 AC-05 makes acceptance depend on source kinds whose requirements are downstream of SF-REQ-011 and not yet specified.**
FACT: `S#/candidates/0/acceptance_criteria/4` requires translating "representative Source Records for every named source kind", verification "Versioned adapter conformance fixtures for all listed kinds"; `C#/contracts/0/deterministic_enforcement_opportunities/1` (BLOCKING): "All named source-kind fixtures retain seven-field provenance". The named kinds include SF-REQ-041–044 (plan:1129-1130, Wave 2B, natively blocked-by 011/013) and SF-REQ-055, whose Source Record forms are defined nowhere. FACT: the same specification says the list "is an extensibility/support contract, not a new Wave assignment or implementation of every intake product" (`S#/candidates/0/scope/3`). FACT: hexagonal G27 sets the bar at two conforming implementations (`hexagonal-contracts.md:35,49`). INFERENCE: as written, SF-REQ-011 cannot be accepted in Wave 2 without inventing fixture shapes for Wave 2B intake and 055, which is the "invented scope" the same AC forbids, and it creates a scheduling cycle (011 acceptance ← 041–044 shapes ← 011). The amendment's word is "Supported source kinds include…", a design target for the port, not an obligation to fixture nine adapters now.
Repair: AC-05 = port contract plus all seven provenance fields proven on at least two heterogeneous kinds, one a single vendor bound in both roles (e.g. GitHub Issues as source, GitHub Projects as work state) and one non-vendor kind (local files / structured documents); every further named kind acquires a conformance case when its own requirement is specified, recorded as an extension obligation in the enforcement entry. Keep the nine kinds in scope as the support contract they are.

**DV-4 — ADVISORY — SF-REQ-015's implementation location contradicts A2 more sharply than 011/013's does, and the FD-02 follow-up does not cover it.**
FACT: `C#/contracts/4/bounded_context` = `execution_coordination`; AA A2 (`:661-672`) assigns "Agent Ready invocation through a port, processing of the four dispositions" to Requirements / Planning; the FD-02 refinement (`pre-python-gate/founder-decisions.md:27-35`) records the open module-mapping question only for "SF-REQ-011..013 … `context_assembly`". The contract's `domain_owner` does say the field is a legacy location and the mapping is SF-REQ-051 work. INFERENCE: honest, but the follow-up record that will drive SF-REQ-051 omits 015. Not a Founder question (FD-02 delegates the mapping to Design Contract work).
Repair: add SF-REQ-015 (lint + `ReadinessAssessment` port) to the FD-02 follow-up sentence, or record the same follow-up in `C#/shared_contracts/architecture_scope`.

**DV-5 — ADVISORY — One normative paragraph is copy-pasted into at least eight places, and two SF-REQ-015 fields hold prose that is not what the field means.**
FACT: the 130-word producer-provenance paragraph appears verbatim in `S#/candidates/4/acceptance_criteria/6`, `/observability_evidence/1`, `C#/contracts/4/state`, `/evidence`, `/design_decisions/1`, `/acceptance_design_trace/6/design_probe`, `D#/nodes/12/completion_predicate`; the ownership paragraph appears in `S#/candidates/4/scope/4`, `/dependencies/4`, `/authority_gaps/0`, `C#/contracts/4/domain_owner`, `/design_decisions/1`, `/source_specification/authority_conditions/0`, `C#/shared_contracts/authority_boundaries`, `D#/nodes/12/completion_predicate`. `S#/candidates/4/dependencies/4` is not a dependency and `/authority_gaps/0` is not a gap. FACT: adapters restate Agent Ready's numeric limits and CLI syntax (`C#/contracts/4/adapters/1-2`). INFERENCE: SWF-25 "unnecessary mechanism/complexity"; verbatim copies drift independently, and the numbers will be wrong the day Agent Ready changes them.
Repair: one `C#/contracts/4/provenance_contract` object referenced by pointer; `authority_gaps` lists only the surviving prerequisites; `dependencies[4]` becomes "Agent Ready product (A1)"; adapter text references "the published contract at the bound package version" with numbers living in the conformance fixture.

**DV-6 — ADVISORY — The "split recommendation" scenario has no carrier in the production worker protocol the Deterministic Test Worker must share.**
FACT: plan:1108 lists the scenario and requires "the same worker-facing port/protocol used by production". `S#/candidates/6/acceptance_criteria/8` and `C#/contracts/6/failure_modes/6` route the recommendation to SF-REQ-013/015. FACT: `src/alienintent/execution_coordination/ports/worker_provider.py:20-32`: `WorkerOutcome.kind` is a free string with constructors `success` and `authority_block`; the coordinator dispatches on `"success"`, `"rework"`, `"authority-block"` (`factory_coordinator.py:207-278`). No split-recommendation outcome exists. INFERENCE: either the scenario rides the existing `authority-block`/`HumanDecisionRequired` escalation (then say so), or it needs a new outcome kind, a real-worker contract change that `C#/contracts/6/design_decisions/0` forbids under 039 alone and that R1-GAP-039-REAL-OUTCOME holds. The contract is silent. UNKNOWN: whether `HumanDecisionRequired` can carry a structured recommendation without a protocol change.
Repair: state in `C#/contracts/6/ports/0` that a scripted split recommendation is emitted as the existing escalation outcome and no new `WorkerOutcome.kind` is introduced; if that proves insufficient, mark AC-09 conditional on R1-GAP-039-REAL-OUTCOME like AC-01.

**DV-7 — ADVISORY — U9's completion predicate now requires the SF-REQ-013 transaction but U9 has no edge to U8.**
FACT: `D#/nodes/11/completion_predicate` ends "SPLIT via SF-REQ-013 then lint/reassess each result"; U9 has no dependencies and stays PROCEEDS_REGARDLESS; U8 owns the transaction and is GAP_BLOCKED on R2-GAP-051-EDGE-AUTHORITY. INFERENCE: as worded, U9 cannot complete until U8 does, an implicit dependency the graph does not carry.
Repair: word U9's obligation as the handoff boundary ("routes SPLIT to the `SplitTransaction` port and refuses to proceed without a materialized result set; the transaction itself is U8").

**DV-8 — ADVISORY — How the Agent Ready version is "established" is not a documented public surface, so the fail-closed rule would block every admission.**
FACT: `C#/contracts/4/state` requires "Agent Ready version (UNKNOWN if unestablished, blocking admission)" from "the configured Agent Ready executable/MCP server and its package metadata". FACT: the README documents no `--version` and does not document MCP `serverInfo`; Agent Ready's UL says "consumers must retain the package version alongside the raw assessment" (`agent-ready/docs/ubiquitous-language.md:124`); "the v0.1 contract is versioned by the package release only" (`README.md:90-91`). INFERENCE: installed-distribution metadata of the configured environment is a legitimate non-private surface, but the contract should name it and the MCP `initialize` `serverInfo.version` equivalent, or an implementer will guess or read private modules.
Repair: name the two mechanisms and record `version_source` beside the version.

## Answers 1–9

**1. Conformance.** Checked each amended obligation at plan:920, 932-934, 946-950, 1103-1108 against the revised objects.
- SF-REQ-011: all seven present: port distinction and one-vendor-two-adapters (`S0/scope/2`, `C0/ports/0,3`), source kinds (`S0/scope/3`), Source Record translation (`C0/ubiquitous_language`, `/ports/0`), seven-field provenance (`C0/state`; negative probes `C0/deterministic_enforcement_opportunities/1`), non-identities (`C0/identities`), substitution (`S0/AC-06`), semantic owner (`C0/domain_owner`). FACT. Over-reach in AC-05: DV-3.
- SF-REQ-013: ownership of the split/replan mutation, judgment/mutation boundary, freeze + 100 % four-category mapping, no-loss invariant, deterministic identity rewrites, lineage, invalidate/materialize/resubmit each result, stated grammar never sort order, compiler-derived initial decomposition, obligation-mapping term, Phase 5 as candidate + SWF-33 precedent, each located (`S2/scope/0-5`, `/AC-05..08`, `C2/ports/0,3`, `/state`, `/transitions/0,3,4`, `/identities`, `/design_decisions/0,1,4`, `/ubiquitous_language`). FACT. Compile-time "Allocation" is gone from S2/C2 and D; only the execution-time sense survives. FACT.
- SF-REQ-015: product-vs-integration ownership, `ReadinessAssessment.assess(candidate_work_unit)` with CLI and MCP adapters, domain ignorance and no private import / rubric copy / duplicate logic, exhaustive four dispositions with BLOCKED excluded, the four process routes, no rewrite of old dispositions, execution failure ≠ disposition, bootstrap vocabulary retained verbatim and labelled, all present (`S4/scope/1,3,4`, `/AC-02,05,06,07`, `C4/ports/2`, `/adapters/1,2`, `/transitions/0-3`, `/ubiquitous_language/Disposition`). FACT. Adapter facts match the public README (exit 0/1/2 at `README.md:66-67`; `assess_work_unit` with `text`+`provider`, extra keys rejected, `isError`, limits at `README.md:71-95`; `provider_evidence` Codex-only in `assessment.schema.json`). FACT.
- SF-REQ-039: title, identity/priority/wave unchanged, same production port/protocol with adapters on both sides and no rename of `WorkerProvider`, non-intelligence, no lifecycle shortcuts, all fifteen scenarios present with node allocation (`S6/scope/2`, `/AC-07..09`, `C6/acceptance_design_trace/*/scenario_node_allocation`). FACT. Carrier for one scenario unclear: DV-6.

**2. Conservation.** Scripted baseline-vs-HEAD field diff of all four candidates and contracts, shared contracts, DAG and BIUs: no non-selected candidate, contract, BIU or top-level record changed except the two `revision_2026_09_22` blocks and `\u2013/\u00a7` re-escaping (byte-level only). FACT. Removed holds are exactly SPLIT-G1 and R1-GAP-013-ALLOCATION (settled by plan:932-934) and the G1/G2 gate POSTW1-DECIDE-004A / `OWNER_ASSIGNMENT_HOLD` (settled by AA A1 `:650-659`, plan:946); grep over `D#/nodes` finds zero remaining references to any of the three. FACT. Surviving: R1-GAP-MONITOR-HOST on 12 nodes (C5, B1, B2, B5, B6, B8, R3, R4, R5, R7, B9, R9); R2-GAP-051-EDGE-AUTHORITY on U8, A, B3, B6, R6, R7 and `C#/shared_contracts/context_dependency_direction`; both 039 gaps on B3, B6, R6, R7 and `C#/contracts/6/authority_gap_refs`; G+I and SF-REQ-056 ratification in `C#/shared_contracts/authority_boundaries`. FACT. Replaced, not weakened: 013 AC-02 is a superset; 013 AC-05 replaced an exclusion the amendment supersedes; 015 AC-02 is a superset of the old four-route text; 015 `operational_constraints/0` replaced "not a claimed exhaustive external schema" as required; 015 `failure_modes/1` broadened to the provenance hold. 013 `recovery` dropped the literal "never modify approved BIUs during recovery", but `transitions/3` and `non_goals/0` conserve it. INFERENCE: nothing lost. The stale root registers (DV-1) and Markdown (DV-2) are the conservation defects.

**3. Boundary.** No import, copy or duplication of Agent Ready (`C4/non_goals/specified/0`, `/architecture_fitness`, `/design_decisions/2`; `src/` has no Agent Ready import). FACT. Integration only through `ReadinessAssessment.assess` with CLI and local-MCP adapters bound in composition. FACT. Dispositions exactly READY/CLARIFY/SPLIT/HOLD, BLOCKED excluded, matching `assessment.schema.json#/properties/disposition/enum`. FACT. Every occurrence of BLOCKED/NEEDS_CLARIFICATION/SPLIT_RECOMMENDED in S, C and D (14 hits) is inside a sentence labelled bootstrap-assessor/historical/terminology-repair. FACT. The Markdown is the exception (DV-2). Compile-time Allocation is gone.

**4. Reuse Before Build.** New capabilities: `RequirementSource`, `SplitTransaction`, `ReadinessAssessment` with two adapters, provenance capture, test-worker scenarios. `src/alienintent` has no existing RequirementSource / ReadinessAssessment / AssessmentFeedback. FACT. Reused: `OperationalStore`/`SQLiteOperationalStore`, `EvidenceRepository`, `BiuContract`/`BudgetPolicy`, `WorkerProvider.start/read_back/cancel`, `FactoryCoordinator`, LRN-002. FACT. Agent Ready is consumed as the product; `tools/orchestration/readiness_assessment.py` is correctly labelled an interim programme adapter and `tools/evidence/check_assessment_producer.py` negative shape evidence, not authentication (`C4/design_decisions/2`); both exist. FACT. Producer identity and version are recorded and checked, not shape-inferred: `C4/state`, `/failure_modes/1`, `015-composed-authority` proven-red obligations (schema-perfect surrogate, copied `provider_evidence`, wrong package identity must all fail). FACT. Gap: version-establishment surface unnamed (DV-8).

**5. DAG integrity.** 46 node ids identical in order; no `dependencies` or `owns_capabilities` changed anywhere; `proof_fixtures` unchanged (FX-S0 PLANNED_NOT_EXECUTED); only U7, U8, U9, U10, A, B3, B6, R6, R7 changed and only in title/scope/predicate/gap-ref/gate-ref/metadata. FACT. Each predicate change traces to an amendment (U7/U8/A to plan:932-934; U9/U10 to plan:946 and A1). INFERENCE: justified. U10's move to PROCEEDS_REGARDLESS is consistent with `D#/authority_policy/proceeds_regardless_definition`, and its `external_prerequisite` kind still carries the availability proof obligation; nothing claims completion or release. FACT. Root register and statistics stale: DV-1. U9 implicit dependency: DV-7.

**6. S0 / WO-220101.** Scripted diff of `B#/bius/0`: only `fixed_decisions` and `revision_2026_09_22` changed; `scope`, `acceptance_criteria`, `architecture_constraints`, `budget`, `candidate_custody`, `release_policy`, `verification_obligations` (FX-S0), `assessment_status` byte-identical. FACT. `fixed_decisions/0` keeps the stable pointer `C#/contracts/6`, gains `source_revision`, `title: "Deterministic Test Worker"`, and one appended decision confining S0 to current-interface substrate. No "fake" token remains in the S0 object. FACT.

**7. Design authority.** Checked against AA §45 (`:603-640`), SWF-25's must-not-decide list, A2/A3/A5, FD-01/FD-02 refinements. Nothing decides product intent, a new requirement, a priority or a wave (`priority_or_wave_invented: false`, `founder_ratification_required: false` on all four; source kinds and scenario matrix come from the amended text). FACT. Candidate mechanisms (Phase 5 grammar, lowest-unused-number reservation, child→parent DONE edges, atomic apply) are labelled candidate design pending independent DV, not Founder approval. The one place a revision reaches beyond its authority is DV-3 (AC-05 effectively schedules Wave 2B intake shapes into Wave 2 acceptance); that is a repair, not a Founder decision. INFERENCE. Nothing requires §45 escalation.

**8. Founder decision-bundle classification.**
- 004A/005A DONE: consistent with plan:932 and A1. FACT.
- R1-GAP-MONITOR-HOST: I agree it is **not a Founder decision**, with two corrections. The gap's question ("who owns the persistent monitor/periodic-scan host, supervision and restart policy") is answered by existing authority: SWF-27/SF-REQ-053 owns persistent monitoring as operational infrastructure and already applied it as `systemd --user` services (`2026-09-20-persistent-control-plane-bounded-coordinator-episodes.md:54-58`); the SF-REQ-037 amendment lists "monitoring host" as an init extension point (plan:1089); SF-REQ-038 validates configured capabilities (plan:1096-1101); AA (b) makes availability project configuration. FACT. Correction 1: AA §14 (`:188-207`) is about agents receiving capabilities to change live environments; it does not speak to supervising the factory's own monitor, and the classification's own source says so (`program-state.json:545`: "§14 … does not name who owns/supervises the host"). Cite SF-REQ-037 + SWF-27 Applied instead. Correction 2: "resolved" means resolved as an authority question; the design still says "Host/supervisor operational owner remains unassigned, returned to SPECIFY" (`C#/shared_contracts/monitor_liveness`) and 12 DAG nodes carry the hold. Closing it is an SF-REQ-053/056 contract revision naming the supervisor mechanism and doctor probe, plus independent DV, so it converts to REQUIRES_OPERATIONAL_PROOF and the DAG holds stay until that lands. INFERENCE.
- R2-GAP-051-EDGE-AUTHORITY as Founder: consistent with `C#/authority_gaps[4]` and A5. FACT.
- R1-GAP-039-ORCHESTRATION / -REAL-OUTCOME as Founder: defensible as requirement-ownership assignment, though AA §4-5 (`:56-81`) already fixes PRODUCER/VERIFIER as domain roles with default verifier independence; what is open is which requirement owns the role-router/RoleOutcomeRecord implementation. A recommendation in the Wave-authorization packet would let the Founder ratify rather than design. Not a misclassification. INFERENCE.
- 006A as REQUIRES_OPERATIONAL_PROOF, 007A R02–R05 and 008A as Founder, 013A concrete half as engineering: consistent with cited authority; nothing engineering-classified is a Founder decision. INFERENCE.
- "S0 is held by exactly one thing: a fresh native Agent Ready assessment": consistent with B and D. FACT. That assessment cannot admit until DV-8's version surface is named, since the 015 contract blocks admission on UNKNOWN version.

**9. Mechanical checks** (run in the worktree; pytest with `TMPDIR=/tmp -p no:cacheprovider`):

| Check | Result |
|---|---|
| `check_wave2_specify.py` | PASS (41 evaluated, 10 selected, 0 failures) |
| `check_design_contracts.py` | PASS (10 contracts, 0 failures) |
| `check_wave2_dag.py` | PASS (46 nodes, 0 failures) |
| `check_wave2_bius.py` | PASS (46 BIUs, 0 failures) |
| `check_canonical_vocabulary.py` | PASS (16 artifacts, 0 failures) |
| `python3 -m pytest tools/evidence -q` | 161 passed in 1.22s |

Green-but-contradicting: (a) `check_wave2_dag.py` passes with `statistics` and `wave2a_completion_blocker` contradicting the nodes (DV-1); (b) `check_canonical_vocabulary.py` passes while `MD:826` presents bootstrap vocabulary as current READY-lane failure handling, because the Markdown is not in `ACTIVE_ARTIFACTS` (DV-2); (c) `no_material_decision_deferred` passes because `deferred_to_implementation` is empty while the 015 context mapping is deferred to SF-REQ-051 (DV-4), correct by the rule's definition but worth knowing.

## Evidence I could not obtain

- UNKNOWN: whether the Agent Ready CLI or MCP server exposes a runtime version; only README, schema and UL were read, and none documents one.
- UNKNOWN: whether `HumanDecisionRequired` can carry a structured split recommendation without a protocol change (DV-6).
- UNKNOWN: the Founder decisions v0.1 source document itself was not read; I verified against its canonicalized form in the plan, AA and UL, which the task names as binding.
- Not run: no provider inference, no live Agent Ready invocation, no execution of any designed fixture. Every "satisfies" is textual coverage, not executed proof.
- `program-state.json` and the classification report exist only on `main`; their consistency with the branch is by reading, not by checker.

DV: REPAIR_REQUIRED
