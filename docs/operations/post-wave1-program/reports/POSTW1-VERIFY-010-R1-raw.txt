{"disposition": "REPAIR_REQUIRED",
 "findings_status": [
  {"id": "DV-1", "status": "CLOSED", "evidence": "The kernel change is no longer authorized inside the test-proof requirement. Two SPECIFY gaps are recorded with resolution actors and explicit blocks (authority_gaps R1-GAP-039-ORCHESTRATION, R1-GAP-039-REAL-OUTCOME, each stating 'Implementers and this designer may not close it'); OutcomeEvidencePort is demoted to 'Conditional target only after R1-GAP-039-ORCHESTRATION and R1-GAP-039-REAL-OUTCOME are resolved'; composition states the separate-role path is blocked and that 'full multi-role acceptance is blocked, not passed by a parallel mock'; design_decisions[0] forbids replacing FactoryCoordinator._completed_for_outcome under SF-REQ-039 alone, reconciling with shared_contracts.authority_boundaries, which now names both gaps as blocking. The live-path hole is closed in the direction the finding demanded: the sandbox/github constructors must return ROLE_OUTCOME_BINDING_HOLD 'before any worker launch, not after every producer success', with 'Existing unmodified profiles are not claimed to enforce this future gate', and 039-composed-lifecycle adds a real-path probe whose proven_red_obligation independently kills the pre-launch binding gate and real-worker record emission. AC-01's probe is explicitly marked blocked rather than claimed satisfiable, and authority_status is downgraded to DESIGN_CANDIDATE_WITH_SPECIFY_HOLD. requirements_created=0 verified: no new Product Requirement is invented to close it."},
  {"id": "DV-2", "status": "CLOSED", "evidence": "The wrong design is withdrawn, not merely annotated. ubiquitous_language redefines Allocation as 'Derived obligation-to-unit extent mapping in a compilation candidate; not an authorized mandatory hand-authored input'; ports mark the allocation representation as 'output/internal, not prerequisite input'; design_decisions[0] states 'Supersedes the mandatory external-allocation design choice... SF-REQ-013 must derive initial decomposition from requirements/design' and 'A validator-only implementation does not satisfy this requirement'; shared_contracts.alternatives_and_selection records the withdrawal. The validator-only path is actively foreclosed, not just deprecated: transitions say 'validation of a hand-authored candidate cannot count as initial compilation acceptance', AC-02's probe says 'Hand-authored allocation fixture success is validation-only, never proof of compilation', and 013-graph-coverage's proven_red_obligation requires 'Bypass the authority-gap gate with a complete hand-authored allocation and require the no-candidate assertion to fail'. The residual product question (derive vs. named external producer, plus identity constraints) is returned as R1-GAP-013-ALLOCATION with derivation selected on stated evidence, which is exactly what the repair instruction permitted. Consequence disclosed rather than hidden: with the gap open compile returns AUTHORITY_GAP_HOLD, so 013 is now blocked rather than narrowed, and authority_status/blocks say so."},
  {"id": "DV-3", "status": "PARTIAL", "evidence": "The missing rule now exists and the fixture is written against it: shared_contracts.context_dependency_direction states an explicit allowed_edges table, target_layers (cross-context imports target domain values or ports only), typed_cycles (execution_coordination MAY reference context_assembly.domain DesignApplicability; reciprocal leaf references, no module cycles) and premise_bridge (evidence_learning adapters MUST NOT import installation or composition; composition/upstream_profile owns the doctor bridge), and 051-architecture-boundary now says it 'Enforce[s] shared_contracts.context_dependency_direction exactly' with named negative fixtures (execution_coordination -> context_assembly.application, evidence_learning -> installation, evidence_learning -> composition, reciprocal leaf cycle) and positive controls. SF-REQ-014's adapters were rewritten to match the premise bridge, so the internal inconsistency the finding cited is gone. What is not closed is the invention the finding was really about: the stated table contradicts imports that exist in the repository today and supplies no exemption, baseline or scope predicate a static checker can apply, so the implementer must still invent the in-scope rule before the BLOCKING gate can run. See DV2-1 for the exact imports."},
  {"id": "DV-4", "status": "CLOSED", "evidence": "SF-REQ-011 gains an identifier_contract section that pins the grammar as a full-token regex (\\A[A-Z][A-Z0-9]*(?:-[A-Z][A-Z0-9]*)*-REQ-[0-9]{3,}(?:[A-Z])?\\Z), states extraction ('capture the entire identifier slot before grammar validation', trim only outer delimiters, 'No substring match may salvage a conforming prefix from a nonconforming token'), and defines typed outcomes: IdentifierShapeConflict(status=UNVERIFIED, reason=UNRECOGNIZED_REQUIREMENT_ID, raw_token, source_ref, locator, grammar_revision=1), distinct MISSING_/AMBIGUOUS_REQUIREMENT_ID and a separate UNAUTHORIZED_REQUIREMENT_NAMESPACE, with 'they count as issues, never silently absent definitions' and recognition explicitly conferring no authority. The PY-09B mechanism is killed by a probe, not a sentence: 011-inventory now requires synthetic SF-REQ-011A / SF-REQ-1000 / TEAM-X-REQ-011 retention and typed rejection of SF-REQ-01 / SF-REQ-011AA / SF-REQ-011-extra in all three prose forms, and its proven_red_obligation is 'Replace full-token grammar with SF-REQ-[0-9]{3}, strip a suffix, or silently drop a malformed token separately: exact identity/typed-reason assertions fail even if baseline 56/53 counts pass' — i.e. the counts fixture can no longer mask the omission. I checked the grammar against the real definition corpus (docs/decisions/alienintent-software-factory-plan.md plus the Canonical-requirement/Recorded-as decision records): 134 conforming tokens, zero nonconforming, so the pinned grammar does not reject any authorized identifier in the sources it is meant to parse. Residual risk in a different corpus is DV2-2, not a failure of the grammar itself."}
 ],
 "episode_age_3600": {"status": "DISCLAIMED", "evidence": "SF-REQ-053 design_decisions[0] (docs/evidence/wave2-design-contracts.json:1487, mirrored at docs/evidence/wave2-design-contracts.md:408) now ends: 'The 3600 seconds is a proposed configurable design default, not a claim that the bootstrap waiter timeout or historical values are canonical; no empirical derivation or inheritance is asserted.' This is the same form as SF-REQ-056's 300 s disclaimer ('not a claim that historical bootstrap 5 minutes is a canonical invariant'), so the asymmetry the verdict flagged is removed. No provenance is claimed, and none is implied — the value stays versioned composition policy, 'not authority or increased budget'."},
 "regression": {"found": true, "detail": "The mechanical properties that previously passed are intact, and I verified them rather than trusting the repair's own numbers: 10/10 contracts, 53/53 acceptance IDs unchanged, deferred_to_implementation empty in all ten, no new bounded context, 18 enforcement opportunities with exactly one added entry (015-composed-authority, the DV-5 repair) and none removed, 9 promotion entries over 7 unique promotions, and every reused promotion keeps its prior strength (LRN-001/002/004/016 BLOCKING; LRN-003/010/019 ADVISORY) — so shared_contracts.enforcement_counting and validation.additional_checks are honest, and the previously-PASS deterministic-enforcement and coverage checks are not weakened. The .md companion was regenerated consistently (it carries the four gaps, the edge table, the identifier contract and the 3600 s disclaimer; no stale mandatory-allocation text). Two things the repair itself introduced do not hold up. (1) The new fixed edge table forbids cross-context edges that exist in production code today, and the enforcement entry that checks it has no exemption mechanism (DV2-1) — notably it forbids installation/application/doctor.py's import, and that module is the very callable the new premise_bridge routes through. (2) The new unconditional token ledger plus blocking identifier-issue semantics has no category for requirement-scoped acceptance identifiers or compound prose references (DV2-2). Both are defects of the repaired sections, not pre-existing. Separately, and not material: identifier_contract.biu_distinction calls the BIU grammar 'Existing', while its source (docs/evidence/wave1-biu-split-replan-design.json:19) calls it a proposed identity grammar and no such validator exists in src/alienintent."},
 "new_material_findings": [
  {"id": "DV2-1", "requirement_id": "SF-REQ-051", "finding": "shared_contracts.context_dependency_direction.allowed_edges forbids four import edges that the repository already contains, and 051-architecture-boundary is BLOCKING on checking 'this exact edge table' with no exemption list, baseline or scope predicate. The table gives installation an empty allow-list and omits execution_coordination -> installation and execution_coordination -> invocation_runtime, but /mnt/d/Projects/alienintent/src/alienintent/execution_coordination/adapters/github_projects_v2.py:20-21 and /mnt/d/Projects/alienintent/src/alienintent/execution_coordination/adapters/github_repository_api.py:14 import alienintent.installation.domain/ports, /mnt/d/Projects/alienintent/src/alienintent/execution_coordination/application/local_artifact_custody.py:11 imports alienintent.invocation_runtime.domain.runtime, and /mnt/d/Projects/alienintent/src/alienintent/installation/application/doctor.py:10 imports alienintent.execution_coordination.domain.webhook_authenticity. The scope sentence ('Fixed rule for all new/changed Wave 2 Python imports... Existing imports outside this slice are not retroactively certified') is not a predicate a static checker can evaluate: /mnt/d/Projects/alienintent/tools/fitness/check_architecture.py walks every .py under --root (python_files/rglob in run()) and has no per-file or grandfathered-edge mechanism.", "why_it_matters": "The implementer cannot land the BLOCKING fixture as specified. They must either edit four production modules (including local_artifact_custody.py, which SF-REQ-039 reuses, and doctor.py, which the new premise_bridge depends on) although shared_contracts.authority_boundaries forbids retroactive Wave 1 changes, or invent an exemption/scope rule the design does not state — which is DV-3's original defect displaced one step: a gate certifying the implementer's own assumption about which files the rule covers. The alternative outcome is the 'gate that cannot run' pattern: the check is quietly dropped from the suite. Repair is cheap and stays inside design authority: record the four existing edges as explicitly named, dated exceptions (or add the edges to the table with a target-layer restriction) and state whether the fixture applies repository-wide or to an enumerated module set."},
  {"id": "DV2-2", "requirement_id": "SF-REQ-011", "finding": "identifier_contract.extraction requires ledgering 'every whitespace-delimited token containing -REQ- in source text', typed_failure makes every nonconforming token an IdentifierShapeConflict that 'block[s] dependent source compilation until explicitly resolved', and no substring may 'salvage a conforming prefix from a nonconforming token' — but the grammar recognises only requirement identifiers, and biu_distinction handles only the requirement/BIU pair. There is no category for requirement-scoped acceptance identifiers or compound references, both of which occur in the project's own decision and specification prose: SF-REQ-053-AC-02 and the other 52 acceptance IDs (used by this very design's acceptance_design_trace and by SF-REQ-013's obligation coverage) appear throughout /mnt/d/Projects/alienintent/docs/evidence/wave2-specified-requirements.md, and compound forms appear at /mnt/d/Projects/alienintent/docs/decisions/2026-09-19-software-factory-wave1-founder-decisions.md:46 ('SF-REQ-001–010'), /mnt/d/Projects/alienintent/docs/decisions/2026-09-20-wave1-release-coordinator.md:83 ('SF-REQ-001/SF-REQ-002') and /mnt/d/Projects/alienintent/docs/decisions/2026-09-20-candidate-worktree-retention.md:61 ('SF-REQ-030/054'). All fail the pinned regex.", "why_it_matters": "Whenever a pinned manifest includes such a source, the inventory produces UNRECOGNIZED_REQUIREMENT_ID issues for legitimate identifiers and blocks compilation for that source, or the implementer invents an AC-suffix rule and a separator-splitting rule that extraction explicitly forbids — reintroducing the unpinned-grammar judgment DV-4 was meant to remove, now on the reject side instead of the drop side. It also collides with the retained 56/53 baseline if any baseline reference is a compound token. The known definition corpus (factory plan and Canonical-requirement/Recorded-as records) is clean, so this is a completeness defect in the new section rather than a certain failure; repair is to state the acceptance-identifier form and its typed disposition, and either exempt multi-reference tokens with an explicit separator rule or state that they are references, not identifier issues."}
 ],
 "verifier_note": "Read-only, pre-implementation, and narrow by instruction: I checked the four findings, the 3600 s value and repair-induced damage, and did not re-run the other eleven checks or re-derive the design from the Phase 8 specification. None of the 18 designed probes exists or could be executed — /mnt/d/Projects/alienintent/src/alienintent/context_assembly and .../evidence_learning contain only __init__.py skeletons and control_plane has only the decision inbox/CLI — so every closure above is a judgment about design text and its stated proven-red obligations, not observed behaviour. I did not re-run tools/evidence/check_design_contracts.py or check_wave1.py, and I did not recompute the 28/29 preserved-source digests or the verdict_sha256; I verified the repair's structural counts (10 contracts, 53 ACs, 18 opportunities, 9/7 promotions, 0 deferred, one added enforcement entry) independently from the JSON and from git diff against HEAD. For DV-4 I tested the pinned regex mechanically against every -REQ- token in the factory plan and the Canonical-requirement/Recorded-as decision records (zero nonconforming) and across docs/ generally (104 distinct nonconforming, dominated by acceptance IDs), which is the basis for DV2-2's scoping; I could not establish which documents the historical 56-referenced/53-defined baseline manifest actually pins, because no manifest artifact is named in the contract. For DV2-1 I confirmed the four offending imports and the checker's whole-tree walk by reading the files, but I did not attempt to implement the proposed context-edge check, so I cannot say how many further violations a full implementation (including re-exports and TYPE_CHECKING imports, which the design requires it to inspect) would surface. Platform behaviours the design still rests on — SQLite BEGIN IMMEDIATE fencing under real multi-process contention, deny-network CI, live worker readback durability — were not tested. Whether the designer holds authority to fix the context edge table at all, given that the architecture authority lists the bounded-context model as unfinished pre-Python work, is an owner question I did not attempt to settle; the repair instruction directed that the graph be stated here."}
