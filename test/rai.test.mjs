import test from "node:test";import assert from "node:assert/strict";import{RAI_CRITIQUE_PROMPT,classifyEvaluation,createCapabilityEpoch,createEvaluation,createFinding,createIteration,enforceManualRaiReviewPolicy,validateIterationLineage}from"../src/domain/rai.mjs";
const epoch=createCapabilityEpoch({provider:"openai",model:"example-model",modelVersion:"2026-09",harnessVersion:"b-disp-test",promptSetVersion:"manual-rai-v1"});
const t0=createIteration({id:"iter-0",artifactId:"artifact-1",ordinal:0,candidateRef:"commit:a",createdAt:"2026-09-15T00:00:00Z",capabilityEpoch:epoch});
const t1=createIteration({id:"iter-1",artifactId:"artifact-1",ordinal:1,candidateRef:"commit:b",createdAt:"2026-09-15T01:00:00Z",capabilityEpoch:epoch,parentIterationId:"iter-0"});
const finding=(id,severity="MATERIAL",status="OPEN",iterationId=t1.id)=>createFinding({id,iterationId,evaluatorId:"jc",severity,summary:`${severity} ${id}`,evidenceRefs:[`evidence:${id}`],status,createdAt:"2026-09-15T01:15:00Z"});
test("iteration zero is the only parentless iteration",()=>assert.throws(()=>createIteration({id:"bad",artifactId:"artifact-1",ordinal:1,candidateRef:"x",createdAt:"2026-09-15T00:00:00Z",capabilityEpoch:epoch}),/require parentIterationId/));
test("lineage is a single contiguous adjacent chain",()=>{assert.equal(validateIterationLineage([t1,t0]),true);const branch=createIteration({id:"iter-1b",artifactId:"artifact-1",ordinal:1,candidateRef:"c",createdAt:"2026-09-15T01:01:00Z",capabilityEpoch:epoch,parentIterationId:"iter-0"});assert.throws(()=>validateIterationLineage([t0,t1,branch]),/duplicate iteration ordinal/);const gap=createIteration({id:"iter-2",artifactId:"artifact-1",ordinal:2,candidateRef:"d",createdAt:"2026-09-15T02:00:00Z",capabilityEpoch:epoch,parentIterationId:"iter-0"});assert.throws(()=>validateIterationLineage([t0,gap]),/contiguous/);});
test("iteration snapshots capability epoch provenance",()=>{const m={provider:"openai",model:"model-a",modelVersion:"1"};const i=createIteration({id:"s",artifactId:"a2",ordinal:0,candidateRef:"s",createdAt:"2026-09-15T00:00:00Z",capabilityEpoch:m});m.model="mutated";assert.equal(i.capabilityEpoch.model,"model-a");assert.ok(Object.isFrozen(i.capabilityEpoch));});
test("evaluation snapshots capability epoch provenance",()=>{const m={provider:"openai",model:"model-a"};const e=createEvaluation({id:"e",iterationId:t1.id,evaluatorId:"jc",capabilityEpoch:m,outcome:"PASS",findingIds:[],completedAt:"2026-09-15T01:20:00Z",critique:RAI_CRITIQUE_PROMPT});m.model="mutated";assert.equal(e.capabilityEpoch.model,"model-a");});
test("classification rejects findings from another iteration",()=>assert.throws(()=>classifyEvaluation({iterationId:t1.id,findings:[finding("wrong","MATERIAL","OPEN","iter-0")]}),/belongs to iteration/));
test("classification rejects duplicate finding identities",()=>{const same=finding("dup");assert.throws(()=>classifyEvaluation({iterationId:t1.id,findings:[same,same]}),/duplicate finding id/);});
test("STOP when only advisory findings remain",()=>assert.equal(classifyEvaluation({iterationId:t1.id,findings:[finding("a","ADVISORY")]}).decision,"STOP"));
test("CONTINUE for actionable findings within authority",()=>assert.equal(classifyEvaluation({iterationId:t1.id,findings:[finding("m")]}).decision,"CONTINUE"));
test("ESCALATE when repair exceeds authority",()=>assert.equal(classifyEvaluation({iterationId:t1.id,findings:[finding("b","BLOCKING")],authorityAllowsRepair:false}).decision,"ESCALATE"));
test("manual review pass is bounded to three findings from one iteration",()=>{const three=[finding("1"),finding("2"),finding("3")];assert.equal(enforceManualRaiReviewPolicy({iterationId:t1.id,findings:three}),true);assert.throws(()=>enforceManualRaiReviewPolicy({iterationId:t1.id,findings:[...three,finding("4")]}),/at most 3/);assert.throws(()=>enforceManualRaiReviewPolicy({iterationId:t1.id,findings:[finding("other","MATERIAL","OPEN","iter-0")]}),/belongs to iteration/);});
